---
phase: 03-paper-product-core
plan: 04
type: tdd
wave: 4
depends_on: [03-01, 03-02, 03-03, 03-10]
files_modified:
  - pa_agent/trading/gateways/paper/accounting_spot.py
  - pa_agent/trading/gateways/paper/faults.py
  - pa_agent/trading/gateways/paper/gateway.py
  - tests/unit/execution/test_paper_spot.py
  - tests/integration/execution/test_paper_spot_recovery.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "An approved paper Spot order reaches the PaperGateway only through the existing persisted permit, SQLite lease, and SubmissionCoordinator."
    - "Opening a buy reserves quote and opening a sell reserves base; partial fills settle only filled assets/fees and cancellation releases exactly the residual reservation."
    - "Explicit observations, not time, drive partial and later fills; reopened PaperGateway queries the same authoritative balances, order, and fill truth."
  artifacts:
    - path: pa_agent/trading/gateways/paper/accounting_spot.py
      provides: product-specific Decimal reserve, fill settlement, and cancellation-release projector
    - path: pa_agent/trading/gateways/paper/gateway.py
      provides: canonical TradingGateway Paper implementation and explicit scenario observation entry point
    - path: tests/integration/execution/test_paper_spot_recovery.py
      provides: real paper SQLite permit-chain/reopen lifecycle regression
  key_links:
    - from: SubmissionCoordinator.submit
      to: PaperGateway.submit_order
      via: existing leased OutboundSubmission only
    - from: PaperGateway.advance_market
      to: paper store and spot accounting
      via: one explicit versioned observation transaction with matching candidates
---

<objective>
Deliver the first full vertical PaperGateway slice: durable approved Spot submission, explicit-book partial fills, reservation accounting, cancellation, and restartable paper truth.

Purpose: Spot proves the shared gateway/store/matching contracts against a complete product accounting invariant without instant-fill behavior or central-ledger authority inversion.
Output: Spot accounting projector, PaperGateway implementation, deterministic fault seam, and focused unit/integration regressions.
</objective>

<execution_context>
@/root/.omp/agent/gsd-core/workflows/execute-plan.md
@/root/.omp/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-paper-product-core/03-CONTEXT.md
@.planning/phases/03-paper-product-core/03-RESEARCH.md
@.planning/phases/03-paper-product-core/03-01-PLAN.md
@.planning/phases/03-paper-product-core/03-02-PLAN.md
@.planning/phases/03-paper-product-core/03-03-PLAN.md
@pa_agent/trading/application/submission.py
@pa_agent/trading/domain/models.py
@pa_agent/trading/domain/lifecycle.py
@pa_agent/trading/ports/gateway.py
@pa_agent/trading/ports/ledger.py
@tests/integration/execution/test_uncertain_recovery.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify durable spot reservation, partial fill, cancellation, and reopen behavior</name>
  <files>tests/unit/execution/test_paper_spot.py, tests/integration/execution/test_paper_spot_recovery.py</files>
  <behavior>
    - A leased approved BUY reserves the Decimal quote maximum (including policy-defined fee exposure) before it is open; a SELL reserves base. Insufficient available assets rejects with canonical paper evidence and no fill.
    - An explicit shallow book partially fills a Spot order, transfers only the filled base/quote and exact fee, retains the residual reservation/order, and a later higher observation version can fill only remaining quantity.
    - Cancellation is first a request; whichever accepted paper event sequence wins against a fill determines terminal evidence. A cancellation terminal state releases only unfilled reservation and cannot reverse already settled fills.
    - A new PaperGateway instance on the same paper database returns identical client-ID lookup, fill list, open order, account snapshot, and versioned economic provenance; no central execution ledger is used as source truth.
    - Direct commands, caller-made lookalike outbound objects, recovery, and scenario APIs cannot obtain a second submission; the test submits only by `SubmissionCoordinator` with a real SQLite-leased permit.
  </behavior>
  <action>Write RED spot unit and filesystem integration tests using a real Phase 2 leased permit and `SubmissionCoordinator`, a configured PaperGateway, Decimal scenario books, and a dedicated paper database. Assert reserve/open accounting at submit, exact partial-fill settlement from explicit observations, later-observation completion, and cancellation residual release. Create deterministic interleavings where a cancellation request and a next observation receive ordered paper events, then assert only the persisted winner sets the state. Close/reopen the paper database and gateway and compare canonical account/order/fill truth including every Plan 03-01 provenance field. Exercise direct `submit_order` rejection for non-leased values where the existing port/type boundary permits that assertion, and assert recovery invokes only durable lookup. Per D-02/D-05, prove residual orders and reservations; per D-03/D-09/D-10, prove observation/event-sequence/restart behavior; per D-04, prove fill fee/slippage evidence exactness.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_spot.py tests/integration/execution/test_paper_spot_recovery.py</automated></verify>
  <done>The focused Spot suites fail because no PaperGateway/accounting projector connects the leased submission path to durable reserves, observation-driven fills, and restartable state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the paper Spot gateway and product accounting projector</name>
  <files>pa_agent/trading/gateways/paper/accounting_spot.py, pa_agent/trading/gateways/paper/faults.py, pa_agent/trading/gateways/paper/gateway.py, tests/unit/execution/test_paper_spot.py, tests/integration/execution/test_paper_spot_recovery.py</files>
  <behavior>
    - `PaperGateway` implements every existing `TradingGateway` evidence/query method with canonical Paper values and accepts only `OutboundSubmission` in `submit_order`.
    - `advance_market(observation)` is an explicit scenario/control API returning normalized durable change evidence; it does not poll, sleep, schedule background work, or allocate outbound authority.
    - Spot accounting writes reserve, transfer, fee, order state, fills, and snapshot in the same paper-store event transaction. No final balance is negative and each balance satisfies total = available + reserved.
  </behavior>
  <action>Implement `accounting_spot.py` as a product-only Decimal projector: validate available base/quote before opening; reserve buy quote or sell base; apply each ordered candidate fill using its frozen execution price/fee inputs; decrement reservation only for settled portion; and release exactly remainder at durable cancellation evidence. Implement a deterministic invocation-indexed `FaultPlan` seam in `faults.py` for later ambiguity tests, without random behavior. Implement `PaperGateway` in `gateway.py` around the Plan 03-02 store: it must implement canonical capabilities, rules, quotes, account, connection, count/rate/loss/fee observations, `submit_order`, cancel request, client-ID lookup, open-order/fill listing, and evidence-only reconcile. On submission, validate Paper Spot target/context, persist a paper open/order event and reservation, optionally match only against an already accepted observation, and return normalized nonterminal/filled evidence; never read the central ledger as account truth. `advance_market` accepts only typed `MarketObservation`, invokes the Plan 03-01 matcher and spot projector within the store transaction, and returns canonical change data for the later projector. Keep the only production invocation chain permit -> lease -> `SubmissionCoordinator.submit` -> `PaperGateway.submit_order`; do not add an overload for `ExecutionCommand`, a free client ID, ticket, UI callback, network adapter, wall-clock fill loop, or central projection in this plan. This implements D-01 through D-05 and preserves D-10 authority boundaries.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_spot.py tests/integration/execution/test_paper_spot_recovery.py tests/unit/execution/test_gateway_contract.py</automated></verify>
  <done>An approved leased Spot command produces independent durable reserves/orders/fills/balances, shallow depth leaves an observable cancellable remainder, and a reopened PaperGateway reports the same authoritative truth without any non-permit submission route.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | — | Safe auditable paper order lifecycle | 03-04 | COVERED | Full Spot vertical slice uses durable gateway truth and protected dispatch.
REQ | SIM-01 | Initial balances, fees, slippage, deterministic fills, restart recovery | 03-04 | COVERED | Configured account/policy, exact provenance, observation matching, reopen tests.
RESEARCH | Stage 2, shared matcher/separate accounting | Spot reserve/settle/release through existing lease path | 03-04 | COVERED | Product-only projector and PaperGateway isolate responsibility.
CONTEXT | D-01 | Explicit deterministic book matching | 03-04 | COVERED | Gateway only advances through typed observation/matcher.
CONTEXT | D-02 | Partial open/cancellable remainder | 03-04 | COVERED | Partial order/reservation persists until later observation/cancel.
CONTEXT | D-03 | No wall-clock lifecycle | 03-04 | COVERED | `advance_market` is explicit; no background timing mechanism.
CONTEXT | D-04 | Versioned Decimal fill economics | 03-04 | COVERED | Spot settlement consumes Plan 03-01 provenance unchanged.
CONTEXT | D-05 | Spot reserve/partial settlement/cancel release | 03-04 | COVERED | Accounting invariants and cancel-race regressions.
CONTEXT | D-09-D-10 | Sequence order and independent restart truth | 03-04 | COVERED | Store sequence decides races; new gateway reads paper database.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| lease -> PaperGateway.submit_order | Only a durable `OutboundSubmission` may open/reserve an order. |
| observation -> Spot projector | Accepted deterministic candidate fills may move virtual assets only in the paper transaction. |
| cancellation request -> terminal state | Request cannot claim cancellation; ordered paper evidence decides final state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-12 | Elevation | paper submit API | critical | mitigate | Preserve the existing `OutboundSubmission` type and sole coordinator route; no command/ticket overload exists. |
| T-03-13 | Tampering | Spot balances/reserves | critical | mitigate | Atomic Decimal reserve/settle/release invariants and nonnegative balance tests. |
| T-03-14 | Repudiation | partial/cancel outcome | high | mitigate | Append immutable fill/cancel evidence and choose outcome by persisted event sequence. |
| T-03-15 | Tampering | restart source of truth | critical | mitigate | Reopen paper database directly; central ledger is absent from gateway accounting queries. |
| T-03-SC | Tampering | package installs | low | accept | No package additions. |
</threat_model>

<verification>
Run focused Spot unit/integration tests and gateway port contract tests. Verify the test uses a real SQLite lease and coordinator, all economics are Decimal exact, no elapsed time causes a fill, and reopen returns the paper-owned account/order/fill truth.
</verification>

<success_criteria>
Paper Spot now behaves as a durable simulation rather than a fake: approved leased orders reserve correct assets, explicit book observations produce reproducible partial/full fills with versioned economics, cancellation releases only residual reserve, and restart reads independent paper truth.
</success_criteria>

<output>
Create `.planning/phases/03-paper-product-core/03-04-SUMMARY.md` when done.
</output>
