---
phase: 02-approval-and-risk-boundary
plan: 12
type: execute
wave: 8
depends_on: [02-05, 02-06, 02-08, 02-10]
files_modified:
  - pa_agent/trading/application/proposal.py
  - pa_agent/trading/domain/errors.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/integration/execution/test_proposal_kill_switch.py
autonomous: true
gap_closure: true
requirements: [SIM-03, SAFE-03]
must_haves:
  truths:
    - "持久化状态为 LATCHED 或重启后仍为 LATCHED 时，ProposalService 不会持久化 accepted RiskAssessment 或创建票据。"
    - "锁存期间产生的风险结论以受控拒绝审计事实持久化，保留 candidate/policy/evidence 绑定且不创建命令、claim、OutboundSubmission 或网关提交。"
  artifacts:
    - path: pa_agent/trading/application/proposal.py
      provides: 提议层 READY 前置检查和锁存竞态的受控风险拒绝归一化
    - path: pa_agent/trading/persistence/sqlite_ledger.py
      provides: accepted risk assessment 写入事务内的 READY 硬门
    - path: tests/integration/execution/test_proposal_kill_switch.py
      provides: LATCHED 与重启锁存的 accepted-risk persistence 回归
  key_links:
    - from: pa_agent/trading/application/proposal.py
      to: pa_agent/trading/ports/ledger.py
      via: get_kill_switch_state before evidence/risk and record_risk_assessment after accepted result
      pattern: get_kill_switch_state|record_risk_assessment
    - from: pa_agent/trading/persistence/sqlite_ledger.py
      to: kill_switch_state
      via: immediate transaction checks READY before inserting accepted proposal_risk_assessments row
      pattern: _require_ready_in_transaction|proposal_risk_assessments
  prohibitions:
    - "锁存状态不得持久化 accepted 风险评估、创建票据或生成任何出站授权。"
    - "ProposalService 不获得 gateway submit、SubmissionCoordinator 或替代执行网关。"
---

<objective>
把已持久化的 kill-switch READY 状态扩展到风险接受持久化边界，并证明重启无法绕过该硬门。

Purpose: 让锁存不仅拦截建票和出站，也阻止系统把新的风险接受当作可复用授权前提。
Output: ProposalService 的锁存拒绝归一化、SQLite accepted-assessment 事务门，以及 LATCHED/重启回归。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/02-approval-and-risk-boundary/02-05-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-06-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify no accepted-risk persistence while the durable latch is not READY</name>
  <files>tests/integration/execution/test_proposal_kill_switch.py</files>
  <read_first>Read `pa_agent/trading/application/proposal.py`, `pa_agent/trading/application/kill_switch.py`, `pa_agent/trading/persistence/sqlite_ledger.py` methods `record_risk_assessment`, `get_kill_switch_state`, and `_require_ready_in_transaction`, `tests/integration/execution/test_kill_switch.py`, `tests/integration/execution/test_intent_rejections.py`, and `02-VERIFICATION.md` gap 3.</read_first>
  <behavior>
    - After `KillSwitchService.latch()` changes the durable singleton to LATCHED, a normal ProposalService conversion-and-assessment request records no accepted assessment, issues no ticket, and produces no command, claim, outbound, or gateway submit call.
    - After closing and reopening the SQLite ledger, the same latched condition produces the same fail-closed outcome; the test verifies no accepted rows exist in `proposal_risk_assessments` and the controlled proposal audit fact is a risk rejection.
    - A direct accepted-assessment write attempted while LATCHED cannot insert an accepted row, proving the service precheck is backed by a durable transaction guard rather than only process memory.
  </behavior>
  <action>Create a new real-SQLite integration module with a deterministic clock and offline `ScriptedEvidenceGateway` / cancellation-capable latch collaborator. First produce and persist a normal candidate, latch through `KillSwitchService`, then call the normal `ProposalService.assess()` path and assert an unaccepted controlled risk result, no approval tickets, zero `order_commands` and `submission_claims`, zero submit calls, and no `accepted = 1` assessment row. Close and reopen the ledger while retaining LATCHED, repeat the assessment, and assert the same durable outcome. Include a direct ledger accepted-assessment attempt to require the transaction-level READY gate. Keep audit assertions to canonical reason/result fields and no raw gateway payloads or secrets.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_proposal_kill_switch.py</automated></verify>
  <acceptance_criteria>The tests fail before implementation and prove LATCHED plus post-restart LATCHED states cannot store an accepted risk assessment, issue a ticket, create outbound state, or call a gateway submit method.</acceptance_criteria>
  <done>SQLite regression coverage proves both the application and ledger boundaries fail closed for risk acceptance across restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate accepted risk persistence on durable READY state</name>
  <files>pa_agent/trading/application/proposal.py, pa_agent/trading/domain/errors.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_proposal_kill_switch.py</files>
  <read_first>Read the failing tests from Task 1, `pa_agent/trading/domain/approval.py` kill-switch values, `pa_agent/trading/domain/risk.py` `RiskAssessment`, `pa_agent/trading/application/proposal.py`, `pa_agent/trading/persistence/sqlite_ledger.py` methods `record_risk_assessment` and `_require_ready_in_transaction`, and `02-06-SUMMARY.md`.</read_first>
  <behavior>
    - ProposalService reads the durable kill state before collecting fresh evidence or accepting risk, converts a non-READY state into a controlled rejected assessment, persists only the rejection, and does not call ApprovalService.
    - SQLite checks READY inside the immediate transaction immediately before an accepted risk row would be inserted, so a latch committed after the service's preliminary check cannot persist acceptance.
  </behavior>
  <action>Add `KILL_SWITCH_NOT_READY` to `RiskRejectionReason`. In `ProposalService.assess()`, consult `ExecutionLedger.get_kill_switch_state()` before fresh evidence collection; for LATCHED or RECOVERING, construct a controlled rejected `RiskAssessment` with that stable reason, selected policy digest, and deterministic evidence/reason digest, persist the rejection, return it, and skip ApprovalService ticket creation. In `SQLiteExecutionLedger.record_risk_assessment()`, call `_require_ready_in_transaction()` only when `assessment.accepted` is true and before any accepted assessment/audit insert; preserve persistence of rejected audit facts while a latch is active. Handle the narrow durable READY failure in ProposalService by re-reading state and normalizing only a confirmed non-READY race into the same stable rejected result; re-raise unrelated ledger storage failures. Do not change the pure RiskEngine, cancellation/recovery evidence semantics, or the established `SubmissionCoordinator`-only submit path.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_proposal_kill_switch.py tests/integration/execution/test_kill_switch.py && .venv/bin/ruff check pa_agent/trading/application/proposal.py pa_agent/trading/persistence/sqlite_ledger.py tests/integration/execution/test_proposal_kill_switch.py</automated></verify>
  <acceptance_criteria>With LATCHED or restarted LATCHED state, durable tables contain no accepted risk assessment and no ticket/outbound records; rejected audit facts remain queryable, while READY behavior retains normal accepted assessment persistence.</acceptance_criteria>
  <done>SAFE-03 blocks risk acceptance at both proposal orchestration and SQLite persistence boundaries, including after restart and during a latch race.</done>
</task>

</tasks>

## Artifacts this phase produces

- Durable READY guard for accepted `proposal_risk_assessments` insertion in `pa_agent/trading/persistence/sqlite_ledger.py`.
- `RiskRejectionReason.KILL_SWITCH_NOT_READY` and proposal-level LATCHED/RECOVERING risk-rejection handling.
- `tests/integration/execution/test_proposal_kill_switch.py` covering LATCHED, restart, and direct durable-write rejection.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable kill-switch state -> proposal assessment | A process-local or stale proposal flow must not treat a latched system as risk accepted. |
| proposal assessment -> SQLite audit | Accepted risk state needs a transaction-time READY decision, while controlled rejection remains auditable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-22 | Elevation | `ProposalService.assess` | high | mitigate | Non-READY precheck returns a controlled rejected assessment and skips ticket issuance under SAFE-03. |
| T-02-23 | Tampering/Elevation | `SQLiteExecutionLedger.record_risk_assessment` | high | mitigate | Immediate transaction rechecks READY before accepted insert; LATCHED and reopen regressions prove durable fail-closed behavior. |
| T-02-24 | Repudiation | risk rejection audit | medium | mitigate | Persist controlled candidate/policy/evidence-bound risk rejections while preserving accepted-row exclusion. |
| T-02-SC | Tampering | package installs | low | accept | No package-manager operation is planned. |
</threat_model>

<verification>
Run the new proposal/latch regression and existing kill-switch integration test after each task. The focused command must pass before the summary is created.
</verification>

<success_criteria>
No LATCHED or restarted-LATCHED process can persist a risk-accepted result, create a ticket, or gain submission authority; every rejected result remains controlled and auditable.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-12-SUMMARY.md` when done.
</output>
