---
phase: 04-local-trading-workspace
plan: 05
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified:
  - pa_agent/trading/application/workspace_projection.py
  - pa_agent/trading/gateways/paper/store.py
  - tests/unit/execution/test_workspace_projection.py
  - tests/integration/execution/test_workspace_projection_reopen.py
  - tests/e2e/execution/test_trading_workspace.py
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "账户、订单、成交、reconciliation、错误和 latch 均源于 immutable persisted projection，而非 chart、CSV、widget cache 或 raw SQLite。"
    - "Spot、逐仓杠杆和 USDT 永续独立展示 source/last-success/freshness；stale 可读但不能决定 approval。"
    - "跨产品概览仅供查看，不能产生 risk/eligibility/submit authority。"
  artifacts:
    - path: pa_agent/trading/application/workspace_projection.py
      provides: "immutable, versioned, read-only WorkspaceProjectionV1 façade"
    - path: pa_agent/trading/gateways/paper/store.py
      provides: "typed product-scoped persisted projection reader"
  key_links:
    - from: pa_agent/trading/gateways/paper/store.py
      to: pa_agent/trading/application/workspace_projection.py
      via: "version-checked durable product facts feed frozen section DTOs"
---

<objective>
实现版本化、只读、按产品隔离的 workspace projection 与持久化 read API（D-05、D-06、D-07）。

Purpose: 让账户首屏展示完整 Paper/ledger truth 而不泄漏 store/ledger/网关 authority 到 GUI。
Output: typed persisted reader、immutable product/summary/readiness projection 和 reopen 行为。
</objective>

<execution_context>
@/root/.omp/agent/gsd-core/workflows/execute-plan.md
@/root/.omp/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-local-trading-workspace/04-01-PLAN.md
@.planning/phases/04-local-trading-workspace/04-02-PLAN.md
@.planning/phases/04-local-trading-workspace/04-03-PLAN.md
@.planning/phases/04-local-trading-workspace/04-04-PLAN.md
@.planning/phases/04-local-trading-workspace/04-RESEARCH.md
@.planning/phases/04-local-trading-workspace/04-UI-SPEC.md
@.planning/phases/04-local-trading-workspace/04-PATTERNS.md
@.planning/phases/03-paper-product-core/03-VERIFICATION.md
</context>

<interfaces>
`WorkspaceProjectionV1` must be frozen and include target digest, connection/reconciliation/config/latch status band, immutable `ProductWorkspaceSection` values, and display-only `CrossProductSummary`. It is scoped to the selected target and product; each section supplies persisted balances, positions, open orders, fills, capability, safe errors, source, `last_successful_reconciled_at`, and freshness. It must expose no risk result, approval verdict, permit, lease, gateway, PaperStore, ledger connection, or submit method.
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 提供版本化产品 scoped 的 Paper durable read API</name>
  <files>pa_agent/trading/gateways/paper/store.py, tests/integration/execution/test_workspace_projection_reopen.py</files>
  <read_first>pa_agent/trading/gateways/paper/store.py；pa_agent/trading/application/paper_projection.py；pa_agent/trading/domain/models.py；tests/integration/execution/test_workspace_projection_reopen.py；04-RESEARCH.md 的 Pattern 3；04-PATTERNS.md 的 Paper and ledger truth</read_first>
  <behavior>
    - 读取以 committed product snapshot/event sequence 为准，支持 Spot、isolated-margin、USDT-perpetual 的账户/仓位/订单/成交事实。
    - 每个 query 返回 typed/version-checked immutable values、capability/unavailable reason 和 independently recorded reconciliation metadata；不返回 raw payload 字典或 connection。
    - reopen/read/reconcile projection 不提交、不重放 market event、不伪造 terminal state。
  </behavior>
  <action>在 PaperStore 近源增加明确的只读产品投影查询（或专用 reader collaborator），从已持久化版本/sequence 重建 canonical facts。保留现有单 scope API 兼容性；不要让 GUI 解析 SQL/payload，不要把 generic gateway snapshot 的 Spot 偏向冒充其他 product。让 reopen integration test 通过真实 store/ledger 断言无二次 submit 和独立状态。</action>
  <verify><automated>.venv/bin/python -m pytest -q -o addopts='' tests/integration/execution/test_workspace_projection_reopen.py</automated></verify>
  <acceptance_criteria>三 product 的 unavailable、partial、stale、orders/fills 和 exact source time 均可从 durable truth 重建；读取不会造成 gateway call、new permit、lease 或 submission。</acceptance_criteria>
  <done>持久化读取边界能为 workspace 提供准确、重开安全、产品 scoped 事实。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 聚合 immutable workspace projection 并保护 summary 纯度</name>
  <files>pa_agent/trading/application/workspace_projection.py, tests/unit/execution/test_workspace_projection.py, tests/e2e/execution/test_trading_workspace.py</files>
  <read_first>pa_agent/trading/application/workspace_projection.py；pa_agent/trading/ports/ledger.py；pa_agent/trading/application/paper_runtime.py；tests/unit/execution/test_workspace_projection.py；tests/e2e/execution/test_trading_workspace.py；04-UI-SPEC.md 的“账户状态”“新鲜度、错误与刷新”</read_first>
  <behavior>
    - façade 汇集 typed Paper reader、ledger reconciliation/cancellation/latch 和 capability facts为一个 immutable target/product-bound `WorkspaceProjectionV1`。
    - section freshness 独立为 fresh/stale/refresh-failed/never-successful；刷新中保留 last successful content。
    - summary 只输出展示 totals/counts/time range 和固定 non-eligibility text，不计算 risk/exposure/approval readiness。
  </behavior>
  <action>扩展已在 Plan 04 建立的 module：定义 product section、safe error、cross-product summary 与 refresh result，并使用 app-level façade 组装。不混合不同 target digest/product 的值，任何 source/reconciliation failure 转为受控 section state。让 unit/e2e tests变绿，覆盖 read-only tables 的所有 account UI consideration entries（empty、loading、error、populated、partial、overflow、zero-one-many、long-text）。</action>
  <verify><automated>.venv/bin/python -m pytest -q -o addopts='' tests/unit/execution/test_workspace_projection.py tests/e2e/execution/test_trading_workspace.py</automated></verify>
  <acceptance_criteria>D-05 的 balances/positions 首屏数据、D-06 的 product grouping/display-only summary、D-07 的独立 freshness/stale 可读且不可资格化均通过 public `WorkspaceProjectionV1` 实现。</acceptance_criteria>
  <done>UI-02 的所有数据展示都能从一个 target/product-bound immutable `WorkspaceProjectionV1` 读取。</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Paper/ledger persistent truth → read model | 投影不能失真、造权威或引起写副作用。 |
| projection → UI summary | 展示汇总不得升格为 product risk/approval 判定。 |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | target-scoped projection result | high | mitigate | immutable target digest/section product IDs，加后续 worker callback triple guard；e2e switch tests覆盖错 target 拒绝。 |
| T-04-02 | Elevation of Privilege | projection summary | high | mitigate | DTO 不含 risk/permit/lease/submit，unit tests断言 display-only API，façade 保持 command/read 分界。 |
| T-04-03 | Information Disclosure | gateway error projection | high | mitigate | 仅传播 controlled safe code/message，raw durable payload/headers 不出 store/read model。 |
| T-04-04 | Tampering | terminal/recovery display | high | mitigate | 读取 persisted latch/cancellation/reconciliation evidence，stale/failed 不转换为 READY/terminal，reopen test覆盖。 |
| T-04-05 | Denial of Service | projection query | high | mitigate | read façade设计为 worker 调用；后续 QThread suite验证 delayed query 不阻塞 UI。 |
</threat_model>

<verification>
运行 unit、reopen integration、account pytest-qt 三个 focused suites。
</verification>

<success_criteria>
UI-02 通过 versioned persisted read model 覆盖，cross-product summary 永远只是方向性显示。
</success_criteria>

## Artifacts this phase produces
- `pa_agent/trading/gateways/paper/store.py`
- `pa_agent/trading/application/workspace_projection.py`

<output>
Create `.planning/phases/04-local-trading-workspace/04-05-SUMMARY.md` when done
</output>
