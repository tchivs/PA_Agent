---
phase: 02-approval-and-risk-boundary
plan: 09
type: execute
wave: 5
depends_on: [02-01, 02-05]
files_modified:
  - pa_agent/trading/application/intent_factory.py
  - pa_agent/trading/domain/errors.py
  - tests/fixtures/execution_factories.py
  - tests/unit/execution/test_intent_factory.py
  - tests/integration/execution/test_intent_rejections.py
autonomous: true
gap_closure: true
requirements: [CORE-03, SIM-03]
must_haves:
  truths:
    - "超过固定 60 秒来源分析时效的时区感知快照在候选创建前以稳定原因码拒绝。"
    - "过期来源经 ProposalService 留下持久化、可查询的转换拒绝事实，且不创建候选、票据、命令或网关提交。"
  artifacts:
    - path: pa_agent/trading/application/intent_factory.py
      provides: 注入时钟的固定来源分析时效策略和候选前拒绝
    - path: tests/integration/execution/test_intent_rejections.py
      provides: 2020 UTC 快照的持久化拒绝回归
  key_links:
    - from: pa_agent/trading/application/proposal.py
      to: pa_agent/trading/persistence/sqlite_ledger.py
      via: 捕获 stale-source ConversionRejection 后调用 record_conversion_rejection
      pattern: SOURCE_ANALYSIS_STALE|record_conversion_rejection
  prohibitions:
    - "IntentFactory 不获得 gateway、ledger admission、ApprovalService 或 SubmissionCoordinator 依赖。"
    - "过期来源不得跨越转换边界创建 CandidateExecutionIntent。"
---

<objective>
补齐分析建议进入候选意图前的确定性来源时效门，并将失败保持为可审计的转换拒绝。

Purpose: 防止旧分析在新鲜风险证据、票据和唯一出站授权之前进入执行链。
Output: 固定 60 秒来源时效策略、稳定 stale-source 原因码，以及 2020 UTC 快照的离线持久化回归。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/02-approval-and-risk-boundary/02-01-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify source-analysis freshness and durable stale rejection</name>
  <files>tests/fixtures/execution_factories.py, tests/unit/execution/test_intent_factory.py, tests/integration/execution/test_intent_rejections.py</files>
  <read_first>Read `pa_agent/trading/application/intent_factory.py`, `pa_agent/trading/application/proposal.py`, `pa_agent/trading/domain/errors.py`, `tests/fixtures/execution_factories.py`, `tests/unit/execution/test_intent_factory.py`, `tests/integration/execution/test_intent_rejections.py`, and `02-VERIFICATION.md` gap 1.</read_first>
  <behavior>
    - A snapshot completed exactly within the fixed 60-second source-analysis policy remains eligible when evaluated by an injected UTC clock; a snapshot older than that policy receives one stable conversion rejection reason before candidate construction.
    - A timezone-aware `2020-01-01T00:00:00+00:00` snapshot evaluated against a fixed current UTC clock is rejected through ProposalService, persists one conversion-rejected audit fact after SQLite reopen, and leaves candidate, ticket, command, claim, outbound, and gateway-submit counts at zero.
    - Test fixture defaults produce a current timezone-aware source timestamp at factory invocation so unrelated valid-path tests remain deterministic through their injected clocks.
  </behavior>
  <action>Write RED coverage first for D-01/D-02/D-03. Add an exact 60-second boundary and a strictly older case to `test_intent_factory.py`, using a fixed injected UTC clock and asserting the new `ConversionRejectionReason.SOURCE_ANALYSIS_STALE`. Update `make_source_analysis_snapshot()` so its default completion time is generated at factory invocation rather than frozen at module import. Extend `test_intent_rejections.py` with the timezone-aware 2020 snapshot regression: construct `IntentFactory` with the fixed current clock, call `ProposalService.propose()`, reopen the real SQLite ledger, and assert the controlled stale rejection appears before any candidate or submission-side row. Keep the gateway fake offline and assert its submit count stays zero.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_intent_factory.py tests/integration/execution/test_intent_rejections.py</automated></verify>
  <acceptance_criteria>Tests fail before implementation for a stale aware snapshot, then require the stable stale-source reason, one durable conversion rejection after reopen, zero candidate/ticket/order/claim rows, and zero gateway submission calls.</acceptance_criteria>
  <done>Executable regression coverage distinguishes current from stale source snapshots and proves stale analysis cannot reach candidate creation or any submission authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the fixed source freshness policy in IntentFactory</name>
  <files>pa_agent/trading/application/intent_factory.py, pa_agent/trading/domain/errors.py, tests/fixtures/execution_factories.py, tests/unit/execution/test_intent_factory.py, tests/integration/execution/test_intent_rejections.py</files>
  <read_first>Read the failing tests from Task 1, `pa_agent/trading/domain/approval.py`, `pa_agent/trading/application/intent_factory.py`, `pa_agent/trading/application/proposal.py`, `pa_agent/trading/persistence/sqlite_ledger.py` method `record_conversion_rejection`, and `02-01-SUMMARY.md`.</read_first>
  <behavior>
    - `IntentFactory` validates a snapshot's UTC age using an injected callable clock before it constructs `CandidateExecutionIntent`.
    - `ProposalService` continues to persist the typed conversion failure through its existing `record_conversion_rejection` boundary and receives no new submission capability.
  </behavior>
  <action>Define the fixed Phase 2 source-analysis maximum age as 60 seconds in `intent_factory.py`, injected through `IntentFactory`'s UTC-clock dependency with a production UTC default. Add `SOURCE_ANALYSIS_STALE` to `ConversionRejectionReason`, and make `_validate_snapshot()` reject only after validating an aware completion timestamp and before target/recommendation validation or candidate construction. Treat a future source completion timestamp as invalid completion time, preserve stable source ID/version/digest provenance required by D-03, and retain ProposalService's existing D-02 persistence path rather than adding a parallel audit or submission route. Do not import gateway, ledger, approval, AI, notify, GUI, records, or submission modules into the factory.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_intent_factory.py tests/integration/execution/test_intent_rejections.py && .venv/bin/ruff check pa_agent/trading/application/intent_factory.py pa_agent/trading/domain/errors.py tests/fixtures/execution_factories.py tests/unit/execution/test_intent_factory.py tests/integration/execution/test_intent_rejections.py</automated></verify>
  <acceptance_criteria>Current source snapshots still create immutable candidates, while stale and future timestamps yield controlled pre-candidate conversion failures; the durable stale rejection contains source metadata and no outbound-capable artifact exists.</acceptance_criteria>
  <done>CORE-03 now has a clock-testable, fail-closed source-analysis freshness boundary that records stale input before candidate creation.</done>
</task>

</tasks>

## Artifacts this phase produces

- `ConversionRejectionReason.SOURCE_ANALYSIS_STALE` in `pa_agent/trading/domain/errors.py`.
- Clock-injected 60-second source freshness enforcement in `pa_agent/trading/application/intent_factory.py`.
- Unit and real-SQLite regressions for a timezone-aware 2020 source snapshot.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted analysis snapshot -> IntentFactory | An old advisory record must not be replayed as a current candidate. |
| conversion rejection -> proposal audit ledger | Only stable source metadata and a controlled reason may become durable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Tampering/Elevation | `IntentFactory.propose` | high | mitigate | Injected UTC clock, fixed 60-second source policy, controlled stale reason, and SQLite reopen regression implement D-01/D-02/D-03. |
| T-02-17 | Repudiation | conversion rejection audit | medium | mitigate | Existing ProposalService-to-ledger rejection recording retains source metadata and stable reason codes under SIM-03. |
| T-02-SC | Tampering | package installs | low | accept | No package-manager operation is planned. |
</threat_model>

<verification>
Run the focused unit and SQLite integration tests after each task. Run `.venv/bin/pytest -q tests/unit/execution/test_intent_factory.py tests/integration/execution/test_intent_rejections.py` before creating the summary.
</verification>

<success_criteria>
An old, timezone-aware analysis snapshot deterministically produces a durable reason-coded rejection before a candidate, ticket, command, claim, outbound authorization, or gateway call can exist.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-09-SUMMARY.md` when done.
</output>
