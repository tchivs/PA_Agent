---
phase: 02-approval-and-risk-boundary
plan: 14
type: execute
wave: 1
depends_on: [02-13]
files_modified:
  - pa_agent/trading/domain/approval.py
  - pa_agent/trading/application/intent_factory.py
  - pa_agent/trading/application/risk_engine.py
  - tests/fixtures/execution_factories.py
  - tests/unit/execution/test_risk_engine.py
  - tests/integration/execution/test_approval_ticket_issuance.py
  - tests/integration/execution/test_approval_consumption.py
autonomous: true
gap_closure: true
requirements: [SAFE-02, SAFE-04, SIM-03]
must_haves:
  truths:
    - "合法 MARKET BUY 和 SELL 使用当前 ask 或 bid 作为风险价格，能够在所有限制满足时获得接受评估、票据和一次出站授权。"
    - "MARKET 的名义金额、精度、费用、敞口、偏离和余额检查使用同一侧的当前报价；LIMIT 继续使用候选限价。"
    - "Paper Spot SELL 在基础资产可用余额不足数量加基础资产计价费用时失败关闭。"
  artifacts:
    - path: pa_agent/trading/application/risk_engine.py
      provides: MARKET/LIMIT 分支的经济语义和 Spot BUY/SELL 余额门
    - path: tests/integration/execution/test_approval_consumption.py
      provides: MARKET 订单在新鲜消费边界的离线 SQLite 回归
  key_links:
    - from: pa_agent/trading/application/intent_factory.py
      to: pa_agent/trading/application/risk_engine.py
      via: MARKET candidate 的无候选限价表示与侧向当前报价定价
      pattern: OrderType.MARKET|quote.ask|quote.bid
---

<objective>
修复 policy 允许但风险层无法执行的 MARKET 语义，并为 Paper Spot SELL 加入基础资产可用余额门。

Purpose: 使新鲜、目标绑定的市场报价而非空的 MARKET 限价字段成为风险和审批链唯一经济输入。
Output: Decimal 纯风险规则、候选表示兼容性，以及从建票到消费的 MARKET BUY/SELL 回归。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/02-approval-and-risk-boundary/02-REVIEW.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-13-SUMMARY.md

<interfaces>
`IntentFactory` represents a valid MARKET recommendation with `price=None`; `RiskEngine.assess()` must therefore derive its execution price from the fresh `EvidenceBundle.quote`.

`RiskPolicy` stays the sole selected `phase2-v1` Paper Spot policy. `FeeRateObservation.fee_currency` determines whether an additional fee consumes USDT or the base asset.

`ApprovalService` refreshes evidence and reassesses before `consume_valid_ticket_and_begin_outbound`; no new gateway submit entry point is permitted.

`02-PATTERNS.md` assigns `application/intent_factory.py` and `application/risk_engine.py` to the pure `application/validation.py` analog: keep all side-price and balance arithmetic deterministic and free of gateway, ledger, UI, or mutable analysis dependencies. It assigns `domain/approval.py` to the frozen canonical-value pattern and `test_approval_consumption.py` to the real-SQLite row-count/reopen analog in `test_idempotency_recovery.py`; preserve the existing gateway-only dependency direction.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define MARKET and Spot SELL economic-boundary regressions</name>
  <files>tests/fixtures/execution_factories.py, tests/unit/execution/test_risk_engine.py, tests/integration/execution/test_approval_ticket_issuance.py, tests/integration/execution/test_approval_consumption.py</files>
  <behavior>
    - A MARKET BUY uses fresh ask and a MARKET SELL uses fresh bid for order notional, quantity precision, minimum/maximum notional, projected exposure, expected quote price, fee estimate, and price-deviation calculation; a LIMIT order retains its candidate price path.
    - Valid MARKET BUY and SELL candidates produced through `IntentFactory` can issue one ticket and can consume it once against fresh equivalent economic evidence.
    - SELL rejects for zero base balance, insufficient base balance, and a base-asset fee that makes an otherwise exact quantity balance insufficient; exact sufficient available balance is accepted.
  </behavior>
  <action>Write RED tests first for verification gap 1 and review finding CR-06. Per D-01, D-05, and D-08, build canonical MARKET candidates through `IntentFactory`, retaining `price=None`, then assert exact Decimal side pricing from ask for BUY and bid for SELL. Parameterize base-fee and quote-fee observations so SELL requires BTC available balance for quantity plus a BTC fee only when the fee currency is BTC; do not subtract a USDT fee from BTC availability. Assert accepted MARKET assessments preserve `order_notional`, expected quote, fee, deviation, and projected-exposure metrics. Follow the `02-PATTERNS.md` `test_risk_engine.py` pure-function analog and the `test_approval_consumption.py` real-SQLite row-count analog: add proposal issuance and refreshed consumption paths for both sides, checking accepted paths create only the existing single ticket/outbound artifacts and rejected SELL cases create no ticket, command, claim, outbound value, or gateway call. Keep D-01 through D-04 advisory conversion and D-09 review fields intact.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_risk_engine.py tests/integration/execution/test_approval_ticket_issuance.py tests/integration/execution/test_approval_consumption.py</automated></verify>
  <done>Failing tests distinguish MARKET from LIMIT economics, prove both MARKET sides traverse both authorization boundaries, and prove base-asset SELL funds are required before approval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement side-priced MARKET assessment and base-balance validation</name>
  <files>pa_agent/trading/domain/approval.py, pa_agent/trading/application/intent_factory.py, pa_agent/trading/application/risk_engine.py, tests/fixtures/execution_factories.py, tests/unit/execution/test_risk_engine.py, tests/integration/execution/test_approval_ticket_issuance.py, tests/integration/execution/test_approval_consumption.py</files>
  <action>Make the immutable candidate representation preserve the canonical MARKET invariant of absent limit price while preserving finite Decimal validation for LIMIT prices; align `IntentFactory` and factories with that invariant per D-01/D-02. Following the `02-PATTERNS.md` pure `application/validation.py` analog, make `RiskEngine.assess()` select one execution price before every economic calculation: candidate price for LIMIT, fresh ask for MARKET BUY, and fresh bid for MARKET SELL. Apply that value consistently to tick validation, notional limits, exposure, side-specific deviation, and fee estimation, while retaining existing quote-spread checks and all policy/evidence fail-closed reasons required by D-05/D-07/D-08. For BUY require quote-asset availability for notional plus quote-asset fee; for SELL derive the base asset from the selected supported symbol, require its available amount for quantity plus base-denominated fee, and reject unsupported symbol-to-base mapping. Preserve the `02-PATTERNS.md` gateway-only dependency direction: do not add margin, leverage, borrow, UI, adapter, ledger access, or alternate submission behavior.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_risk_engine.py tests/integration/execution/test_approval_ticket_issuance.py tests/integration/execution/test_approval_consumption.py &amp;&amp; .venv/bin/ruff check pa_agent/trading/domain/approval.py pa_agent/trading/application/intent_factory.py pa_agent/trading/application/risk_engine.py tests/fixtures/execution_factories.py tests/unit/execution/test_risk_engine.py tests/integration/execution/test_approval_ticket_issuance.py tests/integration/execution/test_approval_consumption.py</automated></verify>
  <done>Legal Paper Spot MARKET BUY/SELL orders can be assessed and approved from fresh side-specific prices, and underfunded SELL orders fail at risk before any approval or outbound authority exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fresh quote/account evidence -> MARKET risk | MARKET economics must derive from current side-specific canonical observations. |
| candidate SELL -> Paper Spot balance gate | A sell intent must not create a naked base-asset order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-30 | Tampering/Elevation | `RiskEngine.assess` | high | mitigate | Side-selected fresh quote drives all MARKET economic checks under D-05/D-08; end-to-end ticket regressions prove no bypass. |
| T-02-31 | Elevation | Paper Spot SELL | high | mitigate | Derive supported base asset and verify available quantity plus base fee before a risk acceptance. |
| T-02-SC | Tampering | package installs | low | accept | No package installation is planned. |
</threat_model>

<verification>
Run the focused unit and two real-SQLite integration files after each task, then `.venv/bin/pytest -q tests/unit/execution tests/integration/execution tests/property/execution` when this wave completes.
</verification>

<success_criteria>
The sole Phase 2 Paper Spot policy can accept a valid MARKET order using fresh side-specific economics, while a Spot SELL without enough available base asset never crosses the proposal or consumption boundary.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-14-SUMMARY.md` when done.
</output>
