---
phase: 03-paper-product-core
plan: 09
type: tdd
wave: 4
depends_on: [03-03, 03-10]
files_modified:
  - pa_agent/trading/application/intent_factory.py
  - pa_agent/trading/application/evidence_collector.py
  - pa_agent/trading/application/risk_engine.py
  - pa_agent/trading/application/proposal.py
  - tests/unit/execution/test_paper_product_admission.py
  - tests/unit/execution/test_risk_engine.py
  - tests/integration/execution/test_approval_consumption.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "Before a permit can exist, admission obtains fresh target-scoped product evidence and fails closed for every absent, stale, malformed, mismatched, or unsafe margin/perpetual fact."
    - "Isolated-margin admission accepts only its exact pair with current collateral, debt, health, borrow availability, and repay evidence; one pair's facts cannot satisfy another pair."
    - "USDT-perpetual admission accepts only isolated one-way evidence, bounded leverage, adequate initial/maintenance margin, and a candidate-digest-bound protective exit plan."
  artifacts:
    - path: pa_agent/trading/application/risk_engine.py
      provides: product-aware pre-permit risk admission that consumes only canonical candidate and evidence contracts
    - path: pa_agent/trading/application/evidence_collector.py
      provides: fresh pair/symbol-scoped paper evidence collection with canonical digest and observation-version binding
    - path: tests/unit/execution/test_paper_product_admission.py
      provides: fail-closed margin and perpetual admission regressions
  key_links:
    - from: FreshEvidenceCollector
      to: RiskEngine
      via: immutable target/product scope, observation version, canonical evidence digest, and selected policy identity
    - from: accepted RiskAssessment
      to: existing approval ticket issuance
      via: unchanged ProposalService and ledger path; no product fact may be supplied after assessment
---

<objective>
Make the existing pre-permit admission boundary product-aware without creating a new admission or dispatch path.

Purpose: D-06 and D-07 require the Paper margin/perpetual contexts to be checked against fresh product truth before a ticket or permit exists. Product accounting must never be asked to repair an unsafe or stale entry.
Output: Target-scoped evidence contracts, fail-closed evidence collection and risk checks, and approval-boundary regressions.
</objective>

<execution_context>
@/root/.omp/agent/gsd-core/workflows/execute-plan.md
@/root/.omp/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-paper-product-core/03-CONTEXT.md
@.planning/phases/03-paper-product-core/03-RESEARCH.md
@.planning/phases/03-paper-product-core/03-03-PLAN.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@pa_agent/trading/domain/risk.py
@pa_agent/trading/application/intent_factory.py
@pa_agent/trading/application/evidence_collector.py
@pa_agent/trading/application/risk_engine.py
@pa_agent/trading/application/proposal.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify fresh pair and perpetual admission evidence</name>
  <files>tests/unit/execution/test_paper_product_admission.py, tests/unit/execution/test_risk_engine.py, tests/integration/execution/test_approval_consumption.py</files>
  <behavior>
    - Margin admission requires fresh evidence keyed exactly by `(target_id, account_id, isolated_symbol)`: Decimal collateral, available collateral, debt principal, accrued interest, health, borrow availability, repayment status, observation version, observed time, and canonical digest. Missing, stale, mismatched, non-finite, or cross-pair evidence rejects before accepted assessment, ticket, permit, order, or fill.
    - Perpetual admission requires fresh evidence keyed exactly by `(target_id, account_id, symbol)`: isolated margin confirmation, one-way position-mode confirmation, maximum leverage, available/initial/maintenance margin, mark price, position/exposure fact, observation version, observed time, and canonical digest. Any absent or unsafe fact rejects before the same side effects.
    - The immutable candidate contains the Plan 03-03 context and protective-exit digest before evidence collection. A caller cannot add, replace, or mutate borrow/repay, leverage, mode, or exit-plan facts after analysis conversion, assessment, ticket review, or permit issuance.
  </behavior>
  <action>Write RED tests around `IntentFactory`, `FreshEvidenceCollector`, `RiskEngine`, `ProposalService`, and durable ticket consumption. Build exact Decimal Paper target fixtures for two margin pairs and one perpetual symbol. Assert pair isolation, debt/health/borrow/repay freshness, isolated one-way mode, leverage cap, initial/maintenance adequacy, and protective-exit digest binding. Assert every rejection leaves the durable accepted-assessment, ticket, permit, outbound-attempt, gateway-submit, and paper-fill counts unchanged. Exercise a mutation attempt after candidate creation and an evidence-scope substitution attempt; each must fail closed. Per D-06 and D-07, source analysis supplies order intent, the immutable product-admission request is bound at candidate creation, and only collector-owned fresh product observations supply account/margin facts.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_product_admission.py tests/unit/execution/test_risk_engine.py tests/integration/execution/test_approval_consumption.py</automated></verify>
  <done>Tests initially fail because current evidence and risk admission are Spot-only and cannot prove target-scoped margin/perpetual facts or prevent post-candidate fact injection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collect immutable fresh product evidence through the typed gateway port</name>
  <files>pa_agent/trading/application/evidence_collector.py, tests/unit/execution/test_paper_product_admission.py, tests/unit/execution/test_risk_engine.py</files>
  <behavior>
    - `FreshEvidenceCollector` obtains the immutable margin/perpetual values only through Plan 03-10 typed `TradingGateway` queries and rejects collection failure rather than using cached, caller-owned, or default values.
    - `EvidenceBundle` digest material binds selected target, product-context digest, observation version/time, and every immutable product observation to the assessment.
  </behavior>
  <action>Extend `FreshEvidenceCollector` to call only the Plan 03-10 exact pair/symbol `TradingGateway` methods for product-specific facts, then include their already canonical scope, freshness, version, and digest material in the existing `EvidenceBundle` hash. Reject unavailable, stale, mismatched, or malformed results before risk assessment. Retain the existing Spot collector path and do not accept a caller map, direct PaperStore value, or default product evidence. Do not redefine product evidence types in this plan: Plan 03-10 owns their domain contract, port, fake, durable PaperStore source, and PaperGateway reader.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_product_admission.py tests/unit/execution/test_risk_engine.py tests/unit/execution/test_paper_product_evidence.py</automated></verify>
  <done>Evidence is immutable, target/product scoped, canonical, and fresh; the collector cannot substitute another pair/symbol or access a paper store directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce product evidence in the existing pre-permit admission path</name>
  <files>pa_agent/trading/application/intent_factory.py, pa_agent/trading/application/risk_engine.py, pa_agent/trading/application/proposal.py, tests/unit/execution/test_paper_product_admission.py, tests/integration/execution/test_approval_consumption.py</files>
  <behavior>
    - `RiskEngine.assess` validates candidate target/context/exit digest, selected policy, evidence scope, age, observation version, Decimal values, and all product safety predicates before returning an accepted result.
    - Existing ProposalService, ticket, permit, lease, and SubmissionCoordinator APIs remain the only route after an accepted assessment.
  </behavior>
  <action>Extend `IntentFactory.propose` only to accept the already immutable Plan 03-03 product-admission request while creating the candidate; it must not expose a later setter or mutable mapping. Extend `RiskEngine` with product branches that reject cross/portfolio margin, any pair mismatch, unhealthy/insufficient/stale borrow-repay evidence, hedge/non-isolated perpetual mode, leverage above capability/policy, inadequate initial or maintenance margin, or a missing/mismatched exit digest. Wire `ProposalService` through these existing collaborators only. Preserve Paper Spot behavior and the verified proposal -> ticket -> permit -> lease -> `SubmissionCoordinator.submit` chain; do not introduce a caller-built evidence bypass, direct paper submission, network/UI code, or a generic leverage field. This completes D-06/D-07 pre-permit admission before Plans 03-05 through 03-07 mutate product state.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_product_admission.py tests/unit/execution/test_risk_engine.py tests/integration/execution/test_approval_consumption.py tests/integration/execution/test_uncertain_recovery.py</automated></verify>
  <done>Only fresh, exact, immutable Paper product facts can reach ticket issuance. Invalid margin/perpetual contexts fail before a permit or simulated fill, while the existing Spot path and sole submit route remain valid.</done>
</task>
</tasks>

<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | — | Safe complete Paper margin/perpetual lifecycle | 03-09 | COVERED | Entry is rejected before a permit when product truth is unsafe.
REQ | SIM-01 | Product-configured leverage and restart-safe paper semantics | 03-09 | COVERED | Fresh product evidence gates admission before accounting plans.
RESEARCH | Resolved fact authority | No mutable post-analysis product injection | 03-09 | COVERED | Candidate binds context; collector owns fresh account facts.
CONTEXT | D-06 | Pair-isolated collateral/debt/health/borrow-repay | 03-09 | COVERED | Exact pair-scoped fresh evidence and fail-closed predicates.
CONTEXT | D-07 | Isolated one-way leverage/margin/protective exit | 03-09 | COVERED | Symbol evidence, capability/policy checks, exit-digest binding.
CONTEXT | D-12 | No alternate submit/resubmit authority | 03-09 | COVERED | Only risk admission changes; dispatch contract is retained.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| product admission request -> candidate | Context is immutable and target-bound at candidate creation. |
| Paper gateway observations -> evidence bundle | Collector-owned fresh facts must match the exact account/product scope. |
| risk result -> ticket | Only a complete accepted assessment can continue through existing approval authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-34 | Tampering | margin pair evidence | critical | mitigate | Exact pair scope, canonical digest/version, freshness, and no-cross-pair acceptance tests. |
| T-03-35 | Elevation | perpetual mode/leverage/margin | critical | mitigate | Require isolated one-way capability, bounded leverage, adequate margins, and exit digest before permit. |
| T-03-36 | Tampering | post-analysis context injection | critical | mitigate | Freeze context into candidate digest before evidence/assessment; no mutable update API exists. |
| T-03-37 | Repudiation | rejected product admission | high | mitigate | Persist controlled rejection through existing proposal/risk audit path with no authority mutation. |
| T-03-SC | Tampering | package installs | low | accept | No package-manager operation is planned. |
</threat_model>

<verification>
Run the product-admission, risk-engine, approval-consumption, and uncertain-recovery suites. Each unsafe or stale product case must prove zero accepted assessment/ticket/permit/gateway/fill side effects.
</verification>

<success_criteria>
Product-aware risk admission is a distinct, testable pre-permit boundary. It obtains authoritative fresh Paper margin/perpetual evidence, binds no mutable product facts after candidate creation, and rejects every unsafe product case before accounting or dispatch.
</success_criteria>

<output>
Create `.planning/phases/03-paper-product-core/03-09-SUMMARY.md` when done.
</output>
