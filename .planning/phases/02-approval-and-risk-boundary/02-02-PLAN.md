---
phase: 02-approval-and-risk-boundary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pa_agent/trading/ports/credential_store.py
  - pa_agent/trading/security/__init__.py
  - pa_agent/trading/security/credentials.py
  - pa_agent/trading/security/redaction.py
  - pa_agent/config/settings.py
  - pa_agent/app_context.py
  - tests/unit/execution/test_secret_redaction.py
  - tests/integration/execution/test_secret_nonpersistence.py
autonomous: true
requirements: [SAFE-05]
must_haves:
  truths:
    - "交易设置只保留非机密凭据引用，绝不序列化凭据值。"
    - "合成秘密、签名、敏感请求字段和敏感异常内容不会进入交易审计、日志、通知或测试持久化输出。"
  artifacts:
    - path: pa_agent/trading/ports/credential_store.py
      provides: 仅按非机密引用查找秘密的端口
    - path: pa_agent/trading/security/redaction.py
      provides: 注册秘密和值/键名敏感信息的递归脱敏器
    - path: pa_agent/config/settings.py
      provides: 仅非机密执行目标和凭据引用的设置模型
  key_links:
    - from: pa_agent/config/settings.py
      to: pa_agent/trading/security/credentials.py
      via: TradingSettings 持久化 CredentialReference 而非凭据材料
      pattern: CredentialReference
---

<objective>
建立凭据引用和集中脱敏边界，使 Phase 2 的设置与审计可安全扩展而不选择或持久化真实 OS 凭据后端。

Purpose: 将未来外部适配器所需的秘密与现有通用设置、日志和记录输出隔离。
Output: CredentialStore 端口、引用值、递归脱敏器、非机密设置和合成秘密回归测试。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-VALIDATION.md

<interfaces>
From `pa_agent/config/settings.py`: `Settings.model_dump()` is written directly by `save_settings()`, so any trading settings model must contain references and non-secret policy/target fields only.

From `pa_agent/app_context.py`: `AppContext.bootstrap()` creates settings and logging before application services; additions must stay dependency-injected and must not give existing AI, UI, alert, or notification objects a gateway.

From `pa_agent/records/pending_writer.py`: the existing sanitizer replaces only one exact API-key value and is not the trading-boundary sanitizer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define credential-reference and recursive-redaction security tests</name>
  <files>tests/unit/execution/test_secret_redaction.py, tests/integration/execution/test_secret_nonpersistence.py</files>
  <read_first>Read `pa_agent/config/settings.py`, `pa_agent/records/pending_writer.py`, `pa_agent/app_context.py`, and `02-PATTERNS.md` section “Credential reference, recursive redaction, settings, and composition”.</read_first>
  <behavior>
    - A credential reference is non-secret metadata and a credential store receives only that reference; no concrete OS keychain provider is selected.
    - Recursive redaction handles mappings, sequences, strings, headers, query-like values, exception payloads, sensitive key names, registered secret values, and signature-like fields.
    - Saved settings and persisted trading audit payloads retain neither synthetic key/secret/passphrase nor a synthetic authorization/signature value.
  </behavior>
  <action>Create Wave 0 unit and integration tests using distinct synthetic secret values. Exercise nested mappings/sequences, exception-shaped values, header/query-shaped values, and a settings save/reload round-trip. Assert only non-secret credential references and controlled redaction tokens can cross the persistence boundary; test payloads must remain offline and must not contain real credentials.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_secret_redaction.py tests/integration/execution/test_secret_nonpersistence.py</automated></verify>
  <acceptance_criteria>The tests fail before implementation and prove both registered-value and sensitive-key-based redaction plus settings non-persistence.</acceptance_criteria>
  <done>Wave 0 security coverage independently detects leakage through nested data, exception-shaped data, and serialized settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement credential references, redaction, and non-secret composition</name>
  <files>pa_agent/trading/ports/credential_store.py, pa_agent/trading/security/__init__.py, pa_agent/trading/security/credentials.py, pa_agent/trading/security/redaction.py, pa_agent/config/settings.py, pa_agent/app_context.py, tests/unit/execution/test_secret_redaction.py, tests/integration/execution/test_secret_nonpersistence.py</files>
  <read_first>Read the tests created in Task 1, `pa_agent/trading/ports/clock.py`, `pa_agent/config/settings.py`, `pa_agent/app_context.py`, and `02-PATTERNS.md` section “Credential reference, recursive redaction, settings, and composition”.</read_first>
  <behavior>
    - `CredentialStore` is runtime-checkable and takes `CredentialReference`; it does not expose settings serialization or a storage backend.
    - `TradingSettings` defaults to Paper and can carry a non-secret reference and explicitly selected mode/policy identifiers; Live stays unavailable.
  </behavior>
  <action>Implement frozen `CredentialReference`, a narrow runtime-checkable `CredentialStore` port, and a reference-only unavailable/environment test provider that never persists returned secret material. Implement one centralized recursive redactor that replaces registered values and sensitive-field content before logging/audit/notification-facing use, while ledger-facing audit callers use explicit allowlisted fields and digests. Add a non-secret `TradingSettings` nested model to `Settings` with `Mode.PAPER` default and explicit Testnet selection semantics; reject secret-like fields through validation rather than storing them. Wire only these safe values through `AppContext.bootstrap()` after settings/logging initialization, without constructing a gateway, selecting a real credential provider, or passing submission authority to existing components.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_secret_redaction.py tests/integration/execution/test_secret_nonpersistence.py &amp;&amp; ruff check pa_agent/trading/security pa_agent/trading/ports/credential_store.py pa_agent/config/settings.py pa_agent/app_context.py tests/unit/execution/test_secret_redaction.py tests/integration/execution/test_secret_nonpersistence.py</automated></verify>
  <acceptance_criteria>All synthetic-secret tests pass; settings serialize only non-secret execution configuration and credential references, while centralized redaction is available for the later audit and application services.</acceptance_criteria>
  <done>SAFE-05 has a tested reference/non-persistence contract without a real credential backend, adapter, network path, or generic settings secret field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| credentials -> settings/logging/audit | Secret material must never cross into generic persistence or diagnostics. |
| credential reference -> provider | Only opaque reference metadata reaches the lookup port. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Information Disclosure | settings, logger, audit and notification payloads | high | mitigate | Reference-only settings plus centralized recursive redaction and synthetic end-to-end leakage tests. |
| T-02-06 | Elevation | credential provider selection | medium | mitigate | Narrow port exposes lookup by opaque reference only; no provider is selected or injected into advisory code. |
| T-02-SC | Tampering | package installs | low | accept | No package installation occurs; existing project dependencies provide the required contracts. |
</threat_model>

<verification>
Run the two security test files after every task; after Wave 1 merge, run `.venv/bin/pytest -q tests/unit/execution tests/integration/execution tests/property/execution`.
</verification>

<success_criteria>
No tracked Phase 2 settings or controlled audit/logging payload can retain synthetic credential material; later adapters have an explicit non-secret lookup contract.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-02-SUMMARY.md` when done.
</output>
