---
phase: 02-approval-and-risk-boundary
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - pa_agent/trading/domain/risk.py
  - pa_agent/trading/domain/errors.py
  - pa_agent/trading/application/risk_engine.py
  - tests/fixtures/execution_factories.py
  - tests/unit/execution/test_execution_target_policy.py
  - tests/unit/execution/test_risk_engine.py
autonomous: true
requirements: [SAFE-01, SAFE-02]
must_haves:
  truths:
    - "风险阈值来自明确选择并绑定到目标模式和产品的策略，而非全局默认值或票据临时输入。"
    - "不支持的模式、产品、精度、余额/保证金、杠杆、滑点、暴露、频率、开放订单或损失限制会确定性拒绝候选。"
  artifacts:
    - path: pa_agent/trading/domain/risk.py
      provides: 冻结、版本化、产品绑定的风险策略和评估值
    - path: pa_agent/trading/application/risk_engine.py
      provides: 无网关/账本依赖的纯确定性风险计算
  key_links:
    - from: pa_agent/trading/application/risk_engine.py
      to: pa_agent/trading/domain/approval.py
      via: CandidateExecutionIntent 的明确 ExecutionTarget 和产品上下文
      pattern: CandidateExecutionIntent|ExecutionTarget
---

<objective>
定义产品绑定、模式绑定且可复核的纯风险策略与评估，使新鲜取证层能够在没有任何临时覆盖的前提下确定性接受或拒绝候选。

Purpose: 将所有经济和产品风险判断从 GUI、通知和网关实现中移出。
Output: 风险领域模型、纯风险引擎、默认 Paper/Testnet 选择规则和 Wave 0 单元测试。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-VALIDATION.md
@.planning/phases/02-approval-and-risk-boundary/02-01-SUMMARY.md

<interfaces>
From Plan 02-01: `CandidateExecutionIntent` is frozen and bound to a `SourceAnalysisSnapshot` plus explicit `ExecutionTarget`; no mutable record or file path is available.

From `pa_agent/trading/domain/models.py`: `GatewayCapabilities`, `InstrumentRules`, `AccountObservation`, `QuoteObservation`, and `TimeObservation` are immutable canonical values. `UsdtPerpetualOrderContext` is the only context carrying leverage.

From `pa_agent/trading/application/validation.py`: pure validation functions use canonical command/rule values and raise typed domain failures; the risk engine must preserve this dependency-free structure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify selected-target policy and product-aware risk matrix</name>
  <files>tests/fixtures/execution_factories.py, tests/unit/execution/test_execution_target_policy.py, tests/unit/execution/test_risk_engine.py</files>
  <read_first>Read `pa_agent/trading/domain/models.py`, the completed `02-01-SUMMARY.md`, `tests/unit/execution/test_order_validation.py`, and `02-VALIDATION.md` rows for SAFE-01 and SAFE-02.</read_first>
  <behavior>
    - Paper is the default target; Testnet requires explicit target selection; Live cannot receive a policy that permits a candidate in this milestone.
    - Each policy version is bound to one mode and product and defines all relevant notional, precision, balance/margin, leverage, price deviation, slippage, exposure, frequency, open-order, and daily loss/drawdown limits.
    - The pure engine accepts only a matching candidate, selected target, policy, and complete canonical evidence input; each limit boundary returns a stable reason code.
  </behavior>
  <action>Write Wave 0 unit tests and fixture factories first. Use exact Decimal inputs to cover Paper default/Testnet explicit/Live disabled behavior, all listed SAFE-02 checks, isolated-margin context, and USDT-perpetual leverage bounds. Include a test that cross-product/global policy substitution and caller-provided threshold overrides are rejected, implementing D-08.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_execution_target_policy.py tests/unit/execution/test_risk_engine.py</automated></verify>
  <acceptance_criteria>The tests fail before implementation and make every safety boundary a deterministic input-output assertion without using a gateway or ledger.</acceptance_criteria>
  <done>Wave 0 covers selected-mode policy binding and each product-aware risk gate with stable reason codes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement immutable risk policy values and the pure risk engine</name>
  <files>pa_agent/trading/domain/risk.py, pa_agent/trading/domain/errors.py, pa_agent/trading/application/risk_engine.py, tests/fixtures/execution_factories.py, tests/unit/execution/test_execution_target_policy.py, tests/unit/execution/test_risk_engine.py</files>
  <read_first>Read the tests created in Task 1, `pa_agent/trading/domain/models.py`, `pa_agent/trading/application/validation.py`, and `02-PATTERNS.md` section “Fresh evidence collection and pure risk calculation”.</read_first>
  <behavior>
    - `RiskAssessment` records accepted/rejected result, reason codes, bounded metrics, policy version/digest, and evidence digest without mutable collections.
    - Risk evaluation has no import or call path to a gateway, ledger, GUI, notification, alert, or submission coordinator.
  </behavior>
  <action>Implement frozen product- and mode-scoped `RiskPolicy`, policy selector, limit summaries, canonical connection-health value, complete `EvidenceBundle`, and immutable `RiskAssessment` with canonical SHA-256 binding. Choose conservative fixed Paper test defaults and a short policy version in the policy object, not unbound call-site arguments; document the chosen values in code-level policy names/tests. Implement `RiskEngine.assess()` as a pure function over candidate, selected target, policy, and that complete evidence-bundle contract: enforce SAFE-01 and every SAFE-02 limit using finite Decimal arithmetic, product context, and stable reason codes. Reject unsupported capabilities and Live before an assessment can be accepted. Preserve D-08 by requiring exact selected-target/product/policy alignment and rejecting temporary or cross-product thresholds.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_execution_target_policy.py tests/unit/execution/test_risk_engine.py &amp;&amp; ruff check pa_agent/trading/domain/risk.py pa_agent/trading/domain/errors.py pa_agent/trading/application/risk_engine.py tests/unit/execution/test_execution_target_policy.py tests/unit/execution/test_risk_engine.py</automated></verify>
  <acceptance_criteria>All policy and risk tests pass; accepted assessments have immutable policy/evidence bindings, and all unsupported or out-of-limit inputs reject before any potential submission path.</acceptance_criteria>
  <done>A pure, reproducible policy evaluator exists for all Phase 2 product risk controls and does not use generic global execution thresholds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate and target -> risk engine | Candidate facts must be evaluated only against the policy selected for its exact environment and product. |
| policy selection -> assessment | A caller must not replace product policy with unbound threshold values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering/Elevation | `RiskEngine` policy inputs | high | mitigate | Exact mode/product policy binding, Live rejection, frozen digest-bearing assessments, and limit-boundary tests implement D-08. |
| T-02-07 | Denial of Service | malformed economic input | medium | mitigate | Canonical finite Decimal models and typed reason-coded failures prevent exceptional values from becoming accepted risk results. |
| T-02-SC | Tampering | package installs | low | accept | No package-manager changes are planned. |
</threat_model>

<verification>
Run both focused unit files, then `.venv/bin/pytest -q tests/unit/execution` after the plan completes.
</verification>

<success_criteria>
Every future risk decision can be reproduced from an immutable candidate, exact execution target, one product policy, and a complete evidence bundle.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-03-SUMMARY.md` when done.
</output>
