---
phase: 02-approval-and-risk-boundary
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - pa_agent/trading/ports/gateway.py
  - pa_agent/trading/ports/ledger.py
  - pa_agent/trading/application/proposal.py
  - pa_agent/trading/application/evidence_collector.py
  - pa_agent/trading/persistence/migrations.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/fixtures/fake_exchange.py
  - tests/integration/execution/test_intent_rejections.py
  - tests/integration/execution/test_fresh_evidence_risk.py
  - tests/integration/execution/test_approval_audit_ledger.py
autonomous: true
requirements: [SIM-03, SAFE-01, SAFE-02]
must_haves:
  truths:
    - "每次评估都从明确目标刷新能力、规则、账户、报价、服务器时间和连接状态；任何失败、过期、冲突或时钟异常都会拒绝。"
    - "候选、转换拒绝、风险结果和取证拒绝都以来源、策略和证据摘要的受控审计字段持久化。"
  artifacts:
    - path: pa_agent/trading/application/evidence_collector.py
      provides: 全有或全无的新鲜规范证据包收集
    - path: pa_agent/trading/application/proposal.py
      provides: 将纯转换结果以受控审计字段持久化的提议协调器
    - path: pa_agent/trading/persistence/sqlite_ledger.py
      provides: 提议和原因码审计事件的 SQLite 持久化
    - path: tests/fixtures/fake_exchange.py
      provides: 可脚本化的全量离线证据网关
  key_links:
    - from: pa_agent/trading/application/evidence_collector.py
      to: pa_agent/trading/ports/gateway.py
      via: 每轮调用能力、规则、账户、报价、时间和连接证据方法
      pattern: get_capabilities|get_instrument_rules|get_account_snapshot|get_quote|get_server_time
    - from: pa_agent/trading/application/evidence_collector.py
      to: pa_agent/trading/ports/ledger.py
      via: 将受控来源/策略/证据摘要与拒绝原因写入审计端口
      pattern: record_.*(proposal|rejection|assessment)
---

<objective>
把纯风险评估接到每次完整刷新、失败关闭的规范证据包和可查询审计账本，且不让缓存、原始载荷或异常文本参与授权。

Purpose: 防止旧规则、报价、账户或连接状态被重放为新的风险接受。
Output: 全量取证器、网关连接观察、审计账本迁移和真实 SQLite 离线集成覆盖。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-VALIDATION.md
@.planning/phases/02-approval-and-risk-boundary/02-01-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-03-SUMMARY.md
@.planning/phases/01-execution-foundation/01-07-SUMMARY.md

<interfaces>
From `pa_agent/trading/ports/gateway.py`: gateway evidence calls currently return canonical `GatewayCapabilities`, `RuleObservation`, `AccountObservation`, `QuoteObservation`, and `TimeObservation`; extend only with a canonical connection/target observation.

From `pa_agent/trading/persistence/sqlite_ledger.py`: `transaction()` creates a short `BEGIN IMMEDIATE` transaction; `_canonical_json()` and `_append_event()` are established durable encoding/audit helpers.

From Plan 02-03: `RiskEngine.assess(candidate, target, policy, evidence)` is pure; this plan supplies a new immutable evidence input and persists controlled outcomes around it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build scripted complete-evidence and audit integration tests</name>
  <files>tests/fixtures/fake_exchange.py, tests/integration/execution/test_intent_rejections.py, tests/integration/execution/test_fresh_evidence_risk.py, tests/integration/execution/test_approval_audit_ledger.py</files>
  <read_first>Read `tests/fixtures/fake_exchange.py`, `tests/integration/execution/test_refresh_before_validation.py`, `tests/integration/execution/test_idempotency_recovery.py`, `pa_agent/trading/ports/gateway.py`, and `02-VALIDATION.md` rows for CORE-03, SIM-03, and SAFE-02.</read_first>
  <behavior>
    - Each collection calls capability, rule, account, quote, time, and connectivity evidence in deterministic order on every attempt; a second evaluation makes a second complete call sequence.
    - Any unavailable, mismatched, stale, future, non-finite, clock-skewed, degraded, or contradictory observation yields a reason-coded durable reject and zero gateway submission calls.
    - Valid conversion proposals and all rejection/assessment outcomes are queryable from a reopened SQLite ledger with source, policy, evidence digests, timestamps, and controlled reason codes.
  </behavior>
  <action>Extend the existing offline fake with independently scripted canonical capability, rule, account, quote, server-time, and connection responses plus call-order recording; retain fail-fast submission behavior. Create real SQLite integration tests first for D-02 durable conversion rejects, D-05 full fresh evidence refresh, D-07 evidence failures, and SIM-03 controlled audit records. Include restart/reopen assertions and use a synthetic raw gateway failure value to prove only a reason code and allowlisted summary reach persistence.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_intent_rejections.py tests/integration/execution/test_fresh_evidence_risk.py tests/integration/execution/test_approval_audit_ledger.py</automated></verify>
  <acceptance_criteria>The integration tests fail before implementation and require a new full evidence call sequence, durable reason-coded audit rows, and no submission call on every rejection route.</acceptance_criteria>
  <done>Wave 0 proves fresh evidence, audit persistence, and stale/replayed-evidence rejection using only scripted canonical fakes and temporary SQLite files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fresh evidence collection and controlled SQLite audit persistence</name>
  <files>pa_agent/trading/ports/gateway.py, pa_agent/trading/ports/ledger.py, pa_agent/trading/application/proposal.py, pa_agent/trading/application/evidence_collector.py, pa_agent/trading/persistence/migrations.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/fixtures/fake_exchange.py, tests/integration/execution/test_intent_rejections.py, tests/integration/execution/test_fresh_evidence_risk.py, tests/integration/execution/test_approval_audit_ledger.py</files>
  <read_first>Read the tests created in Task 1, `pa_agent/trading/application/validation.py`, `pa_agent/trading/persistence/migrations.py`, `pa_agent/trading/persistence/sqlite_ledger.py`, and `02-PATTERNS.md` sections “Fresh evidence collection and pure risk calculation” plus “SQLite migration, append-only audit, and atomic consumption”.</read_first>
  <behavior>
    - `FreshEvidenceCollector.collect()` never reads a previous ledger observation as a fallback.
    - Audit rows use canonical JSON, SHA-256 digests, foreign keys/indexes, stable IDs, and bounded allowlisted fields; they never store raw gateway payloads, exception bodies, headers, signatures, or secret values.
  </behavior>
  <action>Add a canonical target/connection observation to the gateway contract and extend `ExecutionLedger` with proposal, reason-coded rejection, evidence/assessment, and later ticket-audit methods needed by this phase. Implement `ProposalService` as the sole coordinator that calls the pure `IntentFactory` and persists either candidate proposal metadata or D-02 conversion rejection; the factory itself stays ledger-free. Implement `FreshEvidenceCollector` to construct the Plan 02-03 `EvidenceBundle` by calling all six current target-scoped evidence sources each time, validate D-05 target/account/product/symbol alignment and age, and reject on D-07 error, contradiction, skew, or degraded connection without cache fallback. Add an ascending SQLite migration for proposals, controlled decision/audit events, evidence/assessment snapshots, and supporting indexes; implement transactional append/query helpers using canonical JSON plus digests and the shared redaction/allowlist boundary. Persist D-07 evidence/risk failures with source metadata and reason code only, then run the pure engine with D-08 policy binding. Do not invoke ledger admission, begin outbound submission, or `submit_order` in this plan.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_intent_rejections.py tests/integration/execution/test_fresh_evidence_risk.py tests/integration/execution/test_approval_audit_ledger.py &amp;&amp; ruff check pa_agent/trading tests/fixtures/fake_exchange.py tests/integration/execution/test_intent_rejections.py tests/integration/execution/test_fresh_evidence_risk.py tests/integration/execution/test_approval_audit_ledger.py</automated></verify>
  <acceptance_criteria>Every evaluation obtains an all-or-nothing fresh canonical bundle and writes an explainable bounded audit outcome; stale or failed evidence cannot become an accepted risk result.</acceptance_criteria>
  <done>Risk assessment now has a real SQLite-backed, reason-coded proposal/evidence audit trail and no stale evidence or raw gateway leakage path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway observations -> evidence bundle | Remote/current evidence is untrusted until exact target, time, freshness, and consistency checks pass. |
| evidence/assessment -> SQLite audit | Only allowlisted canonical summaries and hashes may be persisted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | `FreshEvidenceCollector` | high | mitigate | Refresh all required evidence on every attempt; validate freshness, target match, clock skew, and contradictions under D-05 and D-07. |
| T-02-08 | Information Disclosure | audit persistence | high | mitigate | Canonical allowlisted summaries, digest-only provenance, centralized redaction, and synthetic failure leakage tests. |
| T-02-09 | Repudiation | audit events | medium | mitigate | Append controlled source/policy/evidence digests and timestamps through transactional SQLite rows. |
| T-02-SC | Tampering | package installs | low | accept | No new packages are installed. |
</threat_model>

<verification>
Run the three named integration files after each task, then `.venv/bin/pytest -q tests/unit/execution tests/integration/execution tests/property/execution` at Wave 3 completion.
</verification>

<success_criteria>
No cached, stale, incomplete, contradictory, or raw gateway evidence can produce an accepted risk result; proposal and rejection provenance remains queryable after restart.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-04-SUMMARY.md` when done.
</output>
