---
phase: 01-execution-foundation
plan: 04
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - pa_agent/trading/application/__init__.py
  - pa_agent/trading/application/recovery.py
  - tests/fixtures/fake_exchange.py
  - tests/integration/execution/test_uncertain_recovery.py
  - tests/property/execution/test_lifecycle_machine.py
  - tests/integration/execution/test_idempotency_recovery.py
autonomous: true
requirements: [NFR-03]
must_haves:
  truths:
    - "Timeout, cancellation, restart, stream gap, and malformed acknowledgement remain pending or uncertain until canonical reconciliation evidence arrives."
    - "Recovery retains the first client ID, reconciliation job, and non-admissible submission state across restart."
    - "Generated failure schedules prove an unresolved logical command can never obtain a second admissible claim or cause a gateway submission."
  artifacts:
    - path: pa_agent/trading/application/recovery.py
      provides: Evidence-only recovery/reconciliation API over canonical ports and durable ledger state.
    - path: tests/fixtures/fake_exchange.py
      provides: Offline deterministic reconciliation-only gateway fake; its submit method fails if called.
    - path: tests/property/execution/test_lifecycle_machine.py
      provides: Stateful admission/idempotency/lifecycle invariants across restart and ambiguity schedules.
  key_links:
    - from: pa_agent/trading/application/recovery.py
      to: pa_agent/trading/ports/ledger.py
      via: Recovery loads persisted jobs and preserves their command/client/admission identities while appending evidence.
      pattern: "SubmissionAdmission|reconciliation"
    - from: pa_agent/trading/application/recovery.py
      to: pa_agent/trading/ports/gateway.py
      via: Recovery invokes lookup/reconciliation evidence methods only, never submit_order.
      pattern: "lookup_order_by_client_id|reconcile"
---

<objective>
Provide evidence-only recovery and a generated failure corpus proving D-06 and D-08: uncertain work retains its first identities and cannot regain submission admission after restart.

Purpose: Prove ambiguity and recovery behavior without a real adapter, paper gateway, or remote order submission.
Output: Recovery service, reconciliation-only fake gateway, integration tests, and a Hypothesis state machine.
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-execution-foundation/01-CONTEXT.md
@.planning/phases/01-execution-foundation/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/01-execution-foundation/01-01-SUMMARY.md
@.planning/phases/01-execution-foundation/01-02-SUMMARY.md
@.planning/phases/01-execution-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement evidence-only restart recovery</name>
  <files>pa_agent/trading/application/__init__.py, pa_agent/trading/application/recovery.py, tests/fixtures/fake_exchange.py, tests/integration/execution/test_uncertain_recovery.py</files>
  <behavior>
    - Test 1: timeout after possible acceptance, local cancellation, stream gap, and malformed acknowledgement persist uncertainty and retain reconciliation work rather than terminal result.
    - Test 2: after reopening SQLite, recovery enumerates unresolved jobs and queries only by each existing client order ID.
    - Test 3: absent/inconclusive evidence retains the original job and non-admissible state; definitive canonical evidence alone advances legal lifecycle state.
    - Test 4: recovery never calls submit_order; the fake submission method raises on invocation and its call count remains zero.
  </behavior>
  <action>Create a constructor-injected recovery service that scans startup jobs and reconciles one job using only canonical lookup/evidence methods with the persisted first client ID. Append normalized evidence through the ledger and central lifecycle guard; retain job and admission state after timeout, local cancellation, stream gap, malformed acknowledgement, empty lookup, or contradiction unless definitive evidence exists per D-06. Build a deterministic fake gateway whose evidence methods are scripted and whose submission method fails/asserts if called. Do not implement a submission coordinator, a remote adapter, polling loop, paper accounting, UI, credentials, or analysis conversion per D-09 and D-10.</action>
  <verify><automated>python -m pytest tests/integration/execution/test_uncertain_recovery.py -q</automated></verify>
  <done>Recovery uses the original durable client/job identities, makes no submit call, and keeps incomplete work unresolved until canonical evidence resolves it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove admission and lifecycle invariants under generated restart schedules</name>
  <files>tests/property/execution/test_lifecycle_machine.py, tests/integration/execution/test_idempotency_recovery.py, tests/integration/execution/test_uncertain_recovery.py</files>
  <behavior>
    - Test 1: generated create/claim/repeat/reopen/unknown/reconcile schedules yield one command, client ID, job, and admissible claim for each logical key; every repeat while unresolved is non-admissible.
    - Test 2: after restart, the first client ID and reconciliation job remain queryable and a repeated logical command cannot obtain a second admission claim.
    - Test 3: every terminal projection has definitive evidence; duplicate/out-of-order evidence is idempotent or recorded as an incident.
    - Test 4: fake gateway submit call count remains zero across every recovery schedule because Phase 1 implements no remote submission.
  </behavior>
  <action>Build a bounded Hypothesis `RuleBasedStateMachine` around the real transition guard, temporary SQLite ledger, recovery service, deterministic clock, and reconciliation-only fake gateway. Generate first admission, repeated logical command attempts, ambiguity, close/reopen, duplicate/out-of-order evidence, and definitive reconciliation. Assert after each operation that one logical key has one client ID/job, at most one durable admissible claim, every later unresolved attempt is non-admissible with the original identities, and fake gateway submission remains zero. Extend integration scenarios with exact before/after-restart assertions for the retained client ID/job and denied second claim. Keep tests offline, credential-free, and independent of Qt, LLM, recommendation CSV, chart data, paper accounting, adapters, and remote submission.</action>
  <verify><automated>python -m pytest tests/property/execution/test_lifecycle_machine.py tests/integration/execution/test_idempotency_recovery.py tests/integration/execution/test_uncertain_recovery.py -q</automated></verify>
  <done>Integration and property evidence prove the durable admission invariant, unresolved restart recovery, and zero remote submissions in Phase 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| persisted reconciliation job to recovery service | Restarted local state is incomplete and cannot be treated as remote outcome evidence. |
| gateway reconciliation response to ledger | External or fake observations become durable lifecycle evidence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | recovery/admission path | critical | mitigate | Recovery preserves original identities, invokes no submission, and property tests prove unresolved repeats cannot regain an admissible claim. |
| T-01-10 | Repudiation | ambiguous result handling | high | mitigate | Local interruption conditions retain uncertainty and append-only evidence; terminal state requires canonical evidence. |
| T-01-11 | Denial of service | restart recovery scan | medium | mitigate | Recovery returns bounded typed results and retains failed work rather than discarding it. |
| T-01-12 | Information disclosure | reconciliation evidence | medium | mitigate | Fixtures and persisted evidence contain canonical sanitized values only. |
| T-01-SC | Tampering | package installation | low | accept | No package installation is planned. |
</threat_model>

<verification>Run the task command, then `python -m pytest tests/unit/execution tests/property/execution tests/integration/execution -m "unit or property or integration" -q` and `python -m pytest -m "not live" -q` as the phase gate.</verification>

<success_criteria>After interruption or restart, unresolved work remains queryable with one original client ID/job and no second admissible submission claim; Phase 1 makes no remote submission.</success_criteria>

<output>Create `.planning/phases/01-execution-foundation/01-04-SUMMARY.md` when done.</output>
