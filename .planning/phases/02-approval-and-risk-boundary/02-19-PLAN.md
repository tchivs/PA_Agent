---
phase: 02-approval-and-risk-boundary
plan: 19
type: execute
wave: 5
depends_on: [02-18]
files_modified:
  - pa_agent/trading/domain/approval.py
  - pa_agent/trading/application/recovery_assessment.py
  - pa_agent/trading/application/kill_switch.py
  - pa_agent/trading/ports/ledger.py
  - pa_agent/trading/persistence/migrations.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/integration/execution/test_kill_switch.py
  - tests/property/execution/test_approval_kill_switch_machine.py
autonomous: true
gap_closure: true
requirements: [SAFE-03, SAFE-02, SIM-03]
must_haves:
  truths:
    - "每个持久化 RecoveryScope 只能由专用 recovery assessment 评估；账本仅在同一事务内以不可伪造的持久化 scope identity 查询当前 scope，并核验 target、policy 和 scope digest 后才分配 scope-bound assessment ID；该评估绝不能建票、命令、claim、dispatch permit、OutboundSubmission 或网关调用。"
    - "恢复 assessment 持久化 target/policy、完整新鲜证据、接受结果、时间和 scope digest；begin 与 complete 均在其各自 SQLite 事务中核验当前 scope 的 ID。"
    - "调用方构造不存在或篡改的 RecoveryScope，以及伪造、过期、拒绝、跨 scope、跨 target/policy/evidence 和重启路径，均不能写 recovery-assessment 行、不能获得 assessment ID、无法从 LATCHED/RECOVERING 转为 READY，且没有网关提交。"
    - "02-12 的非 READY 订单 RiskAssessment 硬门保持不变；恢复清除证明是不同领域记录，不能借 ProposalService 订单评估绕过该硬门。"
  artifacts:
    - path: pa_agent/trading/domain/approval.py
      provides: 与订单 RiskAssessment 分离的冻结 RecoveryScope 和 RecoveryAssessment 值
    - path: pa_agent/trading/application/recovery_assessment.py
      provides: 以持久化 RecoveryScope 为唯一输入的专用新鲜恢复评估服务
    - path: pa_agent/trading/persistence/migrations.py
      provides: 递增 migration 6 的 recovery scope 和 scope-bound assessment 表
    - path: pa_agent/trading/persistence/sqlite_ledger.py
      provides: recovery ID 与 scope/target/policy/evidence/freshness在 begin/complete 同事务核验
    - path: tests/integration/execution/test_kill_switch.py
      provides: 所有 recovery-assessment 伪造和错配路径的真实 SQLite 零 READY/零提交回归
  key_links:
    - from: pa_agent/trading/application/kill_switch.py
      to: pa_agent/trading/application/recovery_assessment.py
      via: 每个 ledger-provided RecoveryScope 生成独立受限 assessment ID
      pattern: RecoveryScope|RecoveryAssessment|record_recovery_assessment
    - from: pa_agent/trading/application/kill_switch.py
      to: pa_agent/trading/persistence/sqlite_ledger.py
      via: assessment 持久化、begin 与 complete 均在各自事务中按不可伪造的持久化 scope identity 查询当前 scope，并核验 target、policy、scope digest 和 assessment IDs
      pattern: record_recovery_assessment|persistent_scope_id|begin_kill_switch_recovery|complete_kill_switch_recovery|scope_digest
---

<objective>
用专用、scope-bound recovery-assessment 契约替代调用方布尔值；在 SQLite 内持久化并于 begin/complete 恢复转换时验证 ID、范围、目标、策略、完整新鲜证据和接受结果。

Purpose: 关闭 CR-02，阻止任意调用者、旧订单 RiskAssessment 或进程重启将锁存状态伪造成 READY，同时保留已完成的取消/对账和无提交恢复语义。
Output: 独立恢复领域值与服务、migration 6 持久化表、事务内 scope/assessment 验证，以及 integration/property 负向回归。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/02-approval-and-risk-boundary/02-REVIEW.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-06-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-12-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-18-SUMMARY.md

<interfaces>
Do not call `ProposalService.assess()` and do not reuse `RiskAssessment` or `proposal_risk_assessments` for recovery. Define a separate frozen `RecoveryAssessment` carrying `scope_digest`, target digest, policy version/digest, complete evidence digest and canonical evidence, accepted/reason-coded result, observed time, and a ledger-minted opaque `recovery_assessment_id`. `RecoveryAssessmentService.assess(scope)` accepts only a persisted `RecoveryScope`, refreshes full recovery evidence, evaluates no unresolved scope exposure/open orders/claims under the fixed policy, and returns no ticket or outbound-capable type.

Migration 6 must persist recovery scopes with target/policy/scope digest and a separate assessment table with target/policy/evidence/acceptance/time facts. `begin_kill_switch_recovery` and `complete_kill_switch_recovery` take the complete tuple of assessment IDs for the current persisted scope set; each method verifies exact one-to-one scope coverage in its own `BEGIN IMMEDIATE` transaction. This permits the fixed single-scope Phase 2 case without assuming future scopes are interchangeable.

`02-PATTERNS.md` assigns `domain/approval.py` to frozen canonical values, `kill_switch.py` to the evidence-only `application/recovery.py` analog, `migrations.py` to ascending schema migrations, `sqlite_ledger.py` to rowcount-checked short transactions and append-only audit, and the tests to real SQLite/state-machine patterns. Recovery evidence is complete and fresh: capabilities, rules, account/balances/positions, quote, server time, connection, open-order count, order-rate, loss/drawdown, and fee-rate observations are all target-bound and stored. Preserve dependency direction: recovery assessment may use canonical gateway evidence and ledger port, but has no ProposalService, approval service, submission coordinator, gateway submit, ticket, or outbound dependency.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify dedicated recovery-assessment zero-READY regressions</name>
  <files>tests/integration/execution/test_kill_switch.py, tests/property/execution/test_approval_kill_switch_machine.py</files>
  <behavior>
    - A normal recovery uses a newly collected, persisted scope-bound recovery assessment ID, not `assessment_accepted`, and reaches READY only after both begin and complete independently validate the current ID set.
    - Forged ID, expired ID, rejected assessment, assessment from another scope, target, policy, or full evidence bundle, incomplete cancellation/reconciliation, and a reopen between begin/complete leave the durable state non-READY and make zero gateway submit calls.
    - A recovery assessment never creates a proposal ticket, order command, claim, dispatch permit, OutboundSubmission, or proposal-order assessment row.
  </behavior>
  <action>Write RED real-SQLite and Hypothesis state-machine regressions for verification gap 4 and CR-02. Replace every caller Boolean in tests with the real dedicated recovery-assessment fixture. Create a latched system with persisted RecoveryScope data, then inject an invented ID, an old ID beyond the fixed freshness window, a rejected record, a record whose scope digest differs, and records whose target digest, policy digest, or complete evidence digest differs. Add a restart between RECOVERING and completion and assert it remains non-READY until fresh matching IDs are assessed and transactionally accepted again. For every negative case assert state, row counts for tickets/commands/claims/dispatch attempts, and `submit_call_count == 0`; query the recovery tables to prove full evidence and acceptance facts were stored separately from `proposal_risk_assessments`. Per D-05, D-06, D-07, D-08, D-11, and D-12, retain processed work, evidence-only cancellation, terminal reconciliation, actor action, and fail-closed invariants.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_kill_switch.py tests/property/execution/test_approval_kill_switch_machine.py</automated></verify>
  <done>RED tests demonstrate that no Boolean, order assessment, fake ID, stale ID, rejected ID, cross-boundary ID, or restart can reach READY or create an outbound side effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define and orchestrate the scope-bound recovery assessment domain</name>
  <files>pa_agent/trading/domain/approval.py, pa_agent/trading/application/recovery_assessment.py, pa_agent/trading/application/kill_switch.py, pa_agent/trading/ports/ledger.py, tests/integration/execution/test_kill_switch.py</files>
  <behavior>
    - RecoveryAssessmentService accepts only ledger-loaded RecoveryScope values and produces a separate reason-coded clearance result from fresh canonical recovery evidence; its ledger persistence request carries the scope's opaque persistent identity and cannot mint an assessment ID from a caller-constructed absent or tampered scope.
    - For recovery-assessment persistence, the ledger begins the operation by loading the current scope by that opaque persistent identity in the same transaction and verifies exact target, policy, and scope-digest equivalence before allocating an assessment ID or writing a row.
    - Real SQLite regressions prove a caller-constructed nonexistent or tampered RecoveryScope cannot write a recovery-assessment row, obtain an ID, enter RECOVERING or READY, or cause a gateway submit; KillSwitchService never accepts `assessment_accepted`, requests/persists one assessment per current scope, and passes only assessment IDs to both recovery transitions.
  </behavior>
  <action>Implement the dedicated contract without reusing ProposalService order assessment. Extend the domain with immutable `RecoveryScope` identity/target/policy/scope-digest invariants, including an opaque ledger-issued persistent scope identity that callers cannot manufacture into a valid scope, and a separate `RecoveryAssessment` value. Create `RecoveryAssessmentService` that receives a ledger-loaded scope, collects all ten fresh canonical observations (capabilities, rules, account/balances/positions, quote, server time, connection, open-order count, order-rate, loss/drawdown, fee rate), evaluates recovery clearance deterministically, and has no ticket, command, claim, dispatch, OutboundSubmission, or `ProposalService` dependency. Extend the ledger port with scope listing, recovery-assessment persistence/query, and begin/complete APIs that take assessment IDs instead of Booleans; make persistence accept the opaque persistent scope identity plus asserted target, policy, and scope digest so SQLite can verify them transactionally before ID allocation. Add real SQLite RED coverage that invokes this service/port with a caller-constructed nonexistent RecoveryScope and with a scope whose target, policy, or digest was tampered: each attempt must leave recovery-assessment row count unchanged, return no ID, retain LATCHED/RECOVERING rather than reaching READY, and retain `submit_call_count == 0`. Update KillSwitchService to obtain the current persisted scopes, request fresh assessments, persist them, and submit only their opaque IDs. Follow `02-PATTERNS.md`'s pure/evidence-only application-service analog and keep the recovery service gateway-submit-free. Explicitly preserve the Plan 02-12 hard gate: `ProposalService.record_risk_assessment` remains unable to persist accepted order assessments while non-READY; only the new restricted recovery table may record clearance while latched, and it cannot issue a ticket or outbound authority.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_kill_switch.py tests/property/execution/test_approval_kill_switch_machine.py</automated></verify>
  <done>Recovery uses an independent scope-bound domain/service and ledger-issued opaque IDs; real SQLite tests prove nonexistent or tampered caller RecoveryScope values write no recovery-assessment row, receive no ID, remain unable to reach RECOVERING/READY, and retain `submit_call_count == 0`, while non-READY order assessment, ticket, and outbound gates remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist scopes and verify assessment IDs inside both recovery transactions</name>
  <files>pa_agent/trading/persistence/migrations.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_kill_switch.py, tests/property/execution/test_approval_kill_switch_machine.py</files>
  <behavior>
    - Migration 6 persists immutable RecoveryScope target/policy/digest records and separate scope-bound recovery assessments with full canonical evidence, accepted result, timestamps, and opaque IDs.
    - Recovery-assessment persistence starts its own immediate transaction by loading the current scope using the opaque persistent scope identity, then verifies exact target, policy, and scope-digest matches before it writes a row or allocates an assessment ID.
    - Both begin and complete recovery load the active persisted scopes and conditionally verify exact ID-to-scope/target/policy/evidence/freshness/acceptance matches in their own immediate transactions before a state transition; nonexistent or tampered caller scopes therefore cannot create a record, obtain an ID, enter RECOVERING/READY, or submit through the gateway.
  </behavior>
  <action>Implement ascending migration 6 according to `02-PATTERNS.md`: add recovery-scope and recovery-assessment tables with a ledger-generated opaque persistent scope identity, foreign keys, canonical JSON/digests, indexes for current scope/ID lookup, and append-only transition audit references. Persist scope target/policy snapshots at latch/recovery-scope creation rather than deriving an unbound scope from a caller. For `record_recovery_assessment`, start an immediate SQLite transaction, query the current scope by the opaque persistent scope identity, and before allocating any assessment ID or inserting any recovery-assessment row require the supplied scope's target digest, policy version/digest, and scope digest to exactly match the loaded durable scope. Reject absent scope identities and every mismatch without assigning an ID or writing an assessment row. Persist each full fresh evidence bundle, acceptance/reason result, assessment time, and ledger-generated ID in the recovery table only after this validation. In `begin_kill_switch_recovery` and `complete_kill_switch_recovery`, start separate immediate transactions, load the current durable scopes, require exact one-to-one provided ID coverage, validate accepted status, fixed-window freshness, scope digest, target digest, policy version/digest, and complete evidence digest, then retain `_recovery_facts_are_clear_in_transaction` and conditionally transition with rowcount checks. Add and make pass real SQLite regressions that invoke recovery-assessment persistence with a nonexistent scope and separately tampered target, policy, and scope digest; assert unchanged recovery-assessment rows, no returned ID, no RECOVERING/READY transition, and `submit_call_count == 0`. On any failed predicate, make no READY transition and append no authority. Preserve D-12 audit events, D-11 transaction semantics, 02-12's order-assessment READY guard, cancellation-only behavior, and the gateway-only submission boundary.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_kill_switch.py tests/property/execution/test_approval_kill_switch_machine.py &amp;&amp; .venv/bin/pytest -q tests/unit/execution tests/integration/execution tests/property/execution &amp;&amp; .venv/bin/ruff check pa_agent/trading/domain/approval.py pa_agent/trading/application/recovery_assessment.py pa_agent/trading/application/kill_switch.py pa_agent/trading/ports/ledger.py pa_agent/trading/persistence/migrations.py pa_agent/trading/persistence/sqlite_ledger.py tests/integration/execution/test_kill_switch.py tests/property/execution/test_approval_kill_switch_machine.py</automated></verify>
  <done>Recovery-assessment persistence and both recovery transitions query only current persisted scopes transactionally. Real SQLite regressions prove nonexistent or tampered caller RecoveryScope values create no recovery-assessment row, receive no ID, cannot enter RECOVERING or READY, and retain `submit_call_count == 0`; all forged, stale, rejected, mismatched, and restart cases also remain non-READY with zero submit calls.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | - | Fresh deterministic controls before a typed command can submit; advisory paths stay non-submittable | 02-14 to 02-19 | COVERED | MARKET/SELL correctness, redacted output, explicit binding, verified dispatch, and scope-bound recovery close every remaining truth.
REQ | CORE-03 | Deterministic advisory conversion with no submission authority | 02-14, 02-15, 02-18 | PRESERVED | MARKET conversion remains typed; output and dispatch work do not grant advisory producers authority.
REQ | SIM-03 | Durable proposed/approved/rejected/submitted/recovery audit | 02-15, 02-16, 02-18, 02-19 | COVERED | Sanitized audit, refresh facts, dispatch lease, recovery scopes, and assessments are persisted.
REQ | SAFE-01 | Explicit Paper Spot boundary | 02-14, 02-16, 02-19 | PRESERVED | All policies/targets remain the fixed Paper Spot selection.
REQ | SAFE-02 | Product-aware complete fresh risk controls | 02-14, 02-16, 02-19 | COVERED | MARKET/SELL funds, strict binding equivalence, and independent recovery clearance fail closed.
REQ | SAFE-03 | Persistent latched kill switch and deliberate recovery | 02-18, 02-19 | COVERED | Verified dispatch cannot bypass latch; recovery requires persisted scope-bound assessments.
REQ | SAFE-04 | Single-use expiring operator approval | 02-16, 02-17, 02-18 | COVERED | D-10 bindings, two-stage contract, and persistent one-time lease are enforced.
REQ | SAFE-05 | Credential isolation and secret-free outputs | 02-15 | COVERED | Every actual credential, SQLite, log, notification, and record producer uses centralized output sanitization or controlled codes.
RESEARCH | - | No packages; immutable Decimal, pure functions, SQLite transactions, offline fakes | 02-14 to 02-19 | COVERED | Plans use existing stack, fresh evidence, canonical values, migration ordering, rowcount, and fake-gateway tests.
CONTEXT | D-01-D-04 | Frozen advisory conversion and no advisory submission | 02-14, 02-15, 02-17, 02-18 | COVERED | MARKET remains typed; records/notifications stay advisory; only leased ledger value reaches the gateway.
CONTEXT | D-05-D-08 | Complete fresh target-policy evidence and fixed controls | 02-14, 02-16, 02-19 | COVERED | Side quotes, per-observation freshness, semantic equivalence, and recovery evidence are mandatory.
CONTEXT | D-09-D-12 | Complete review, binding invalidation, single consumption, durable terminal events | 02-14, 02-16, 02-18, 02-19 | COVERED | Review facts remain bound, lease is one-time, and recovery validation/auditing preserves lifecycle safety.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted RecoveryScope -> RecoveryAssessmentService | Service can assess only a durable scope, never a caller-invented target/policy scope. |
| recovery assessment ID -> begin/complete transaction | IDs are untrusted until exact scope/target/policy/evidence/freshness/acceptance verification under SQLite transaction. |
| recovery assessment recording -> order authorization | The separate clearance record cannot become a proposal assessment, ticket, command, dispatch permit, or gateway call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-39 | Elevation/Tampering | recovery transition API | critical | mitigate | Remove Boolean APIs; both begin and complete transactionally verify ledger IDs against active persisted scope sets. |
| T-02-40 | Repudiation | recovery evidence/audit | high | mitigate | Migration 6 retains scope/target/policy digests, full canonical evidence, accepted result, time, ID, and transition audit references. |
| T-02-41 | Elevation | cross-scope or stale clearance | high | mitigate | Exact one-to-one scope ID coverage plus policy/target/evidence/freshness checks; negative real-SQLite and restart tests hold non-READY. |
| T-02-42 | Elevation | recovery assessment reuse as order risk | critical | mitigate | Separate domain/table/service; retain 02-12 non-READY order assessment hard gate and prohibit ticket/outbound dependencies. |
| T-02-SC | Tampering | package installs | low | accept | No package-manager action is planned. |
</threat_model>

<verification>
Run focused kill-switch integration/property tests after each task. Completion requires the full offline execution corpus and scoped Ruff command in Task 3.
</verification>

<success_criteria>
CR-02 is closed only when recovery-assessment persistence uses a same-transaction query by opaque persistent scope identity and verifies target, policy, and scope digest before it can allocate an ID or write a row; no caller Boolean, caller-constructed nonexistent/tampered RecoveryScope, forged/expired/rejected/cross-boundary assessment ID, old order assessment, or restart can enter RECOVERING or transition to READY, create a recovery-assessment row or ID, create approval/outbound authority, or increment `submit_call_count`.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-19-SUMMARY.md` when done.
</output>
