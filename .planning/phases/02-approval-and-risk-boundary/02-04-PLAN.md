---
phase: 02-approval-and-risk-boundary
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - pa_agent/trading/ports/gateway.py
  - pa_agent/trading/application/evidence_collector.py
  - tests/fixtures/fake_exchange.py
  - tests/integration/execution/test_fresh_evidence_risk.py
autonomous: true
requirements: [SAFE-01, SAFE-02]
must_haves:
  truths:
    - "每次评估都从明确目标刷新能力、规则、账户、报价、服务器时间、连接状态、开放订单、频率窗口、当日损失/回撤和费用率；任何失败、过期、冲突或时钟异常都会拒绝。"
  artifacts:
    - path: pa_agent/trading/application/evidence_collector.py
      provides: 全有或全无的新鲜规范证据包收集
    - path: tests/fixtures/fake_exchange.py
      provides: 可脚本化的全量离线证据网关及当前账户风险/费用观察
  key_links:
    - from: pa_agent/trading/application/evidence_collector.py
      to: pa_agent/trading/ports/gateway.py
      via: 每轮调用能力、规则、账户、报价、时间、连接、开放订单、频率窗口、损失/回撤和费用率证据方法
      pattern: get_capabilities|get_instrument_rules|get_account_snapshot|get_quote|get_server_time|get_open_order_count|get_order_rate_window|get_loss_drawdown|get_fee_rate
---

<objective>
把纯风险评估接到每次完整刷新、失败关闭的规范证据包，且不让缓存、原始载荷或异常文本参与授权。

Purpose: 防止旧规则、报价、账户或连接状态被重放为新的风险接受。
Output: 全量取证器、网关目标/账户风险/费用观察和离线集成覆盖；下一计划负责将结果写入审批账本。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-VALIDATION.md
@.planning/phases/02-approval-and-risk-boundary/02-01-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-03-SUMMARY.md
@.planning/phases/01-execution-foundation/01-07-SUMMARY.md

<interfaces>
From `pa_agent/trading/ports/gateway.py`: gateway evidence calls currently return canonical `GatewayCapabilities`, `RuleObservation`, `AccountObservation`, `QuoteObservation`, and `TimeObservation`; extend only with a canonical connection/target observation.

From Plan 02-03: `RiskEngine.assess(candidate, target, policy, evidence)` is pure; this plan supplies its new immutable evidence input only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build scripted complete-evidence integration tests</name>
  <files>tests/fixtures/fake_exchange.py, tests/integration/execution/test_fresh_evidence_risk.py</files>
  <read_first>Read `tests/fixtures/fake_exchange.py`, `tests/integration/execution/test_refresh_before_validation.py`, `pa_agent/trading/ports/gateway.py`, and `02-VALIDATION.md` row for SAFE-02.</read_first>
  <behavior>
    - Each collection calls capability, rule, account, quote, time, connectivity, current open-order count, policy-window order count, UTC-day loss/drawdown, and fee-rate evidence in deterministic order on every attempt; a second evaluation makes a second complete call sequence.
    - Any unavailable, mismatched, stale, future, non-finite, clock-skewed, degraded, or contradictory observation yields a reason-coded rejection for the following ledger coordinator to persist, and zero gateway submission calls.
    - Missing, expired, or target/product/symbol/quote-mismatched fee rates reject before a risk acceptance or ticket can be created; the risk engine receives the exact Decimal estimate and rate version.
  </behavior>
  <action>Extend the existing offline fake with independently scripted canonical capability, rule, account, quote, server-time, connection, open-order-count, order-rate-window, UTC-day loss/drawdown, and fee-rate responses plus call-order recording; retain fail-fast submission behavior. Create integration tests first for D-05 full fresh evidence refresh and D-07 evidence failures. Make each current-account observation identify the selected target/account/product and its observation instant; make rate data state the exact policy window and loss/drawdown state the exact UTC day. Include missing, expired, and target/product/symbol/quote-mismatched fee-rate cases, assert the Decimal fee estimate/rate version reaches the assessment, and assert every reject path has no submit attempt or cache fallback.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_fresh_evidence_risk.py</automated></verify>
  <acceptance_criteria>The integration tests fail before implementation and require a new complete call sequence, including current-account counters and fee rates, with no submission call on every rejection route.</acceptance_criteria>
  <done>Wave 0 proves fresh current evidence, current-account limit inputs, and fee-rate rejection using only scripted canonical fakes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fresh evidence collection and canonical account-risk inputs</name>
  <files>pa_agent/trading/ports/gateway.py, pa_agent/trading/application/evidence_collector.py, tests/fixtures/fake_exchange.py, tests/integration/execution/test_fresh_evidence_risk.py</files>
  <read_first>Read the tests created in Task 1, `pa_agent/trading/application/validation.py`, `pa_agent/trading/security/redaction.py`, and `02-PATTERNS.md` section “Fresh evidence collection and pure risk calculation”.</read_first>
  <behavior>
    - `FreshEvidenceCollector.collect()` never reads a previous ledger observation as a fallback.
    - The collector returns only frozen canonical observations and stable reason codes; no raw gateway payload, exception body, header, signature, or secret value crosses the collection boundary.
  </behavior>
  <action>Add canonical target/connection observations plus target-bound `get_open_order_count`, `get_order_rate_window`, `get_loss_drawdown`, and `get_fee_rate` gateway methods. Their return values must expose target/account/product/symbol identity, observed time, and respectively exact count/window, UTC-day loss/drawdown, or rate version/quote identifier/fee currency. Implement `FreshEvidenceCollector` to construct the Plan 02-03 `EvidenceBundle` by calling every target-scoped source on every evaluation, validate D-05 target/account/product/symbol/quote alignment and age, and reject on D-07 error, contradiction, skew, degraded connection, missing counters, or invalid fee inputs without cache fallback. It returns controlled typed reasons and canonical values only so the following approval-ledger plan can persist them through the shared D-02 redaction/allowlist boundary. Run the pure engine only after the complete bundle is present with D-08 policy binding. Do not invoke ledger admission, ticket creation, begin outbound submission, or `submit_order` in this plan.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_fresh_evidence_risk.py &amp;&amp; ruff check pa_agent/trading/ports/gateway.py pa_agent/trading/application/evidence_collector.py tests/fixtures/fake_exchange.py tests/integration/execution/test_fresh_evidence_risk.py</automated></verify>
  <acceptance_criteria>Every evaluation obtains an all-or-nothing fresh canonical bundle containing current-account and fee inputs; stale or failed evidence cannot become an accepted risk result or reach any submission path.</acceptance_criteria>
  <done>Risk assessment has a focused, fail-closed evidence source with no stale evidence, raw gateway leakage, audit persistence, or gateway submission path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway observations -> evidence bundle | Remote/current evidence is untrusted until exact target, time, freshness, and consistency checks pass. |
| evidence/assessment -> SQLite audit | Only allowlisted canonical summaries and hashes may be persisted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Tampering | `FreshEvidenceCollector` | high | mitigate | Refresh all required evidence on every attempt; validate freshness, target match, clock skew, and contradictions under D-05 and D-07. |
| T-02-08 | Information Disclosure | audit persistence | high | mitigate | Canonical allowlisted summaries, digest-only provenance, centralized redaction, and synthetic failure leakage tests. |
| T-02-09 | Repudiation | audit events | medium | mitigate | Append controlled source/policy/evidence digests and timestamps through transactional SQLite rows. |
| T-02-SC | Tampering | package installs | low | accept | No new packages are installed. |
</threat_model>

<verification>
Run the focused fresh-evidence integration file after each task, then `.venv/bin/pytest -q tests/unit/execution tests/integration/execution tests/property/execution` at Wave 3 completion.
</verification>

<success_criteria>
No cached, stale, incomplete, contradictory, or raw gateway evidence can produce an accepted risk result; the next plan persists proposal and rejection provenance through the approval ledger.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-04-SUMMARY.md` when done.
</output>
