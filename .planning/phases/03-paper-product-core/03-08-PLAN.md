---
phase: 03-paper-product-core
plan: 08
type: tdd
wave: 11
depends_on: [03-07, 03-14]
files_modified:
  - tests/property/execution/test_paper_state_machine.py
  - tests/integration/execution/test_paper_fault_recovery.py
  - tests/integration/execution/test_paper_kill_switch_convergence.py
  - tests/integration/execution/test_paper_offline_boundary.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "Timeout/fault after accepted paper submission remains `SUBMISSION_UNKNOWN`, is reconciled by durable client ID against independently persisted paper truth, and never receives a replacement permit or second gateway submit."
    - "Duplicate/out-of-order observations, cancellation races, restart, and terminal projection replays converge paper and central projections by persisted paper event sequence and observation version."
    - "Kill-switch cancellation invokes only existing durable cancellation work; paper cancel request/evidence and central projection converge before deliberate recovery, without silently treating request as cancel completion."
  artifacts:
    - path: tests/property/execution/test_paper_state_machine.py
      provides: generated offline observation/cancel/fault/restart schedules with paper/central invariants
    - path: tests/integration/execution/test_paper_fault_recovery.py
      provides: timeout-after-accept/reopen/client-ID reconciliation/no-resubmit regression
    - path: tests/integration/execution/test_paper_kill_switch_convergence.py
      provides: latch/cancel/reconcile/recover convergence regression
  key_links:
    - from: deterministic FaultPlan after durable acceptance
      to: RecoveryService
      via: existing ambiguous submission record and client-ID lookup, never another lease/submit
    - from: KillSwitchService cancellation work
      to: PaperGateway cancel evidence and PaperEvidenceProjector
      via: request then ordered paper observation/evidence, followed by central audit projection
---

<objective>
Harden the complete PaperGateway lifecycle so faults, cancellation races, kill-switch recovery, duplicate observations, and process restart converge deterministically to independent paper truth and its central audit projection.

Purpose: the product slices prove local semantics; this final plan proves the required operational failure paths cannot invent terminal states, regress durable state, or create another outbound submission.
Output: State-machine convergence coverage, focused real-SQLite fault/recovery and kill-switch workflows, and only the minimal integration changes required to satisfy those tests.
</objective>

<execution_context>
@/root/.omp/agent/gsd-core/workflows/execute-plan.md
@/root/.omp/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-paper-product-core/03-CONTEXT.md
@.planning/phases/03-paper-product-core/03-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/03-paper-product-core/03-07-PLAN.md
@.planning/phases/03-paper-product-core/03-12-PLAN.md
@pa_agent/trading/application/submission.py
@pa_agent/trading/application/recovery.py
@pa_agent/trading/application/kill_switch.py
@pa_agent/trading/application/paper_projection.py
@pa_agent/trading/gateways/paper/gateway.py
@pa_agent/trading/gateways/paper/faults.py
@tests/property/execution/test_approval_kill_switch_machine.py
@tests/integration/execution/test_uncertain_recovery.py
@tests/integration/execution/test_kill_switch.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify generated paper lifecycle convergence and timeout-after-accept recovery</name>
  <files>tests/property/execution/test_paper_state_machine.py, tests/integration/execution/test_paper_fault_recovery.py</files>
  <behavior>
    - A deterministic `FaultPlan` can raise after PaperGateway commits accepted order truth but before coordinator receives evidence. The central ledger becomes/remains `SUBMISSION_UNKNOWN`; new gateway/store instances reconcile by persisted client ID, project the same truth, and record zero additional gateway submissions, permits, leases, commands, or client IDs.
    - Generated sequences of submit, explicit observation, duplicate observation, lower/conflicting observation, cancellation request, cancellation evidence, timeout, restart, and central projection retry maintain invariants: accepted paper event sequence strictly increases; per-scope observation version never decreases; terminal paper/central states never regress; fill quantity never exceeds order quantity; each product accounting invariant holds; and central projection equals independently queryable paper truth by client ID/event sequence.
    - The generator uses bounded Decimal strategies and no sleep/network/current-clock scheduling. Every expected terminal state follows normalized paper evidence, never a local timeout/cancel shortcut.
  </behavior>
  <action>Write RED Hypothesis `RuleBasedStateMachine` coverage and a real-filesystem SQLite fault regression. Seed the machine with each product’s deterministic scenarios, use only explicit market observations and the invocation-indexed FaultPlan, and model the independent paper store as reference authority. Add invariant assertions for product balances/reserves, isolated margin pair separation/health, perpetual no-negative/no-open-after-liquidation state, event sequence, observation cursor, fill bounds/provenance, terminal non-regression, and central projection convergence. In the integration test, use the ordinary coordinator with a real leased permit, trigger a configured after-accept fault, assert existing ambiguity behavior and exactly one submit, then close/reopen paper/central databases and run `RecoveryService` plus the shared projector. Assert client-ID-only lookup resolves to paper truth with no new lease/permit/ticket/command/client ID and no second gateway call. Per D-09-D-12, explicitly cover sequence ordering, independent reconciliation, deduplication, and uncertain acceptance.
  </action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/property/execution/test_paper_state_machine.py tests/integration/execution/test_paper_fault_recovery.py</automated></verify>
  <done>The suite fails because no comprehensive state machine proves convergence and a post-accept paper fault cannot yet be demonstrated as unknown/reconciled without duplicate submission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify completed product-scope recovery through cancellation convergence</name>
  <files>tests/property/execution/test_paper_state_machine.py, tests/integration/execution/test_paper_fault_recovery.py, tests/integration/execution/test_paper_kill_switch_convergence.py</files>
  <behavior>
    - Latching creates existing durable cancellation work for independently submitted/open Spot, isolated-margin, and perpetual orders. Cancellation work records only requested/timeout and cannot manufacture terminal cancellation evidence.
    - Plan 03-12 exact scope contracts plus Plan 03-14 durable persistence deny READY until each Spot reservation/open-order, margin pair debt/interest/health/repay fact, and perpetual symbol position/margin/funding/liquidation fact is independently resolved through committed Paper truth.
    - In each product scope, explicitly ordered cancellation evidence or market observation settles the fill/cancel race; automatic bridge projection converges after reopen, delayed duplicates, stale observations, and batch retry without creating submit authority.
  </behavior>
  <action>Write final real-SQLite convergence workflows against completed Plan 03-12 scope contracts and Plan 03-14 scope migration/READY transaction for one partially open Spot order, one isolated-margin pair, and one isolated perpetual symbol. Seed each only through the protected dispatch chain, latch, request cancellation, and assert no request/timeout claims terminal cancellation. In fill-first and cancel-first orders, apply explicit terminal evidence or observation and let the injected Plan 03-07 runtime bridge project automatically. Close/reopen Paper and central stores, invoke the ledger-owned scope assessment/begin/complete recovery path, and assert convergence only after each exact durable scope is clear. Add duplicate delayed callbacks and stale observations per scope, then assert zero new ticket, permit, lease, command, client ID, or submit effects. Do not reimplement recovery scope, policy selection, or READY transaction logic here; Plans 03-12 and 03-14 own those production contracts.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/property/execution/test_paper_state_machine.py tests/integration/execution/test_paper_fault_recovery.py tests/integration/execution/test_paper_kill_switch_convergence.py tests/integration/execution/test_kill_switch.py tests/integration/execution/test_uncertain_recovery.py tests/integration/execution/test_paper_recovery_product_scope.py</automated></verify>
  <done>Final convergence proves completed exact durable recovery scopes, automatic projection, ordered cancellation, and restart behavior without reopening recovery authority or causing an outbound side effect.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Prove the complete paper lifecycle remains offline and dependency-isolated</name>
  <files>tests/integration/execution/test_paper_offline_boundary.py</files>
  <behavior>
    - One filesystem-backed workflow performs protected paper submit, explicit observation, cancellation request and terminal resolution, process restart, recovery lookup, and automatic central projection while outbound transport sentinels observe zero transport/network calls.
    - The workflow uses `PaperTradingRuntime`, the real PaperGateway/store, coordinator, recovery service, control/projection path, and `PaperProjectionBatch`; it proves one submit and sequence-ordered projection rather than replacing dependencies with a fake lifecycle.
    - Import-boundary assertions prove the paper gateway, accounting, store, matching, projection, runtime composition, submission, recovery, and control modules do not import PyQt/UI/presentation or `pa_agent.data`, `pa_agent.ai`, or analysis modules.
  </behavior>
  <action>Write a focused offline-boundary integration regression with real temporary Paper and central SQLite files. Install sentinels at project transport seams and fail the test on outbound socket, HTTP, adapter, or credential transport attempts. Construct the real runtime composition, execute one protected submission through the existing permit/lease coordinator, apply an explicit paper observation, request and resolve cancellation in sequence, close/reopen both databases, and invoke durable client-ID recovery. Assert the injected observer projects every committed operation automatically, preserves deterministic Paper/central convergence and exact provenance, records one gateway submit, and makes zero sentinel calls. Add import-graph assertions over the named paper/execution modules that reject UI/PyQt/presentation and data/analysis/AI dependencies. Keep this test offline-only and do not use credentials, wall-clock waits, an external exchange, or a manual bridge call.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/integration/execution/test_paper_offline_boundary.py</automated></verify>
  <done>A full automatically projected submit/observe/cancel/restart/recover workflow proves zero outbound transport and prohibited dependency imports while retaining the protected Paper lifecycle.</done>
</task>
<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | — | Deterministic complete lifecycle recovery across faults/cancel/restart | 03-08 | COVERED | Final convergence suite exercises every operational failure condition.
REQ | SIM-01 | Deterministic fills and restart recovery | 03-08 | COVERED | Generated schedules and real reopened SQLite reconciliation cover all products.
RESEARCH | Stage 5, Pattern 4, Validation Architecture | state machine, fault-after-accept, recovery, kill switch | 03-08 | COVERED | Uses existing Recovery/KillSwitch services and new evidence projector.
CONTEXT | D-09 | Fill/cancel race ordered by persisted sequence/version | 03-08 | COVERED | Both race orders and terminal non-regression are state-machine invariants.
CONTEXT | D-10 | Independent truth reconciles by client ID/event sequence | 03-08 | COVERED | New store/gateway instances drive central convergence after reopen.
CONTEXT | D-11 | Duplicate/out-of-order data cannot regress state | 03-08 | COVERED | Generated stale/conflicting/delayed schedules prove no mutation/regression.
CONTEXT | D-12 | After-accept timeout uncertain/reconcile/no resubmit | 03-08 | COVERED | Fault regression asserts one submit and durable client-ID recovery only.
CONTEXT | D-02/D-05 | Partial residual/cancel release during kill workflow | 03-08 | COVERED | Partial open cancellation race preserves settled and releases only residual state.
CONTEXT | D-01..D-12 | Locked deterministic, accounting, recovery, and no-resubmit semantics | 03-08 | COVERED | Final state machine, convergence paths, and offline-boundary regression retain every locked decision.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| post-accept fault -> recovery | Local caller sees uncertainty; only persisted paper client-ID evidence may resolve it. |
| cancellation work -> PaperGateway | Work authorizes a request only, not a terminal state or a new submit. |
| paper facts -> central projection after restart | Reconciliation consumes canonical paper evidence in event sequence order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-29 | Tampering/Replay | post-accept timeout | critical | mitigate | Preserve `SUBMISSION_UNKNOWN`, reconcile durable client ID, and assert no replacement permit/lease/client ID/submit. |
| T-03-30 | Repudiation | cancel request | high | mitigate | Record request separately; terminal state requires ordered paper evidence and projector application. |
| T-03-31 | Tampering | delayed/duplicate observations | critical | mitigate | Hypothesis schedules enforce cursor/sequence idempotence, terminal non-regression, and projection convergence. |
| T-03-32 | Elevation | kill-switch recovery | critical | mitigate | Preserve Phase 2 durable cancellation/recovery and assert no new outbound authority throughout convergence workflow. |
| T-03-33 | Denial of service | fault handling | medium | mitigate | Deterministic fault seam and bounded offline schedules; no retry loop/polling/network behavior. |
| T-03-SC | Tampering | package installs | low | accept | No package-manager activity. |
</threat_model>

<verification>
Run the new Hypothesis state machine, three-product fault/recovery and kill-switch convergence, and offline-boundary modules, then the existing uncertain-recovery/kill-switch suites. The final focused phase gate is `.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_matching.py tests/unit/execution/test_paper_product_models.py tests/unit/execution/test_paper_product_policy_ticket.py tests/unit/execution/test_paper_product_evidence.py tests/unit/execution/test_paper_product_admission.py tests/unit/execution/test_paper_recovery_product_scope.py tests/unit/execution/test_gateway_contract.py tests/unit/execution/test_gateway_operation_bridge.py tests/unit/execution/test_paper_spot.py tests/unit/execution/test_paper_margin.py tests/unit/execution/test_paper_perpetual.py tests/unit/execution/test_paper_projection.py tests/integration/execution/test_paper_store.py tests/integration/execution/test_paper_product_migration.py tests/integration/execution/test_paper_product_policy_ticket.py tests/integration/execution/test_paper_recovery_product_scope.py tests/integration/execution/test_paper_spot_recovery.py tests/integration/execution/test_paper_margin_recovery.py tests/integration/execution/test_paper_perpetual_liquidation.py tests/integration/execution/test_approval_consumption.py tests/integration/execution/test_paper_ledger_projection.py tests/integration/execution/test_paper_fault_recovery.py tests/integration/execution/test_paper_kill_switch_convergence.py tests/integration/execution/test_paper_offline_boundary.py tests/property/execution/test_paper_state_machine.py`.
</verification>
<success_criteria>
Every modeled paper failure path converges rather than guesses: local timeout after durable acceptance stays uncertain, cancel requests remain requests, ordered paper evidence resolves lifecycle/accounting, restart reconciles by persistent client ID, and duplicate/out-of-order events cannot regress either paper truth or central audit. The kill switch retains its Phase 2 authorization barriers throughout.
</success_criteria>
<output>
Create `.planning/phases/03-paper-product-core/03-08-SUMMARY.md` when done.
</output>
