---
phase: 02-approval-and-risk-boundary
plan: 06
type: execute
wave: 7
depends_on: [02-07]
files_modified:
  - pa_agent/trading/domain/approval.py
  - pa_agent/trading/ports/ledger.py
  - pa_agent/trading/application/kill_switch.py
  - pa_agent/trading/application/recovery.py
  - pa_agent/trading/persistence/migrations.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/integration/execution/test_kill_switch.py
  - tests/property/execution/test_approval_kill_switch_machine.py
autonomous: true
requirements: [SAFE-03, SIM-03]
must_haves:
  truths:
    - "锁存熔断在重启后仍阻止新票据、风险接受和票据消费，并撤销待定票据。"
    - "锁存只为 capability 明确支持取消的规范化持久化开放订单记录取消请求与对账工作，不把取消请求当作已取消或已平仓，也不发送盲目平仓订单。"
    - "只有在持久化取消/对账工作已处理、新鲜规范账户/开放订单/仓位证据显示无未解决 submission claim 或策略定义残余敞口、重新取证评估通过且操作员显式恢复后，熔断才会解除。"
  artifacts:
    - path: pa_agent/trading/application/kill_switch.py
      provides: 持久化锁存、取消请求和显式恢复编排
    - path: tests/integration/execution/test_kill_switch.py
      provides: SQLite 重启、撤销、取消请求和恢复门槛测试
    - path: tests/property/execution/test_approval_kill_switch_machine.py
      provides: 票据/熔断/重启交错安全不变量
  key_links:
    - from: pa_agent/trading/application/kill_switch.py
      to: pa_agent/trading/ports/ledger.py
      via: latch、revoke pending tickets、cancellation work、recovery state 事务端口
      pattern: latch|revoke|cancellation|recovery
    - from: pa_agent/trading/application/recovery.py
      to: pa_agent/trading/ports/gateway.py
      via: 仅持久化 client ID 的规范化对账和取消证据，不创建新提交
      pattern: lookup_order_by_client_id|cancel_order
---

<objective>
交付跨重启锁存的全局熔断与谨慎恢复路径：阻止新增授权、撤销待定票据、请求可取消订单取消，并且只在对账证据和显式恢复后解除。

Purpose: 把最终安全边界放在持久化账本/协调器内，而不是 UI 控件或进程内标志。
Output: 熔断领域状态、服务和账本迁移、集成/属性测试以及阶段全套回归验证。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-VALIDATION.md
@.planning/phases/02-approval-and-risk-boundary/02-05-SUMMARY.md
@.planning/phases/01-execution-foundation/01-07-SUMMARY.md

<interfaces>
From Plan 02-07: ticket consumption atomically checks current durable kill state before returning an `OutboundSubmission`; a new latch must therefore invalidate pending tickets and deny consumption in the same ledger authority boundary.

From `pa_agent/trading/application/recovery.py`: recovery may query only the persisted client-order ID and append normalized evidence; it has no submission path.

From `pa_agent/trading/ports/gateway.py`: `cancel_order(client_order_id)` returns canonical evidence. A cancellation request is not evidence of a cancelled order or closed exposure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify durable latch, cancellation, and recovery state-machine behavior</name>
  <files>tests/integration/execution/test_kill_switch.py, tests/property/execution/test_approval_kill_switch_machine.py</files>
  <read_first>Read `pa_agent/trading/application/recovery.py`, `pa_agent/trading/ports/ledger.py`, `pa_agent/trading/ports/gateway.py`, `tests/property/execution/test_lifecycle_machine.py`, `tests/integration/execution/test_approval_consumption.py`, and `02-VALIDATION.md` row for SAFE-03.</read_first>
  <behavior>
    - Latching persists reason, actor label, time, policy/evidence summary and transitions READY to LATCHED; reopen preserves the state.
    - Latching rejects new proposals/risk acceptance/ticket consumption, revokes pending tickets with terminal audit events, and creates cancellation/reconciliation work only for canonical persistent open orders whose capability explicitly advertises cancellation.
    - Cancellation request success, timeout, absent evidence, or restart never proves cancellation/flat exposure and never triggers a blind close order; reset requires processed persisted cancellation/reconciliation work, fresh canonical account/open-order/position evidence showing no unresolved submission claim or policy-defined residual exposure, a fresh assessment, and explicit operator action through RECOVERING to READY.
    - Generated interleavings of issue/approve/double-click/reject/expire/latch/reopen/reset never yield two outbound authorizations or a submit-capable ticket while latched.
  </behavior>
  <action>Create Wave 0 real-SQLite integration tests and a Hypothesis `RuleBasedStateMachine` before implementation. Use deterministic clock/fake gateway schedules, including restart and cancellation ambiguity. Assert cancellation work is created only for canonical persistent open orders whose advertised capability supports cancellation, never as blind close work. Assert controlled audit/cancellation-work rows rather than inferred remote outcomes. Cover D-12 kill-switch revocation as a distinct terminal event, the resolved reset evidence set, and Phase 1 durable client IDs remaining unchanged throughout recovery.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_kill_switch.py tests/property/execution/test_approval_kill_switch_machine.py</automated></verify>
  <acceptance_criteria>The tests fail before implementation and enforce a persisted latch across reopen, no bypass through issuance/approval/recovery, and evidence-gated explicit reset.</acceptance_criteria>
  <done>Wave 0 establishes deterministic integration and generated regression coverage for kill-switch safety, cancellation ambiguity, and recovery ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement persistent kill-switch aggregate and reconciled explicit reset</name>
  <files>pa_agent/trading/domain/approval.py, pa_agent/trading/ports/ledger.py, pa_agent/trading/application/kill_switch.py, pa_agent/trading/application/recovery.py, pa_agent/trading/persistence/migrations.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_kill_switch.py, tests/property/execution/test_approval_kill_switch_machine.py</files>
  <read_first>Read the tests created in Task 1, `pa_agent/trading/persistence/sqlite_ledger.py`, `pa_agent/trading/application/recovery.py`, `pa_agent/trading/application/approval.py`, and `02-PATTERNS.md` section “Approval, kill switch, and only outbound admission”.</read_first>
  <behavior>
    - All issuance, risk acceptance, ticket consumption, and submission-admission ledger entry points fail closed unless persisted state is READY.
    - Recovery orchestration only records cancellation attempts/evidence and cannot construct a new command, client ID, claim, ticket, or outbound authorization.
  </behavior>
  <action>Implement frozen kill-switch state and cancellation/recovery-work values with READY, LATCHED, and RECOVERING lifecycle. Add an ascending SQLite migration for singleton durable state, latch/restore audit events, ticket revocations, and cancellation/recovery-work rows. Implement ledger transactions that latch once, revoke every pending ticket, enqueue cancellation only for canonical persistent open orders whose capability explicitly advertises cancellation, and preserve unknown/unresolved submissions for reconciliation. Implement `KillSwitchService` using the ledger and canonical gateway/recovery boundaries: request cancellation by durable client ID, record result/evidence, never send a blind close order, and never claim a remote order or exposure is resolved without reconciliation. Require explicit operator reset only after persisted cancellation/reconciliation work is processed, fresh canonical account/open-order/position evidence shows no unresolved submission claim or policy-defined residual exposure, and a fresh risk assessment has succeeded; route through RECOVERING before READY. Keep all analysis, alert, notification, presentation, and credential code advisory-only. Finish by running the complete execution suite and Ruff checks required by `02-VALIDATION.md`.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_kill_switch.py tests/property/execution/test_approval_kill_switch_machine.py &amp;&amp; .venv/bin/pytest -q tests/unit/execution tests/integration/execution tests/property/execution &amp;&amp; .venv/bin/ruff check pa_agent/trading tests</automated></verify>
  <acceptance_criteria>All kill-switch and full execution tests pass; the durable latch survives restart, leaves no pending ticket submit-capable, records cancellation work without inventing terminal outcomes, and only permits deliberate evidence-gated recovery.</acceptance_criteria>
  <done>SAFE-03 and its SIM-03 audit trail are complete, with final full execution-suite and Ruff verification green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| safety control -> ledger authorization | A persisted latch must override ticket and submission routes across process restart. |
| cancellation/reconciliation evidence -> recovery | A request result cannot be treated as remote state or exposure resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Denial of Service/Elevation | kill-switch state and ticket routes | high | mitigate | Singleton persistent latch, transactionally revoked tickets, every ledger gate checks READY, and restart/property tests. |
| T-02-12 | Tampering/Repudiation | cancellation and reset evidence | high | mitigate | Record cancellation work only, reconcile by durable client ID, require residual-risk disclosure plus fresh assessment and explicit RECOVERING reset. |
| T-02-03 | Elevation | concurrent approval during latch | high | mitigate | Immediate SQLite transactions serialize latch/revocation and consumption; state-machine tests cover interleavings. |
| T-02-SC | Tampering | package installs | low | accept | No dependency installation occurs. |
</threat_model>

<verification>
Run the focused integration/property pair after each task. Phase completion gate: `.venv/bin/pytest -q tests/unit/execution tests/integration/execution tests/property/execution` and `.venv/bin/ruff check pa_agent/trading tests` both pass.
</verification>

<success_criteria>
The persisted global safety boundary blocks new work and invalidates approvals across restart; cancellation and recovery remain evidence-driven, explicit, auditable, and unable to issue another submission.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-06-SUMMARY.md` when done.
</output>
