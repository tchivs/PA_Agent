---
phase: 03-paper-product-core
plan: 10
type: tdd
wave: 3
depends_on: [03-02, 03-03, 03-13]
files_modified:
  - pa_agent/trading/domain/risk.py
  - pa_agent/trading/ports/gateway.py
  - pa_agent/trading/gateways/paper/store.py
  - pa_agent/trading/gateways/paper/gateway.py
  - tests/fixtures/fake_exchange.py
  - tests/unit/execution/test_gateway_contract.py
  - tests/unit/execution/test_paper_product_evidence.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "Fresh isolated-margin admission evidence is immutable and exact to one selected target, account, and isolated pair; it contains persisted Decimal collateral, available collateral, debt, accrued interest, health, borrow availability, and repayment facts."
    - "Fresh USDT-perpetual admission evidence is immutable and exact to one selected target, account, and symbol; it contains persisted isolated/one-way mode, leverage capability, available/initial/maintenance margin, mark, and position facts."
    - "The evidence collector can query these facts only through a narrow read-only exchange-neutral gateway port; no caller supplies a map or reads PaperStore directly, and Paper truth remains independently persisted."
  artifacts:
    - path: pa_agent/trading/domain/risk.py
      provides: immutable canonical typed product-evidence values with Decimal, freshness, scope, and digest validation
    - path: pa_agent/trading/ports/gateway.py
      provides: narrow typed read-only pair/symbol product-evidence query methods on the canonical TradingGateway port
    - path: pa_agent/trading/gateways/paper/gateway.py
      provides: PaperGateway read-only evidence implementation backed only by committed PaperStore facts
    - path: tests/fixtures/fake_exchange.py
      provides: scripted read-only product-evidence responses that reject outbound submission
  key_links:
    - from: FreshEvidenceCollector
      to: TradingGateway product-evidence methods
      via: exact selected target and isolated pair or perpetual symbol; never a PaperStore handle or caller-built evidence
    - from: PaperGateway product-evidence methods
      to: PaperStore scoped snapshots
      via: immutable committed records keyed by target/account/pair-or-symbol and observation version
---

<objective>
Establish the source-grounded read-only product-evidence boundary before the product-aware RiskEngine admission plan.

Purpose: the current `TradingGateway` only exposes an account snapshot keyed by account/product, while `FreshEvidenceCollector` can call only that port and existing fakes expose no pair/symbol product facts. D-06/D-07 require exact independent Paper facts, so this stage adds a narrow typed contract and durable Paper implementation before any admission code uses it.
Output: Immutable product-evidence domain values, gateway query contract, independent PaperStore/PaperGateway read implementation, offline fakes, and contract tests.
</objective>

<execution_context>
@/root/.omp/agent/gsd-core/workflows/execute-plan.md
@/root/.omp/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-paper-product-core/03-CONTEXT.md
@.planning/phases/03-paper-product-core/03-RESEARCH.md
@.planning/phases/03-paper-product-core/03-02-PLAN.md
@.planning/phases/03-paper-product-core/03-03-PLAN.md
@pa_agent/trading/ports/gateway.py
@pa_agent/trading/domain/risk.py
@pa_agent/trading/application/evidence_collector.py
@tests/fixtures/fake_exchange.py
@tests/unit/execution/test_gateway_contract.py

<interfaces>
From `pa_agent/trading/ports/gateway.py`: `TradingGateway.get_account_snapshot(account_id, product)` is too broad for pair/symbol product truth, while all collector reads already flow through this port. `submit_order` accepts only `OutboundSubmission`; new evidence methods must be read-only and must not accept it.

From `pa_agent/trading/application/evidence_collector.py`: `FreshEvidenceCollector` holds only a `TradingGateway`, refreshes typed observations, and rejects collection failures. The new contract must therefore return canonical frozen values rather than PaperStore rows, maps, or a gateway capability.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify narrow typed pair and symbol evidence queries</name>
  <files>tests/unit/execution/test_gateway_contract.py, tests/unit/execution/test_paper_product_evidence.py, tests/fixtures/fake_exchange.py</files>
  <behavior>
    - The gateway contract exposes one typed isolated-margin query taking `ExecutionTarget` and `isolated_symbol`, and one typed perpetual query taking `ExecutionTarget` and `symbol`; neither has an `OutboundSubmission`, permit, command, batch, store, or mutable mapping parameter.
    - A scripted fake returns only canonical frozen product evidence or a typed unavailable failure, tracks query scope/order, and raises if any evidence-collection path tries to submit.
    - Margin evidence requires exact `(target_id, account_id, isolated_symbol)`, finite Decimal collateral/available collateral/debt/interest/health/borrow availability, repayment status, observed time, observation version, and canonical digest. Perpetual evidence requires exact `(target_id, account_id, symbol)`, isolated and one-way confirmations, finite Decimal maximum leverage/available/initial/maintenance margin/mark/position facts, observed time, observation version, and canonical digest.
  </behavior>
  <action>Write RED port and fake tests from the current `TradingGateway`, `FreshEvidenceCollector`, and `ScriptedEvidenceGateway` shapes. Define the required method names, exact annotations, return types, frozen-value validation, and fake submission sentinel before production code. Exercise a same-account different-pair substitution and same-account different-symbol substitution; both must be distinguishable by scope and cannot be repaired by a caller map. Per D-06/D-07, require all numeric fields to be canonical Decimal values and all product evidence to carry its own freshness/version/digest facts.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_gateway_contract.py tests/unit/execution/test_paper_product_evidence.py</automated></verify>
  <done>The contract tests fail until a read-only typed product-evidence surface and compliant offline fake exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement immutable evidence values and the exchange-neutral read port</name>
  <files>pa_agent/trading/domain/risk.py, pa_agent/trading/ports/gateway.py, tests/fixtures/fake_exchange.py, tests/unit/execution/test_gateway_contract.py, tests/unit/execution/test_paper_product_evidence.py</files>
  <behavior>
    - Product evidence values canonicalize finite Decimal inputs, require aware observation time and a positive monotonic version, bind target/account/scope, and expose deterministic digests without mutable containers.
    - The two new `TradingGateway` methods return only those typed values; existing submit/cancel/lookup semantics and the protected `OutboundSubmission` parameter remain unchanged.
  </behavior>
  <action>Implement frozen isolated-margin and perpetual evidence values in `domain/risk.py`, including canonical scope fields, Decimal validation, observed timestamp/version, and stable digest material. Add two explicit read-only abstract methods to `ports/gateway.py` whose only inputs are the selected target plus pair or symbol and whose only results are these typed facts. Extend the existing scripted evidence fake to provide queued typed responses and retain its hard failure for submit. Do not place product evidence in `AccountObservation`, add a generic untyped payload method, add a PaperStore parameter to the collector, or alter permit/lease/submission authority. This creates the source contract consumed by Plan 03-09.
  </action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_gateway_contract.py tests/unit/execution/test_paper_product_evidence.py tests/integration/execution/test_fresh_evidence_risk.py</automated></verify>
  <done>The only product-evidence retrieval surface is a typed exchange-neutral gateway port; all fakes preserve collection-only authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist and expose Paper product evidence independently</name>
  <files>pa_agent/trading/gateways/paper/store.py, pa_agent/trading/gateways/paper/gateway.py, tests/unit/execution/test_paper_product_evidence.py</files>
  <behavior>
    - Seeded or subsequently committed PaperStore pair/symbol snapshots survive close/reopen and reconstruct equal immutable evidence without opening or reading the central execution ledger.
    - `PaperGateway` is a concrete `TradingGateway`: its evidence reads are store-backed and its two product queries return only committed exact-scope facts. Any lifecycle operation lacking a committed paper order returns a controlled canonical unavailable result rather than manufacturing account truth or accepting caller data.
    - Product evidence reads neither submit nor mutate a paper event, account, order, fill, central projection, permit, lease, or command identity.
  </behavior>
  <action>Extend the Plan 03-02 PaperStore read model with scoped snapshot query methods that reconstruct only the new frozen evidence values from paper-owned persisted records. Create the concrete `PaperGateway` constructor around that store and implement every current `TradingGateway` abstract read/query member using canonical store-backed observations or controlled typed unavailability when no committed paper record exists; implement the two new product-evidence methods as direct exact selected Paper target/account/pair-or-symbol reads. Keep `submit_order` protected by its existing `OutboundSubmission` signature and make a request without a committed paper order fail with the typed gateway error rather than inventing a command, order, or account fact. Test real filesystem SQLite close/reopen, same-account cross-pair and cross-symbol rejection, stale/missing snapshot rejection, exact Decimal/digest preservation, concrete port conformance, and zero submit/mutation calls during collection. The implementation must never expose store access to `FreshEvidenceCollector`, use central projection, allocate an order identity, or accept a caller-built product value. This fulfills D-06/D-07 fresh-fact ownership before risk admission and before any margin/perpetual fill path.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_product_evidence.py tests/integration/execution/test_paper_store.py tests/unit/execution/test_gateway_contract.py</automated></verify>
  <done>Paper product facts are independently durable, immutable, scope-bound, and reachable only through a concrete read-only PaperGateway query surface that Plan 03-09 can consume.</done>
</task>
</tasks>

<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | — | Safe auditable Paper margin/perpetual lifecycle | 03-10 | COVERED | Product truth is available before admission and accounting.
REQ | SIM-01 | Paper margin/perpetual balances, leverage, and restart behavior | 03-10 | COVERED | Durable scoped evidence is reconstructible after PaperStore reopen.
RESEARCH | Resolved product fact authority | 03-10 | COVERED | Current collector/port gap is closed with typed read-only methods.
CONTEXT | D-06 | Pair-isolated collateral/debt/interest/health/borrow/repay facts | 03-10 | COVERED | Exact immutable pair evidence is independent per pair.
CONTEXT | D-07 | Symbol-scoped isolated/one-way margin and position facts | 03-10 | COVERED | Exact immutable symbol evidence is independent per symbol.
CONTEXT | D-10 | Independent persisted Paper truth | 03-10 | COVERED | PaperStore is the only source for PaperGateway product evidence.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| collector -> TradingGateway evidence query | Admission can read only typed exact-scope product facts. |
| PaperStore -> PaperGateway | Gateway reconstructs immutable facts from committed independent Paper truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-38 | Tampering | pair/symbol evidence substitution | critical | mitigate | Exact target/account/scope binding, canonical digest/version, and cross-scope rejection tests. |
| T-03-39 | Elevation | evidence query surface | critical | mitigate | Read-only typed port has no permit, lease, command, store handle, or submit parameter. |
| T-03-40 | Repudiation | durable product facts | high | mitigate | Reopen tests reconstruct immutable PaperStore-backed values and their canonical digests. |
| T-03-SC | Tampering | package installs | low | accept | No package-manager operation is planned. |
</threat_model>

<verification>
Run the port, fake, product-evidence, fresh-evidence, and PaperStore suites. Confirm an evidence collector cannot access PaperStore or submit; pair/symbol substitutions fail; and a reopened PaperGateway returns identical persisted facts.
</verification>

<success_criteria>
A narrow source-grounded gateway contract now supplies immutable fresh product facts from independently durable Paper truth before risk admission. The contract is exchange-neutral, typed, pair/symbol-scoped, and cannot create execution authority.
</success_criteria>

<output>
Create `.planning/phases/03-paper-product-core/03-10-SUMMARY.md` when done.
</output>
