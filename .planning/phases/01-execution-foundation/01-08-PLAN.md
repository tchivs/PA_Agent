---
phase: 01-execution-foundation
plan: 08
type: execute
wave: 8
depends_on: [01-07]
files_modified:
  - pa_agent/trading/persistence/sqlite_connection.py
  - pa_agent/trading/persistence/migrations.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/integration/execution/test_sqlite_ledger.py
  - tests/integration/execution/test_idempotency_recovery.py
autonomous: true
gap_closure: true
requirements: [CORE-04, SIM-02, NFR-03]
must_haves:
  truths:
    - "Concurrent fresh and reopened SQLiteExecutionLedger constructors for one canonical local path complete fail-closed bootstrap without a WAL/migration race."
    - "The bootstrap critical section spans policy configuration—including the persistent WAL transition—and every migration check/apply decision for that path."
    - "A failed migration preserves rollback/retry behavior, and all initialized repositories can still serialize first admission to exactly one generated-ID claim."
  artifacts:
    - path: pa_agent/trading/persistence/sqlite_connection.py
      provides: Process-local canonical-path bootstrap locking and fail-closed SQLite policy setup.
    - path: pa_agent/trading/persistence/migrations.py
      provides: Migration applied-version checks performed inside the same transaction as corresponding DDL/version insertion.
    - path: tests/integration/execution/test_idempotency_recovery.py
      provides: Barrier-driven fresh and reopened concurrent bootstrap/admission regressions over real SQLite.
  key_links:
    - from: SQLiteExecutionLedger.__init__
      to: sqlite_connection bootstrap critical section
      via: One canonical path guard encloses WAL policy setup and run_migrations before the connection becomes usable.
      pattern: "bootstrap|run_migrations|journal_mode"
    - from: run_migrations
      to: schema_migrations
      via: Each pending migration reads its version and commits DDL/version atomically under the bootstrap guard.
      pattern: "schema_migrations|transaction"
---

<objective>
Close the diagnosed SQLite startup race with a per-canonical-path fail-closed bootstrap critical section spanning WAL configuration and migrations, then prove fresh and reopened concurrent initialization deterministically.

Purpose: Concurrent constructors currently contend before admission at the persistent journal-mode transition and can separately select the same pending migration. This plan fixes initialization—not by weakening WAL/FULL/timeout policy or retrying around a race—and preserves exact-one durable admission afterward.
Output: Serialized one-local-process ledger bootstrap, transactional migration selection, and deterministic real-SQLite concurrency regression tests.

Locked-decision traceability: implement D-07 (local transactional SQLite ledger and restart initialization), D-08 (schema/migration initialization for one desktop process), D-05 (one durable identity before a future gateway call), and D-10 (focused offline concurrency tests) from `01-CONTEXT.md` exactly.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-execution-foundation/01-CONTEXT.md
@.planning/phases/01-execution-foundation/01-VERIFICATION.md
@.planning/phases/01-execution-foundation/01-UAT.md
@.planning/debug/sqlite-wal-init-race.md
@.planning/phases/01-execution-foundation/01-07-PLAN.md
@pa_agent/trading/persistence/sqlite_connection.py
@pa_agent/trading/persistence/migrations.py
@pa_agent/trading/persistence/sqlite_ledger.py
@tests/integration/execution/test_sqlite_ledger.py
@tests/integration/execution/test_idempotency_recovery.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize canonical-path policy bootstrap and migration selection</name>
  <files>pa_agent/trading/persistence/sqlite_connection.py, pa_agent/trading/persistence/migrations.py, tests/integration/execution/test_sqlite_ledger.py</files>
  <behavior>
    - Test 1: A bootstrap helper keys its process-local critical section by normalized absolute canonical database path, applies/verifies busy timeout, foreign keys, WAL, and FULL synchronous policy while holding that path’s guard, then invokes migrations before releasing it.
    - Test 2: Different canonical paths do not share a bootstrap lock, while equivalent relative/absolute spellings of the same path do share one; no global database lock serializes unrelated test databases.
    - Test 3: Migration application checks whether each version is already applied inside the same immediate transaction that runs its DDL and writes its version row, so a concurrent caller cannot act on a stale applied-version snapshot.
    - Test 4: An injected migration failure rolls back its DDL and version entry, leaves the ledger unusable for that constructor, and a later serialized retry applies the repaired migration exactly once without downgrading policy.
  </behavior>
  <action>Refactor SQLite initialization around an explicit process-local bootstrap primitive in `sqlite_connection.py`, protected by a small registry guard and per-path locks keyed by `Path.resolve(strict=False)` (or the equivalent canonical absolute path suitable for the supported desktop process). The public bootstrap operation must own storage preparation, connection creation, all required pragma application/verification—including the persistent `journal_mode = WAL` transition—and the supplied migration runner until the path guard releases. Keep ordinary connection ownership thread-confined (`check_same_thread=True`), keep 5000 ms busy timeout/FULL/WAL/foreign-key verification and typed fail-closed errors, and never downgrade to another journal/synchronous mode or turn a lock error into a successful no-op. Do not add a multi-process/cloud protocol: Phase 01’s locked scope is one local desktop process, so the guard is deliberately process-local. In `run_migrations`, retain ordered migrations but move every applied-version read inside the transaction that executes that migration’s DDL and inserts schema_migrations version metadata; preserve clean rollback and retry behavior. Replace only obsolete tests that assert the former split check/apply sequence, and add focused fake migrations plus direct bootstrap assertions. Do not introduce sleeps, background initializers, global serialization across unrelated paths, ORM tooling, network calls, or a weaker storage policy.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/integration/execution/test_sqlite_ledger.py -q</automated>
  </verify>
  <done>One local process serializes policy-changing WAL setup and migration decisions for each canonical ledger path; failures remain typed and fail closed, while a repaired migration can retry cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route every ledger constructor through bootstrap and prove concurrent reopen safety</name>
  <files>pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_idempotency_recovery.py, tests/integration/execution/test_sqlite_ledger.py</files>
  <behavior>
    - Test 1: SQLiteExecutionLedger.__init__ receives its usable connection only from the guarded bootstrap path, so no constructor independently changes journal mode or calls migrations outside that critical section.
    - Test 2: A barrier-driven fresh-path test starts four constructors against one new path simultaneously. Every constructor completes, migration history contains the initial version exactly once, all required pragmas remain verified, and concurrent admission produces one generated-ID admissible claim plus three non-admissible reloads.
    - Test 3: A barrier-driven reopened-path test first initializes and closes the ledger, then starts four simultaneous constructors on the same existing path. Every constructor completes, migration history remains one row per version, no constructor reruns DDL, and exactly one new logical command receives admission.
    - Test 4: The tests use bounded ThreadPoolExecutor, threading.Barrier, joins, and real temporary SQLite only; they contain no scheduling sleep/retry loop and assert all worker exceptions directly.
  </behavior>
  <action>Change `SQLiteExecutionLedger.__init__` to acquire its connection through the bootstrap API from Task 1, which encloses its first configuration and `run_migrations` call before assigning `self._connection`; retain the close-on-failure behavior and do not leave a partially initialized repository usable. Remove the separate constructor sequence that opens/configures and then migrates without coordination. Extend `test_idempotency_recovery.py` with two deterministic barrier scenarios. For a fresh temporary path, synchronize all workers immediately before constructor entry, collect each construction/admission result, then assert no worker exception, one schema migration record for each expected version, one durable claim, exactly one admissible generated-ID result, and shared first identities for the other logical repeats. For a reopened path, initialize/close once before the barrier, have each worker construct/reopen and admit a new common logical command, then assert no WAL/migration configuration error, unchanged migration history, one claim, and generated-ID restart behavior. Keep regular row-count helpers opening through the same policy path, and assert the pragma state from a post-test connection. The test must demonstrate both creation and reopening races are prevented by bootstrap, not hide them through prewarming, broad retries, timing sleeps, or a relaxed SQLite policy. Preserve the coordinator test’s no-concrete-exchange rule and do not add any process-level lock outside Phase 01’s one-process boundary.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/integration/execution/test_sqlite_ledger.py tests/integration/execution/test_idempotency_recovery.py -q</automated>
  </verify>
  <done>Fresh and reopened simultaneous repository construction is deterministically safe for one canonical local SQLite path, preserves the migration/durability policy, and still admits exactly one unresolved logical command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent local repository constructors to SQLite bootstrap | Simultaneous threads contend for a persistent configuration transition and schema initialization. |
| migration code to schema history | DDL and version metadata must form one atomic state decision. |
| bootstrap completion to normal admission | No normal ledger operation may run on partially configured or partially migrated storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-GC-09 | Denial of service | WAL/bootstrap race | high | mitigate | Per-canonical-path critical section serializes policy-changing setup; errors remain bounded typed failures rather than a hidden retry/downgrade. |
| T-01-GC-10 | Tampering | migration schema/version history | high | mitigate | Version lookup, DDL, and version insert execute within one transaction while bootstrap owns the path guard. |
| T-01-GC-11 | Repudiation | durable initialization/admission | high | mitigate | Barrier-driven real SQLite tests assert schema history, pragma state, row counts, one generated identity, and one claim after concurrent startup. |
| T-01-SC | Tampering | package installation | low | accept | No package installation is planned; sqlite3 is standard library. |
</threat_model>

<verification>
<automated>.venv/bin/python -m pytest tests/unit/execution/test_models.py tests/unit/execution/test_gateway_contract.py tests/unit/execution/test_order_validation.py tests/property/execution/test_decimal_invariants.py tests/property/execution/test_rule_validation_properties.py tests/integration/execution/test_sqlite_ledger.py tests/integration/execution/test_idempotency_recovery.py tests/integration/execution/test_refresh_before_validation.py tests/integration/execution/test_uncertain_recovery.py -q</automated>

This is the Phase 01 focused execution corpus only. It must show strict canonical ingress, generated durable identity, complete evidence projection, protected authorization, typed observations, fresh-rule validation, evidence-only recovery, and fresh/reopened bootstrap concurrency. Do not run formatters, linters, project-wide suites, live/Testnet checks, or network tests.
</verification>

<success_criteria>
The Phase 01 local ledger initializes deterministically under concurrent fresh and reopened constructors, keeps its fail-closed WAL/FULL policy and transactional migration invariants, and satisfies every verification/UAT acceptance blocker without adding a concrete exchange connection or real order submission.
</success_criteria>

## Gap Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 01 | One exchange-neutral durable execution truth that safely recovers incomplete work. | 01-06, 01-07, 01-08 | COVERED | Strict inputs, durable projections/authorization, and atomic bootstrap preserve the bounded source of truth. |
| REQ | CORE-01 | Immutable canonical execution values. | 01-06 | COVERED | Runtime enum/context/evidence and typed observation validation close canonical ingress gaps. |
| REQ | CORE-02 | Narrow canonical gateway interface. | 01-06 | COVERED | Gateway accepts only the ledger-created outbound submission; no concrete transport is added. |
| REQ | CORE-04 | Ledger-generated durable client IDs/history before request. | 01-06, 01-07, 01-08 | COVERED | Contract, generated-ID implementation, protected authorization, and concurrent admission proof. |
| REQ | SIM-02 | Transaction-safe separate execution persistence and recovery. | 01-06, 01-07, 01-08 | COVERED | Typed observations, correct fill projections, migrations, and real SQLite regression coverage. |
| REQ | NFR-02 | Decimal arithmetic and fresh metadata validation. | 01-06 | COVERED | Strict command/rule ingress removes raw enum and negative-minimum bypasses while existing fresh lookup remains. |
| REQ | NFR-03 | Reconcile uncertainty and restart without assumed terminal state. | 01-07, 01-08 | COVERED | Protected ambiguity transition and generated-ID restart/bootstrap behavior preserve lookup-only recovery. |
| RESEARCH | Canonical frozen Decimal values and explicit product contexts | No raw coercion or generic context can cross the domain boundary. | 01-06 | COVERED | Uses existing frozen dataclasses and strict Decimal parser; no parallel DTOs. |
| RESEARCH | Ledger owns client identity and atomic pre-submit work | One logical key creates/reuses one identity/claim before a future side effect. | 01-06, 01-07 | COVERED | Caller candidates are ignored; protected authorization replaces separate liveness read. |
| RESEARCH | Cumulative evidence and incident retention | Duplicate evidence is idempotent; contradictory evidence never rewrites truth. | 01-07 | COVERED | Atomic cumulative projection updates and incident regressions are specified. |
| RESEARCH | Exclusive SQLite initialization path | WAL policy and migrations run before normal repository use. | 01-08 | COVERED | Canonical-path guarded bootstrap and in-transaction migration selection. |
| RESEARCH | One local desktop process; no cloud/multi-user synchronization | Do not add external locking or synchronization infrastructure. | 01-08 | COVERED | Process-local per-path guard is explicitly scoped to the locked boundary. |
| CONTEXT | D-01 / D-02 | Independent immutable Decimal execution domain and explicit products. | 01-06 | COVERED | Strict runtime canonical values and context discrimination. |
| CONTEXT | D-03 | Canonical-only gateway boundary. | 01-06, 01-07 | COVERED | Abstract port only; coordinator test uses in-memory fake with no network. |
| CONTEXT | D-05 / D-06 | One durable client ID and unresolved outcomes remain reconciled. | 01-06, 01-07, 01-08 | COVERED | Ledger allocates ID; protected attempt and recovery keep uncertainty/evidence semantics. |
| CONTEXT | D-07 / D-08 | Separate transactional SQLite ledger plus one-process initialization/migration behavior. | 01-07, 01-08 | COVERED | Atomic row updates and per-path guarded WAL/migration bootstrap. |
| CONTEXT | D-09 | Advisory/UI/LLM/CSV paths have no gateway access. | 01-06, 01-07, 01-08 | COVERED | Every task excludes UI, analysis, CSV, credentials, and concrete gateway work. |
| CONTEXT | D-10 | Focused Pytest/Hypothesis testing without network credentials. | 01-06, 01-07, 01-08 | COVERED | Named offline unit/property/integration commands and real local SQLite tests. |
| VERIFICATION | CR-01 | Fill evidence leaves zero durable totals. | 01-07 | COVERED | Cumulative partial/full projection transaction and duplicate/contradiction tests. |
| VERIFICATION | CR-02 | Caller controls durable client ID. | 01-06, 01-07 | COVERED | Ledger-owned ID contract and restart/persistence implementation tests. |
| VERIFICATION | CR-03 | Raw enum/context bypass and malformed rule metadata. | 01-06 | COVERED | Strict constructors, raw-market regression, and non-negative minima coverage. |
| VERIFICATION | CR-04 | Claim liveness check has a TOCTOU race. | 01-06, 01-07 | COVERED | Atomic irreversible authorization and deterministic ambiguity/cancellation interleaving. |
| VERIFICATION | WR-01 | Negative minima weaken validation. | 01-06 | COVERED | InstrumentRules rejects negative minima before validation. |
| VERIFICATION | WR-02 | Arbitrary observation mapping reaches SQLite. | 01-06, 01-07 | COVERED | Typed AccountObservation contract, persistence cutover, and no-row rejection tests. |
| UAT | Test 6 | Concurrent constructors race on WAL/migration bootstrap. | 01-08 | COVERED | Canonical-path bootstrap lock plus barrier-driven fresh/reopened local SQLite tests. |

<output>
Create `.planning/phases/01-execution-foundation/01-08-SUMMARY.md` when done.
</output>
