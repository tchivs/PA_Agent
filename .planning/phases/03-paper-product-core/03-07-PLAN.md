---
phase: 03-paper-product-core
plan: 07
type: tdd
wave: 10
depends_on: [03-05, 03-06, 03-11, 03-12, 03-14]
files_modified:
  - pa_agent/trading/persistence/migrations.py
  - pa_agent/trading/ports/ledger.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - pa_agent/trading/application/paper_projection.py
  - pa_agent/trading/application/paper_runtime.py
  - tests/unit/execution/test_paper_projection.py
  - tests/integration/execution/test_paper_ledger_projection.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "`PaperProjectionBatch` is immutable and read-only: it is reconstructed from independently durable paper truth as ordered normalized evidence, only new paper fills with exact provenance, and account snapshots scoped by paper event sequence."
    - "Submit, explicit market advance, terminal cancellation resolution, and recovery lookup each forward exactly one batch to the same idempotent projector; the projector has no gateway submit, permit, lease, command-ID, or paper-store mutation authority."
    - "Central rows retain exact Decimal paper provenance and are an audit projection only; duplicate/conflicting batches cannot alter paper truth or replace established central evidence."
  artifacts:
    - path: pa_agent/trading/application/paper_projection.py
      provides: immutable read-only batch, Paper projection bridge, and one-way evidence-only central projection service
    - path: tests/integration/execution/test_paper_ledger_projection.py
      provides: all four generic operation-result producers converging through the Paper bridge
  key_links:
    - from: PaperGateway submit/advance/cancel/lookup result
      to: PaperProjectionBatch
      via: canonical paper event sequence and exact paper truth, not a central-ledger reconstruction
    - from: SubmissionCoordinator, Paper control callers, and RecoveryService
      to: PaperEvidenceProjector.apply
      via: one shared read-only batch consumer after a gateway result; never a projector-to-gateway call
---

<objective>
Make central audit projection a one-way, provenance-complete consumer of independently durable paper truth.

Purpose: SIM-01 and D-04/D-10 require normal submit, observation, cancellation, and restart recovery to reach one idempotent audit path without converting the central ledger or projector into a simulator or submission authority.
Output: `PaperProjectionBatch`, a Paper-only adapter for the Plan 03-11 generic post-operation result, forward fill-provenance migration, and real SQLite projection regressions.
</objective>

<execution_context>
@/root/.omp/agent/gsd-core/workflows/execute-plan.md
@/root/.omp/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-paper-product-core/03-CONTEXT.md
@.planning/phases/03-paper-product-core/03-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/03-paper-product-core/03-03-PLAN.md
@pa_agent/trading/application/submission.py
@pa_agent/trading/application/recovery.py
@pa_agent/trading/application/kill_switch.py
@pa_agent/trading/ports/gateway.py
@pa_agent/trading/ports/ledger.py
@pa_agent/trading/persistence/migrations.py
@pa_agent/trading/persistence/sqlite_ledger.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify immutable paper-truth batches and all caller paths</name>
  <files>tests/unit/execution/test_paper_projection.py, tests/integration/execution/test_paper_ledger_projection.py</files>
  <behavior>
    - A batch contains ascending, normalized `GatewayEvidence`; only fills with paper event sequence greater than the durable central projection cursor; their exact paper fill/observation/rule/Decimal provenance; and one account snapshot per `(account_id, product, scope, paper_event_sequence)`. Its values are frozen and reconstructible only from PaperGateway/store truth.
    - The already generic submit result, explicit `advance_market` result, terminal cancellation result, and `RecoveryService` client-ID lookup result each reach the same Paper bridge in event-sequence order; this plan does not alter their caller contracts.
    - Exact retry is idempotent; a same-identity conflicting batch records a central incident without overwriting central facts. Invoking projection leaves paper SQLite semantically unchanged, and a projector has neither a `TradingGateway` submit handle nor permit/lease/command allocation API.
  </behavior>
  <action>Write RED unit and real-filesystem SQLite integration tests spanning Spot, isolated margin, perpetual, and liquidation. Construct PaperGateway truth first, then assert batches normalize and order evidence, select only unseen fills by paper sequence, retain exact canonical Decimal provenance, and scope snapshots by account/product/pair-or-symbol/sequence. Instrument the already cut-over generic submit result, explicit `advance_market` result, terminal cancellation result, and `RecoveryService.reconcile_job` result to prove each reaches one `PaperProjectionBridge` then one `PaperEvidenceProjector.apply(batch)` path while no bridge path calls submission. Snapshot paper truth before and after projection. Verify exact retry, conflict incident, and restart cursor behavior against central SQLite.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_projection.py tests/integration/execution/test_paper_ledger_projection.py</automated></verify>
  <done>Tests fail because no immutable batch has a complete source-of-truth definition and the four lifecycle callers do not yet converge through one projection entry point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement central provenance persistence and read-only projector</name>
  <files>pa_agent/trading/persistence/migrations.py, pa_agent/trading/ports/ledger.py, pa_agent/trading/persistence/sqlite_ledger.py, pa_agent/trading/application/paper_projection.py, tests/unit/execution/test_paper_projection.py, tests/integration/execution/test_paper_ledger_projection.py</files>
  <behavior>
    - Existing non-paper fill rows remain valid after a forward migration; new paper fill rows require their complete existing canonical paper provenance and are unique by paper fill identity.
    - The ledger accepts ordered batch evidence/fills/snapshots atomically by source sequence, records a contradiction incident rather than replacing prior audit truth, and stores a projection cursor per scope.
    - `PaperEvidenceProjector` receives only a frozen batch plus narrow projection port; it cannot rebuild paper truth or invoke an outbound operation.
  </behavior>
  <action>Append forward migrations for paper fill provenance, projection cursors, and scoped sequence-keyed account snapshot audit rows without editing initial history. Extend the ledger port and `SQLiteExecutionLedger` with narrow atomic projection methods accepting canonical evidence, Plan 03-01/03-03 provenance, and snapshot values; preserve historical non-paper compatibility with an explicit source discriminator. Implement frozen `PaperProjectionBatch` and `PaperEvidenceProjector` in `paper_projection.py`; validate monotonic sequence, duplicate identity equality, and scope before any central write. The batch factory must query only PaperGateway/store truth and return no mutable store/gateway/ledger capability. Do not add submit, permit, lease, command construction, or paper-account writes to the projector. This retains D-04 exact economics and D-10 independent truth.
  </action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_projection.py tests/integration/execution/test_paper_ledger_projection.py tests/unit/execution/test_gateway_contract.py</automated></verify>
  <done>Central SQLite persists one immutable, idempotent audit projection per paper sequence while all paper account/order truth remains in the independent paper database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compose the automatic Paper bridge and prove four single-owner producers</name>
  <files>pa_agent/trading/application/paper_projection.py, pa_agent/trading/application/paper_runtime.py, tests/unit/execution/test_paper_projection.py, tests/integration/execution/test_paper_ledger_projection.py</files>
  <behavior>
    - `PaperProjectionBridge` implements `GatewayOperationObserver.observe_operation(result)` by resolving only a `GatewayOperationReference` through the Paper read-reference resolver, then applying exactly that immutable batch through `PaperEvidenceProjector`.
    - `PaperTradingRuntime` in `application/paper_runtime.py` is the sole production composition owner: it constructs one bridge and injects it into `PaperGateway`, `SubmissionCoordinator`, and `RecoveryService`. `KillSwitchService` remains request-only; terminal cancellation evidence is delivered by PaperGateway.
    - `SubmissionCoordinator` owns submit, PaperGateway owns `advance_market` plus terminal cancellation, and RecoveryService owns lookup. Each automatically reaches the bridge once in paper event-sequence order. Duplicate delivery is idempotent; missing/foreign/uncommitted reference or central projection failure cannot mutate Paper truth or cause a new submit.
  </behavior>
  <action>Implement `PaperProjectionBridge.observe_operation(result)` in `paper_projection.py` as the Paper-specific generic observer and implement `PaperTradingRuntime` in `paper_runtime.py` as the only composition seam. The runtime creates one bridge with a narrow Paper reference reader plus narrow central projection port, then passes the identical observer to PaperGateway, SubmissionCoordinator, and RecoveryService constructors. Keep KillSwitchService request-only. Extend unit and real SQLite integration tests to execute protected submit, `advance_market`, terminal cancellation evidence, and durable recovery lookup through those constructed components; assert every path automatically produces one bridge projection with no manual test bridge invocation and no duplicate gateway/application delivery. Snapshot PaperStore before and after projection, test duplicate/failure/reopen delivery, and prove a projection error preserves Paper truth and never causes re-submit, permit, lease, command, or client-ID allocation. Do not add a PaperStore write handle, permit, lease, command, or outbound capability to the bridge, runtime, or observer.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_projection.py tests/integration/execution/test_paper_ledger_projection.py tests/unit/execution/test_gateway_operation_bridge.py tests/integration/execution/test_uncertain_recovery.py tests/integration/execution/test_kill_switch.py</automated></verify>
  <done>One named runtime composition injects one bridge observer into the three owning producers; submit, advance, terminal cancellation, and recovery project exactly once without Paper-truth mutation or outbound authority.</done>
</task>
<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | — | Auditable paper lifecycle after submit, observation, cancel, and restart | 03-07 | COVERED | Single-owner automatic result delivery feeds one-way audit projection.
REQ | SIM-01 | Persistent fills, balances, fees, deterministic/restart behavior | 03-07 | COVERED | Sequence-keyed batch and central audit migration.
RESEARCH | Fill provenance and unified projection gap | 03-07 | COVERED | Complete batch source contract plus four producer wiring paths.
CONTEXT | D-04 | Exact Decimal rule/input provenance | 03-07 | COVERED | Batch and central migration persist every canonical provenance field.
CONTEXT | D-09-D-11 | Ordered/idempotent lifecycle facts | 03-07 | COVERED | Sequence cursor, conflict incident, and batch idempotence.
CONTEXT | D-10 | Independent paper truth | 03-07 | COVERED | Batch factory reads paper truth; projection cannot write it.
CONTEXT | D-12 | Uncertain result never resubmits | 03-07 | COVERED | Observer failure remains lookup-only with no second submit.
</source_audit>
<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Paper truth -> batch | Batch is a normalized read-only view of committed paper facts. |
| batch -> central ledger | Only ordered canonical facts may append to audit projection. |
| lifecycle callers -> projector | Submit, control, cancellation, and recovery may forward batches but cannot grant projection submission authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-24 | Repudiation | fill/account provenance | critical | mitigate | Immutable sequence-keyed batch and exact canonical economic data persist centrally. |
| T-03-25 | Tampering | projection replay/conflict | high | mitigate | Equality idempotence, scope cursor, and append-only contradiction incidents. |
| T-03-26 | Elevation | projection authority | critical | mitigate | Projector accepts no gateway/permit/lease/command capability and cannot submit. |
| T-03-27 | Tampering | paper truth inversion | critical | mitigate | Batch source is PaperGateway/store; central projection never feeds paper queries. |
| T-03-28 | Replay | post-accept failure | critical | mitigate | Preserve ambiguous state and recovery lookup; no retry submission. |
| T-03-SC | Tampering | package installs | low | accept | No package-manager operation is planned. |
</threat_model>

<verification>
Run the projection unit/integration, gateway-contract, uncertain-recovery, and kill-switch suites. Prove ordered batches from submit, advance, cancellation resolution, and lookup converge once while projector code has no outbound capability.
</verification>

<success_criteria>
`PaperProjectionBatch` is a complete immutable audit input from independent paper truth. Its only consumers persist central evidence, fills, and scoped snapshots; its four lifecycle producers have exact one-way wiring and none can weaken the verified dispatch boundary.
</success_criteria>

<output>
Create `.planning/phases/03-paper-product-core/03-07-SUMMARY.md` when done.
</output>
