---
phase: 02-approval-and-risk-boundary
plan: 16
type: execute
wave: 2
depends_on: [02-14]
files_modified:
  - pa_agent/trading/domain/approval.py
  - pa_agent/trading/application/approval.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/integration/execution/test_approval_consumption.py
autonomous: true
gap_closure: true
requirements: [SAFE-02, SAFE-04, SIM-03]
must_haves:
  truths:
    - "T0 签发的票据可由 T0+1 秒等价、完整且新鲜的证据消费一次。"
    - "授权等价逐项绑定报价标识/价格、规则、账户、能力、费用、策略、目标和全部经济结果；除观测时间戳外的任一变化仍会使票据失效。"
    - "消费时的新证据和重评估作为新的可查询审计事实持久化。"
  artifacts:
    - path: pa_agent/trading/domain/approval.py
      provides: 明确区分授权不变量与仅审计刷新时间的票据比较
    - path: pa_agent/trading/persistence/sqlite_ledger.py
      provides: 原子消费内的语义绑定检查和新鲜审计事实
  key_links:
    - from: pa_agent/trading/application/approval.py
      to: pa_agent/trading/persistence/sqlite_ledger.py
      via: fresh EvidenceBundle/RiskAssessment 的语义绑定消费
      pattern: TicketBinding|consume_valid_ticket_and_begin_outbound
---

<objective>
让新鲜而经济等价的取证可消费现有票据，同时继续严格绑定候选、目标、策略与经济约束。

Purpose: 避免将观测时间变化误当作授权事实变化，又不放宽 D-10 的真实绑定失效语义。
Output: 语义绑定比较、消费刷新审计以及 T0/T0+1 秒 SQLite 回归。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/02-approval-and-risk-boundary/02-REVIEW.md
@.planning/phases/02-approval-and-risk-boundary/02-07-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-14-SUMMARY.md

<interfaces>
`TicketBinding` currently compares the full evidence digest and timestamp-bearing data-age digest. `EvidenceBundle.evidence_digest` deliberately includes all observation timestamps. The replacement must expose one named, testable authorization-equivalence API, not an ad hoc comparison inside SQLite.

`ApprovalService` already refreshes all evidence and reruns pure risk before consumption; the ledger still owns the one immediate consume/outbound transaction.

`02-PATTERNS.md` assigns `domain/approval.py` to the frozen canonical-value pattern, `application/approval.py` to the restricted orchestration pattern, and `sqlite_ledger.py` to the `BEGIN IMMEDIATE` conditional-rowcount/audit pattern. Put the pure authorization-equivalence function on `TicketBinding`; keep the service dependent on domain values and ledger port only, and retain append-only refresh audit facts inside the same durable consume decision.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify explicit authorization-equivalence regressions</name>
  <files>tests/integration/execution/test_approval_consumption.py</files>
  <behavior>
    - `TicketBinding.is_authorization_equivalent_to()` accepts only T0 to T0+1 second refresh where every required observation remains within the fixed policy freshness window and every non-timestamp authorization fact is identical.
    - The comparison binds candidate and source provenance; target mode/account/product; policy version/digest; capabilities; instrument rules; account balances/positions; quote identifier/bid/ask and selected execution price; fee currency/rate/version/amount; open-order/rate/loss/drawdown observations; all risk metrics/reason codes/accepted result; command side/amount; expected price; slippage; and data-age semantic result.
    - Candidate/source/target/policy/quote/rule/account/capability/fee/economic/risk changes, an expired ticket, or any observation outside the fixed freshness window each terminate without command, claim, outbound, or gateway call.
    - The successful refreshed consumption has distinct persisted evidence/assessment audit identities from issuance even though it does not allocate a second ticket or outbound authorization.
  </behavior>
  <action>Write RED real-SQLite tests named for `t0_to_t0_plus_one_equivalent_fresh_evidence_consumes_once` and each protected mutation family for verification gap 2. Script issuance observations at T0 and all ten fresh observations at T0+1 second with unchanged target-scoped values; assert exactly one consumed ticket, command, claim, outbound authorization, and gateway call. Build a table-driven mutation matrix for candidate/source, target mode/account/product, policy, capabilities, rules, balances/positions, quote identifier/bid/ask, fee rate/currency/version, open-order/rate/loss/drawdown, quantity, expected price, calculated fee, slippage, risk metrics/reason codes/accepted result, and stale observation age. Per D-05, D-06, D-08, D-10, and D-11, assert a timestamp-only refresh is the sole permitted difference and every other mutation persists a binding invalidation before authority exists. Query proposal evidence/assessment and ticket-event rows to prove the T0+1 bundle and reassessment are appended as distinct audit facts.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_approval_consumption.py</automated></verify>
  <done>RED coverage proves only a complete T0-to-T0+1 identical-value fresh refresh can consume once, while every protected binding or freshness mutation remains non-submit-capable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the pure authorization-equivalence API and atomic refresh audit</name>
  <files>pa_agent/trading/domain/approval.py, pa_agent/trading/application/approval.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_approval_consumption.py</files>
  <action>Implement `TicketBinding.is_authorization_equivalent_to(current, *, policy, now)` as a pure, immutable, testable comparison. It must bind candidate/source/command, target mode/account/product, policy version/digest, capabilities, rules, account facts, quote identifier and side-selected price, fee currency/rate/version/amount, open-order/rate/loss/drawdown, every review economic value, and the complete accepted risk result. It may disregard only raw observation timestamps and their derived storage digests after independently proving every observation in the new complete bundle is within the fixed `phase2-v1` freshness window; it must not disregard data-age semantics or any economic result. This is intentionally limited to identical-value T0 to T0+1 refreshes and does not cancel D-10: any changed protected fact invalidates. Use `02-PATTERNS.md` pure-function and rowcount patterns: ApprovalService passes complete fresh facts, then SQLite appends fresh evidence and reassessment audit facts and conditionally consumes the pending ticket in one immediate transaction; check every conditional update rowcount before command/claim/outbound creation. Preserve D-07 reason-coded failures, D-11 single use, expiry, kill-switch checks, and no-cache D-05 collection.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_approval_consumption.py &amp;&amp; .venv/bin/ruff check pa_agent/trading/domain/approval.py pa_agent/trading/application/approval.py pa_agent/trading/persistence/sqlite_ledger.py tests/integration/execution/test_approval_consumption.py</automated></verify>
  <done>The explicit TicketBinding API permits exactly the stated fresh equivalence case, preserves all D-10 protected bindings, and stores every successful refresh as auditable facts in the same atomic consume decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fresh evidence/assessment -> prior ticket | Only timestamps may differ after each new observation passes the fixed freshness window; every listed authorization fact remains bound. |
| refreshed facts -> consume transaction | The current assessment must be retained and checked before outbound authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-34 | Tampering | `TicketBinding.is_authorization_equivalent_to` | high | mitigate | Testable pure comparison binds quote/rule/account/capability/fee/policy/target and all economics; it permits only T0-to-T0+1 timestamp changes after full freshness validation, preserving D-10. |
| T-02-35 | Repudiation | consumption refresh audit | medium | mitigate | Persist fresh evidence and reassessment as distinct canonical audit facts for every successful consume attempt. |
| T-02-SC | Tampering | package installs | low | accept | No installation is planned. |
</threat_model>

<verification>
Run the focused consumption integration file after each task and the full offline execution corpus after Wave 2.
</verification>

<success_criteria>
Only a complete, fixed-window fresh T0-to-T0+1 observation set with identical authorization facts can consume a valid approval. Timestamps never relax D-10: any quote, rule, account, capability, fee, policy, target, candidate, risk, or economic change terminates the ticket.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-16-SUMMARY.md` when done.
</output>
