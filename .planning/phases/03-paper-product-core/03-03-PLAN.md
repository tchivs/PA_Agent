---
phase: 03-paper-product-core
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - pa_agent/trading/domain/models.py
  - pa_agent/trading/domain/__init__.py
  - pa_agent/trading/domain/approval.py
  - pa_agent/trading/persistence/migrations.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/unit/execution/test_paper_product_models.py
  - tests/integration/execution/test_paper_product_migration.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "A protective exit is one frozen, canonical, Decimal-only value whose authority, validation, digest material, serialization, reconstruction, and approval invalidation rules are identical on every durable path."
    - "Margin and perpetual product facts become immutable candidate/command facts before risk assessment; later account, debt, health, capability, and margin facts must come only from fresh evidence."
    - "Legacy Paper Spot candidate, ticket, command, and persisted records remain readable after forward migration, while new product contexts reconstruct only from canonical durable payloads."
  artifacts:
    - path: pa_agent/trading/domain/models.py
      provides: canonical `ProtectiveExitPlan`, product-context canonical serialization/reconstruction, and compatibility validation
    - path: pa_agent/trading/domain/__init__.py
      provides: public domain exports for the canonical protective-exit and product-context contracts
    - path: pa_agent/trading/persistence/migrations.py
      provides: forward compatibility migration for persisted product-context payload/version data
    - path: pa_agent/trading/persistence/sqlite_ledger.py
      provides: durable canonical command reconstruction without caller-supplied product data
  key_links:
    - from: CandidateExecutionIntent and TicketBinding digests
      to: ProtectiveExitPlan and ProductContext canonical payload
      via: one stable serialization used for candidate, ticket, policy, and command binding
    - from: SQLite command reconstruction
      to: ProductContext reconstruction
      via: persisted canonical payload/version only; never a current caller map or mutable settings
---

<objective>
Define the durable product-context contract before any margin/perpetual accounting or admission work.

Purpose: every later product rule needs one immutable source of meaning. This plan resolves the research questions and moves canonical model ownership, domain exports, serialization/reconstruction, and legacy migration compatibility ahead of the perpetual slice.
Output: Canonical protective-exit/product-context values, durable compatibility path, and focused contract/migration tests.
</objective>

<execution_context>
@/root/.omp/agent/gsd-core/workflows/execute-plan.md
@/root/.omp/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-paper-product-core/03-CONTEXT.md
@.planning/phases/03-paper-product-core/03-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@pa_agent/trading/domain/models.py
@pa_agent/trading/domain/approval.py
@pa_agent/trading/domain/__init__.py
@pa_agent/trading/persistence/migrations.py
@pa_agent/trading/persistence/sqlite_ledger.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify canonical protective exit and product-fact authority</name>
  <files>tests/unit/execution/test_paper_product_models.py</files>
  <behavior>
    - `ProtectiveExitPlan` is frozen and Decimal-only with `symbol`, `entry_side`, derived opposite `exit_side`, `trigger_price`, optional bounded `limit_price`, `maximum_loss`, mandatory `reduce_only=True`, `policy_version`, and canonical digest. It rejects nonpositive prices/loss, nonfinite Decimal values, wrong symbol/side, a limit on the unsafe side of its trigger, or any non-reduce-only plan.
    - The source analysis snapshot remains the authority for symbol, side, order type, quantity, and price intent. The product-admission request is the authority for the selected target, isolated pair/symbol, borrow asset, auto-repay, leverage, position/margin mode, and exit plan at candidate creation. Fresh gateway evidence is the sole authority for mutable collateral, debt, interest, health, borrow availability, capability, mark, and margin adequacy facts.
    - Changing any context or exit field changes candidate/ticket/command binding material and invalidates approval. Canonical serialization round-trips exactly and rejects unknown, missing, duplicate, or noncanonical fields.
  </behavior>
  <action>Write RED domain-contract tests for a frozen `ProtectiveExitPlan` and the existing isolated-margin/perpetual context values. Assert the exact authority partition above, target/product/symbol/side binding, canonical Decimal text, deterministic key ordering, digest stability, and rejection of unsafe/malformed plans. Add ticket-binding tests proving a changed protective-exit, leverage, borrow/repay, or product-context payload cannot reuse an approval. Per D-06 and D-07, prohibit generic leverage, cross/portfolio margin, hedge mode, and auto-add margin by making those forms unrepresentable or controlled validation failures.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_product_models.py tests/unit/execution/test_models.py</automated></verify>
  <done>Tests fail because the current contexts lack a canonical protective-exit contract, authoritative serialization boundary, and binding-invalidation coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical values, exports, and digest binding</name>
  <files>pa_agent/trading/domain/models.py, pa_agent/trading/domain/__init__.py, pa_agent/trading/domain/approval.py, tests/unit/execution/test_paper_product_models.py</files>
  <behavior>
    - Candidate and ticket binding accepts only the canonical product-context serializer and includes its exact canonical payload/digest in existing digest material.
    - New perpetual entry contexts require the matching `ProtectiveExitPlan`; an exit is reduce-only and cannot increase absolute exposure.
    - Domain imports expose the new canonical contracts without creating an application, gateway, UI, or mutable configuration dependency.
  </behavior>
  <action>Implement `ProtectiveExitPlan` in `domain/models.py` and make the canonical product-context serializer/reconstructor the sole in-domain representation for Spot, isolated-margin, and USDT perpetual values. Use string-canonical `Decimal` representation and explicit schema/version validation. Extend `UsdtPerpetualOrderContext` to require the plan for exposure-increasing entries, derive/validate the exit side from candidate side at binding time, and preserve legal reduce-only close semantics. Update `CandidateExecutionIntent`, ticket binding, and their digest material in `domain/approval.py` to consume this canonical payload, then export the contracts from `domain/__init__.py`. Do not read account, policy, evidence, analysis, UI, or settings state in these values; their mutable facts belong to Plan 03-09 evidence collection. This resolves the protective-exit definition and D-06/D-07 fact-boundary decisions before any product accounting executes.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_product_models.py tests/unit/execution/test_intent_factory.py</automated></verify>
  <done>All new product context is immutable, canonical, exported, digest-bound, and incapable of silently changing an approval after candidate creation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add forward-compatible persistence and canonical reconstruction</name>
  <files>pa_agent/trading/persistence/migrations.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_paper_product_migration.py</files>
  <behavior>
    - Forward migration preserves every existing Phase 2 Spot row and its historical meaning; it neither rewrites initial migration history nor fabricates new margin/perpetual data.
    - New candidate/ticket/command records persist exactly one versioned canonical context payload and digest. Lease-time command reconstruction rejects malformed/mismatched durable payloads before gateway dispatch.
    - Reopen reconstructs the same context and exit-plan digest from SQLite alone. A caller cannot supply replacement product fields during ticket consumption, lease, recovery, or reconstruction.
  </behavior>
  <action>Write the compatibility migration and reconstruction tests first, using a pre-Phase-03 Paper Spot database fixture plus new isolated-margin and perpetual rows. Then append an idempotent forward migration in `migrations.py` and update `sqlite_ledger.py` to persist/reconstruct only the Plan 03-03 canonical context payload/version/digest in the existing candidate, ticket, and command lifecycle. Preserve legacy Spot reads through an explicit legacy decoder that produces the canonical Spot context, while rejecting legacy assumptions for non-Spot records. Verify ticket consumption, permit lease, recovery lookup, and reopen obtain context only from durable JSON and validate identity against candidate/ticket/policy binding. Do not alter existing permit/lease semantics, accept caller-injected context, add an alternate command constructor, or modify user-owned SQLite bootstrap work.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/integration/execution/test_paper_product_migration.py tests/integration/execution/test_approval_consumption.py tests/integration/execution/test_uncertain_recovery.py</automated></verify>
  <done>Legacy Spot records remain readable, new product records preserve exact canonical context across reopen, and corrupted or substituted product data cannot reach a leased outbound submission.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | — | Safe auditable access to all Paper products | 03-03 | COVERED | Canonical durable context precedes admission and accounting.
REQ | SIM-01 | Product configuration, leverage limits, restart recovery | 03-03 | COVERED | Versioned product payload reconstructs after SQLite reopen.
RESEARCH | Open questions 1 and 2 | Protective exit and product-fact authority | 03-03 | COVERED | Canonical definition and authority partition are test-locked.
CONTEXT | D-06 | Isolated pair borrow/repay facts | 03-03 | COVERED | Immutable context is distinct from collector-owned mutable account facts.
CONTEXT | D-07 | Isolated one-way leverage/protective exit | 03-03 | COVERED | Exit validity and approval invalidation are canonical/digest-bound.
CONTEXT | D-12 | No resubmission/alternate authority | 03-03 | COVERED | Durable reconstruction retains existing permit and lease route only.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source/admission facts -> candidate | Frozen context must have exact target/symbol/side binding before assessment. |
| candidate -> ticket/command persistence | Canonical payload and digest bind approval to reviewed product facts. |
| SQLite row -> lease reconstruction | Only durable canonical data may rebuild a command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-08 | Tampering | protective exit | critical | mitigate | Frozen validated plan, canonical digest, and approval invalidation test suite. |
| T-03-09 | Elevation | product context | critical | mitigate | Exact target/product/symbol binding and no generic/cross/hedge context. |
| T-03-10 | Repudiation | durable reconstruction | high | mitigate | Versioned canonical serialization and persisted digest verification across reopen. |
| T-03-11 | Elevation | caller context substitution | critical | mitigate | Lease/recovery reconstruct only stored payload; no public replacement input exists. |
| T-03-SC | Tampering | package installs | low | accept | No package-manager operation is planned. |
</threat_model>

<verification>
Run the model, migration, approval-consumption, and uncertain-recovery tests. Prove every protective-exit or product-context mutation invalidates approval, legacy Spot reads survive, and SQLite reconstructs no caller-injected product facts.
</verification>

<success_criteria>
Phase 03 has one durable, immutable product context contract: protective exits are fully defined and approval-bound; margin/perpetual facts have explicit authority; and forward-compatible serialization/reconstruction preserves legacy Spot records before perpetual work begins.
</success_criteria>

<output>
Create `.planning/phases/03-paper-product-core/03-03-SUMMARY.md` when done.
</output>
