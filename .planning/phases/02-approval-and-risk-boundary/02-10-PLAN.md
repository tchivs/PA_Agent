---
phase: 02-approval-and-risk-boundary
plan: 10
type: execute
wave: 6
depends_on: [02-03, 02-09]
files_modified:
  - pa_agent/trading/domain/errors.py
  - pa_agent/trading/domain/risk.py
  - pa_agent/trading/application/risk_engine.py
  - tests/unit/execution/test_risk_engine.py
autonomous: true
gap_closure: true
requirements: [SAFE-01, SAFE-02]
must_haves:
  truths:
    - "唯一 Paper Spot phase2-v1 策略包含绑定到目标和产品的固定 1000 USDT 最大总敞口。"
    - "当前同标的仓位的绝对名义敞口加候选订单名义金额超过策略上限时，纯风险引擎返回稳定拒绝且不接受评估。"
  artifacts:
    - path: pa_agent/trading/domain/risk.py
      provides: 进入 policy digest 的 maximum_total_exposure 限制
    - path: pa_agent/trading/application/risk_engine.py
      provides: projected exposure 计算和稳定风险拒绝
  key_links:
    - from: pa_agent/trading/application/risk_engine.py
      to: pa_agent/trading/domain/risk.py
      via: RiskPolicy.maximum_total_exposure 与 AccountObservation.positions 的目标绑定计算
      pattern: maximum_total_exposure|EXPOSURE_LIMIT_EXCEEDED
  prohibitions:
    - "RiskEngine 不导入 gateway、ledger、ProposalService、ApprovalService 或 SubmissionCoordinator。"
    - "敞口阈值不得由调用方、全局默认值或不支持产品策略提供。"
---

<objective>
把当前仓位和候选订单的总敞口从展示指标提升为 selected-target、产品绑定的确定性风险门。

Purpose: 让超过 Paper Spot 风险预算的评估在建票或出站授权之前失败关闭。
Output: 固定 1000 USDT 最大总敞口、稳定风险原因码、投影敞口度量和纯函数边界测试。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/02-approval-and-risk-boundary/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify fixed product-bound exposure policy boundaries</name>
  <files>tests/unit/execution/test_risk_engine.py</files>
  <read_first>Read `pa_agent/trading/domain/risk.py`, `pa_agent/trading/domain/errors.py`, `pa_agent/trading/application/risk_engine.py`, `pa_agent/trading/domain/models.py` definitions for `Position` and `AccountObservation`, `tests/unit/execution/test_risk_engine.py`, and `02-VERIFICATION.md` gap 2.</read_first>
  <behavior>
    - `select_phase2_policy(make_execution_target())` exposes a fixed `maximum_total_exposure` of exact Decimal 1000 and includes it in the immutable policy digest.
    - A matching Paper Spot account with existing same-symbol exposure plus the candidate order notional exactly at 1000 is accepted when all other gates pass; a strictly higher projected gross exposure is rejected with one `EXPOSURE_LIMIT_EXCEEDED` reason.
    - Opposite-signed or malformed position quantity is handled using canonical finite Decimal values and cannot evade the gross-exposure comparison.
  </behavior>
  <action>Write RED unit cases using target-matched `Position` values in the existing `EvidenceBundle` factory. Assert the fixed policy's `maximum_total_exposure == Decimal("1000")`, digest variation when that field is represented in canonical policy material, exact-boundary acceptance, and a one-cent-over projected gross exposure rejection with `RiskRejectionReason.EXPOSURE_LIMIT_EXCEEDED`. Cover an opposite-signed same-symbol position so the check uses absolute current position notional, and retain unrelated-symbol positions to prove only the candidate symbol's selected-target account exposure participates. Keep all inputs exact Decimal and assert the result is rejected without any service-side interaction.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_risk_engine.py</automated></verify>
  <acceptance_criteria>The new tests fail before implementation and require a policy-bound maximum gross exposure, exact boundary behavior, stable rejection code, and no dependency outside the pure risk evaluator.</acceptance_criteria>
  <done>Unit coverage proves current selected-target product exposure is enforced rather than merely reported as a metric.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement projected exposure rejection in the pure risk boundary</name>
  <files>pa_agent/trading/domain/errors.py, pa_agent/trading/domain/risk.py, pa_agent/trading/application/risk_engine.py, tests/unit/execution/test_risk_engine.py</files>
  <read_first>Read the failing tests from Task 1, `pa_agent/trading/domain/risk.py`, `pa_agent/trading/application/risk_engine.py`, `pa_agent/trading/domain/models.py` decimal canonicalization helpers, and `02-03-SUMMARY.md` decisions on target-bound policies and pure risk evaluation.</read_first>
  <behavior>
    - `RiskPolicy` normalizes and validates `maximum_total_exposure` as an immutable fixed Decimal policy value.
    - `RiskAssessment.metrics` retains existing exposure and exposes projected exposure, while an above-limit projected value produces an unaccepted assessment with the stable exposure reason.
  </behavior>
  <action>Add `EXPOSURE_LIMIT_EXCEEDED` to `RiskRejectionReason`. Extend frozen `RiskPolicy`, its fixed-value validation, digest material, and `select_phase2_policy()` with `maximum_total_exposure=Decimal("1000")` for the only paper-spot-primary / paper-account / Spot policy. In `RiskEngine.assess()`, calculate existing gross exposure only from the selected target's candidate symbol positions using absolute finite Decimal quantity multiplied by mark price; add candidate notional to derive `projected_exposure`; append the stable exposure reason when projected exposure exceeds the policy limit; and include both exact Decimal metrics in `RiskAssessment`. Preserve all D-08 target/policy matching checks, the current evidence and balance gates, and the engine's pure dependency boundary.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_risk_engine.py && .venv/bin/ruff check pa_agent/trading/domain/errors.py pa_agent/trading/domain/risk.py pa_agent/trading/application/risk_engine.py tests/unit/execution/test_risk_engine.py</automated></verify>
  <acceptance_criteria>The only selectable policy has a digest-bound 1000 USDT total-exposure cap, and every projected excess returns `EXPOSURE_LIMIT_EXCEEDED` in an unaccepted immutable assessment.</acceptance_criteria>
  <done>SAFE-02 now has a selected-target, product-bound exposure constraint that is executable by the pure risk evaluator.</done>
</task>

</tasks>

## Artifacts this phase produces

- `RiskPolicy.maximum_total_exposure` fixed to 1000 USDT for `phase2-v1` Paper Spot.
- `RiskRejectionReason.EXPOSURE_LIMIT_EXCEEDED`.
- `existing_exposure` and `projected_exposure` immutable risk metrics plus unit regression coverage.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| account positions and candidate -> RiskEngine | Current exposure must be bounded using the exact selected target/product policy. |
| risk policy -> assessment | Callers cannot substitute a global or temporary exposure threshold. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-18 | Elevation/Tampering | `RiskEngine.assess` | high | mitigate | Digest-bound 1000 USDT target/product cap, projected-gross-exposure calculation, and stable rejection tests enforce SAFE-02 and D-08. |
| T-02-19 | Tampering | immutable risk policy | high | mitigate | Fixed `phase2-v1` validation rejects caller overrides and folds the exposure cap into the policy digest. |
| T-02-SC | Tampering | package installs | low | accept | No dependency installation is planned. |
</threat_model>

<verification>
Run `.venv/bin/pytest -q tests/unit/execution/test_risk_engine.py` after each task. The downstream ticket and consumption proof is delivered by `02-11-PLAN.md`.
</verification>

<success_criteria>
No accepted risk assessment can represent projected Paper Spot BTCUSDT exposure above the fixed target-bound 1000 USDT policy cap.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-10-SUMMARY.md` when done.
</output>
