---
phase: 03-paper-product-core
plan: 05
type: tdd
wave: 6
depends_on: [03-01, 03-02, 03-03, 03-04, 03-09, 03-11]
files_modified:
  - pa_agent/trading/gateways/paper/accounting_margin.py
  - pa_agent/trading/gateways/paper/gateway.py
  - tests/unit/execution/test_paper_margin.py
  - tests/integration/execution/test_paper_margin_recovery.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "Each isolated-margin pair owns separate collateral, borrowed debt, accrued interest, available assets, and health; no cross-pair offset is possible."
    - "Only an approved, policy-bound isolated-margin command with explicit borrow/repay context may open; unhealthy, stale, insufficient, or cross/portfolio inputs reject before a simulated fill."
    - "Market observations explicitly accrue versioned Decimal interest and can update only their own pair’s accounting state after restart."
  artifacts:
    - path: pa_agent/trading/gateways/paper/accounting_margin.py
      provides: pair-scoped Decimal collateral/debt/interest/health/borrow-repay projector
    - path: tests/unit/execution/test_paper_margin.py
      provides: isolated accounting, health rejection, interest, and repayment regressions
    - path: tests/integration/execution/test_paper_margin_recovery.py
      provides: reopened paper store pair-isolation and query recovery regression
  key_links:
    - from: immutable isolated-margin command context
      to: PaperGateway margin accounting
      via: target/policy-bound pair, borrow asset, and repayment facts from Plan 03-03
    - from: explicit market observation
      to: pair health/interest update
      via: paper event sequence, never cross-pair aggregation or clock polling
---

<objective>
Add isolated-margin PaperGateway semantics on top of the durable shared lifecycle while keeping every pair’s credit state strictly independent.

Purpose: D-06 demands real pair isolation, debt, interest, health, and borrow/repay behavior—not reuse of Spot balances with optional fields.
Output: Dedicated margin accounting projector, gateway routing for the already protected product context, and focused offline restart/isolation tests.
</objective>

<execution_context>
@/root/.omp/agent/gsd-core/workflows/execute-plan.md
@/root/.omp/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-paper-product-core/03-CONTEXT.md
@.planning/phases/03-paper-product-core/03-RESEARCH.md
@.planning/phases/03-paper-product-core/03-03-PLAN.md
@.planning/phases/03-paper-product-core/03-04-PLAN.md
@.planning/phases/03-paper-product-core/03-09-PLAN.md
@pa_agent/trading/domain/models.py
@pa_agent/trading/domain/paper.py
@pa_agent/trading/gateways/paper/store.py
@pa_agent/trading/gateways/paper/gateway.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify pair-isolated margin borrow, interest, health, and recovery invariants</name>
  <files>tests/unit/execution/test_paper_margin.py, tests/integration/execution/test_paper_margin_recovery.py</files>
  <behavior>
    - Two pairs sharing an asset retain distinct collateral, debt principal, accrued Decimal interest, availability, and health. A surplus in one pair never improves the other pair’s acceptance or health.
    - A matching policy-bound isolated-margin order uses only its exact `isolated_symbol` context and borrow asset; unsupported cross/portfolio context, insufficient collateral, stale/invalid pair data, or health below the versioned threshold rejects before an order/fill mutation.
    - Explicit new observations apply the versioned Decimal interest formula only to the scoped pair, then re-evaluate health before/after fill; repayment reduces debt/interest according to the immutable auto-repay context and never draws from another pair.
    - Reopen returns the same pair snapshots, order/fill provenance, health, debt, and interest, and a subsequent observation cannot regress a terminal order or reuse a stale observation version.
  </behavior>
  <action>Write RED unit/integration scenarios using the Plan 03-03 isolated-margin target/context and a real paper SQLite database. Define exact Decimal initial collateral, debt/interest policy inputs, books, and observation versions. Assert pair isolation with conflicting health outcomes, health/available-balance prefill rejects with no fill, exact interest/repayment math, and partial fills through the shared matcher. Reopen and query each pair independently. Include a negative regression where cross/portfolio/cross-pair offset cannot be represented by a command, policy, or store key. Per D-06, prove isolated pair ownership and no offsetting; per D-03/D-04/D-09/D-11, prove explicit versioned observations, exact economics, ordered races, and non-regression.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_margin.py tests/integration/execution/test_paper_margin_recovery.py</automated></verify>
  <done>The focused suite fails because the PaperGateway has no pair-scoped debt/interest/health projector and cannot safely route a margin context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement isolated-margin accounting and route only canonical pair contexts</name>
  <files>pa_agent/trading/gateways/paper/accounting_margin.py, pa_agent/trading/gateways/paper/gateway.py, tests/unit/execution/test_paper_margin.py, tests/integration/execution/test_paper_margin_recovery.py</files>
  <behavior>
    - Margin state has a durable `(account_id, isolated_symbol)` key and cannot be queried/updated as a global collateral pool.
    - Borrow, accrued interest, available balance, health, matched fills, repayments, and snapshots update with the same paper event sequence transaction.
    - Gateway account/open-order/fill/reconcile responses remain canonical and query the independent paper store; it does not call central ledger or create a new submission route.
  </behavior>
  <action>Implement `accounting_margin.py` with separate pair-scoped records and a versioned Decimal interest/health formula declared by `PaperEconomicPolicy`. Validate the exact isolated pair, collateral, debt freshness/health, borrow asset, and auto-repay context before accepting a margin order. Within the existing paper-store observation transaction, apply matching candidates only to the identified pair, accrue interest only from explicit observation inputs/version, settle fill assets/debt, conditionally repay only that pair, and atomically persist a canonical margin snapshot and immutable fill provenance. Update `PaperGateway` product dispatch to select this projector only for canonical `IsolatedMarginOrderContext`; retain Spot behavior unchanged and reject unsupported cross/portfolio modes before a paper order. Do not use a shared account pool, a float formula, wall-clock interest, a network adapter, a central-ledger balance read, or a second submission method. This implements D-06 while retaining D-01 through D-05/D-09/D-11 guarantees from the shared foundation.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_margin.py tests/integration/execution/test_paper_margin_recovery.py tests/unit/execution/test_paper_spot.py</automated></verify>
  <done>Margin orders and observations mutate only their durable isolated pair, calculate Decimal interest/health from explicit market events, fail before a fill for invalid credit contexts, and preserve all Spot behavior and gateway authority boundaries.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | — | Complete auditable isolated-margin practice flow | 03-05 | COVERED | Protected command reaches a dedicated pair-scoped product projector.
REQ | SIM-01 | Paper isolated margin semantics, leverage/initial balance/recovery | 03-05 | COVERED | Pair snapshots, debt/interest/health and reopen tests are Decimal/versioned.
RESEARCH | Stage 3, shared matcher/separate accounting | Isolated pair debt/collateral/interest/health | 03-05 | COVERED | Uses dedicated margin module; avoids generic balance formula.
CONTEXT | D-06 | Independent pair collateral/debt/interest/available/health; no offsets | 03-05 | COVERED | Durable pair key/invariants and two-pair negative tests.
CONTEXT | D-03-D-04 | Explicit observations and versioned Decimal economics | 03-05 | COVERED | Interest policy inputs/version are observation-driven and immutable.
CONTEXT | D-09-D-11 | Ordered, non-regressing recovery behavior | 03-05 | COVERED | Reuses event cursor/sequence with reopened pair snapshots.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| isolated-margin context -> product projector | Only exact canonical pair/borrow/repay context is allowed. |
| observation -> pair accounting | One accepted scoped event may update one pair’s interest, health, fills, and snapshot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-16 | Tampering/Elevation | pair isolation | critical | mitigate | Pair-keyed state and cross-pair surplus/offset rejection tests. |
| T-03-17 | Tampering | debt/interest health | critical | mitigate | Versioned Decimal formula, explicit observation input, and atomic pre/post-fill health checks. |
| T-03-18 | Repudiation | borrow/repay accounting | high | mitigate | Immutable command context and event/fill/snapshot provenance persist every state change. |
| T-03-19 | Elevation | margin dispatch | critical | mitigate | Gateway accepts only leased outbound and rejects unrepresentable cross/portfolio contexts. |
| T-03-SC | Tampering | package installs | low | accept | No package changes. |
</threat_model>

<verification>
Run the focused margin unit/integration suite plus the Spot suite. Confirm exact pair accounting survives reopen; one pair’s collateral cannot rescue another; no invalid margin case creates a fill; and no local elapsed time accrues interest.
</verification>

<success_criteria>
Paper isolated margin is a real independent-pair simulation: every account fact is pair-scoped, credit health is Decimal and observation-driven, borrow/repay context is immutable, and cross/portfolio behavior remains impossible rather than silently approximated.
</success_criteria>

<output>
Create `.planning/phases/03-paper-product-core/03-05-SUMMARY.md` when done.
</output>
