---
phase: 03-paper-product-core
plan: 14
type: tdd
wave: 9
depends_on: [03-12]
files_modified:
  - pa_agent/trading/application/kill_switch.py
  - pa_agent/trading/ports/ledger.py
  - pa_agent/trading/persistence/migrations.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/integration/execution/test_paper_recovery_product_scope.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "SQLite persists every nonzero recovery scope’s exact target/account/product/pair-or-symbol key and selected policy identity, while legacy Spot scope rows remain readable only as legacy Spot scopes."
    - "Ledger-owned begin and complete recovery atomically revalidate exact scope, assessment/evidence digest, policy, and one-time transition state before READY; denial creates no ticket, permit, lease, command, client ID, or submit effect."
  artifacts:
    - path: pa_agent/trading/persistence/migrations.py
      provides: forward migration for durable product recovery scope and policy fields
    - path: pa_agent/trading/persistence/sqlite_ledger.py
      provides: transactionally revalidated product-scope begin/complete READY transitions
    - path: tests/integration/execution/test_paper_recovery_product_scope.py
      provides: real-SQLite forged, missing, cross-scope, stale, restart, and zero-authority recovery tests
  key_links:
    - from: durable command/cancellation record
      to: ledger-loaded recovery scope
      via: persisted target/product/pair-or-symbol and policy identity, never fixed Spot derivation
    - from: service-owned exact-scope assessment
      to: BEGIN IMMEDIATE READY transition
      via: assessment/evidence/policy/scope validation in the same ledger transaction
---

<objective>
Persist and atomically enforce the Plan 03-12 exact product recovery scope before Paper projection composition and final convergence.

Purpose: frozen domain scope and assessment values do not protect READY until SQLite independently reconstructs and validates them across process restart. This plan owns that one durable transaction boundary without widening recovery or dispatch authority.
Output: Idempotent recovery-scope migration, typed ledger/kill-switch integration, and real-SQLite denial/restart proofs.
</objective>

<execution_context>
@/root/.omp/agent/gsd-core/workflows/execute-plan.md
@/root/.omp/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-paper-product-core/03-CONTEXT.md
@.planning/phases/03-paper-product-core/03-RESEARCH.md
@.planning/phases/03-paper-product-core/03-12-PLAN.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@pa_agent/trading/application/kill_switch.py
@pa_agent/trading/ports/ledger.py
@pa_agent/trading/persistence/migrations.py
@pa_agent/trading/persistence/sqlite_ledger.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify real-SQLite product-scope READY transaction behavior</name>
  <files>tests/integration/execution/test_paper_recovery_product_scope.py</files>
  <behavior>
    - A persisted scope for Spot, isolated-margin, or perpetual retains its exact target/account/product/symbol-or-pair key and selected policy ID/version/digest after reopen; a legacy Phase 2 row decodes only as historical Spot scope.
    - `begin_recovery` and `complete_recovery` accept only the ledger-loaded Plan 03-12 exact scope and service-owned accepted assessment. Forged, missing, cross-product, cross-pair, cross-symbol, stale, mismatched-policy, and replayed begin/complete values leave the latch non-READY.
    - Valid exact-scope clearance uses one begin then matching complete transition; every denial records zero new ticket, permit, lease, command, client ID, gateway submit, or Paper mutation.
  </behavior>
  <action>Write RED real-filesystem SQLite tests using the completed Plan 03-12 frozen scope/assessment contracts and distinct persisted Spot, margin-pair, and perpetual-symbol Paper facts. Exercise scope persistence/reopen, legacy Spot compatibility, valid exact clearance, and forged/missing/cross-product/cross-pair/cross-symbol/stale/replayed inputs. Count durable authority and gateway side effects around every denial, including a process restart between begin and complete. Seed open work only through the protected dispatch chain; do not create a caller-owned assessment, command, permit, lease, or Paper truth substitute.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/integration/execution/test_paper_recovery_product_scope.py tests/integration/execution/test_kill_switch.py tests/integration/execution/test_uncertain_recovery.py</automated></verify>
  <done>Tests fail because SQLite has not yet persisted/revalidated product scope identity and cannot prove zero-authority denial after restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate and atomically enforce exact product recovery scope</name>
  <files>pa_agent/trading/application/kill_switch.py, pa_agent/trading/ports/ledger.py, pa_agent/trading/persistence/migrations.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_paper_recovery_product_scope.py</files>
  <behavior>
    - Forward migration persists product scope target/account/product/pair-or-symbol keys plus selected policy ID/version/digest for each nonzero durable scope without rewriting migration history.
    - SQLite command-scope creation, private assessment recording, begin, and complete derive only ledger-owned values and validate scope/policy/evidence/one-time transition state inside their immediate transactions.
    - `KillSwitchService` loads durable scopes, uses only service-owned assessments, and never admits a caller override; failure preserves latch state and cannot invoke dispatch.
  </behavior>
  <action>Append idempotent migration(s) and update the typed ledger/kill-switch integration for exact Plan 03-12 values. Persist target/account/product/pair-or-symbol and policy identity for nonzero command/cancellation scopes, preserve historical Spot scope rows with an explicit compatibility decoder, and reject legacy decoding for non-Spot scope data. In `SQLiteExecutionLedger`, revalidate scope identity, selected policy, fresh assessment/evidence digest, and one-time begin/complete transition state in the same `BEGIN IMMEDIATE` transactions that change the latch. In `KillSwitchService`, load ledger-owned scopes and service-recorded assessments with no caller scope override. Eliminate every nonzero fixed-Spot target derivation and aggregate-snapshot clearance branch. On any failure, preserve the latch and return denial with no ticket/permit/lease/command/client-ID/submit effect; do not add resubmission, polling, transport, UI, a central-ledger Paper truth read, or a coordinator bypass.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/integration/execution/test_paper_recovery_product_scope.py tests/integration/execution/test_kill_switch.py tests/integration/execution/test_uncertain_recovery.py tests/integration/execution/test_approval_consumption.py</automated></verify>
  <done>Durable recovery reaches READY only through one atomic exact product-scope transaction across restart, and every invalid scope/evidence path has zero outbound authority effects.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | — | Safe restart and kill-switch recovery for every Paper product | 03-14 | COVERED | Durable pair/symbol scope persistence and READY transaction follow Plan 03-12 domain contracts.
REQ | SIM-01 | Product semantics and restart recovery | 03-14 | COVERED | Reopened SQLite validates independent persisted product clearance facts.
RESEARCH | Durable product-scope recovery | 03-14 | COVERED | Exact scope/policy/evidence validation owns the durable transition boundary.
CONTEXT | D-06/D-07 | Pair/symbol product clearance | 03-14 | COVERED | Exact persisted product key prevents cross-scope clearance.
CONTEXT | D-09-D-12 | Ordered restart/recovery/no resubmit | 03-14 | COVERED | Atomic begin/complete and zero-authority denial tests preserve the boundary.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| service assessment -> SQLite recovery transition | Caller cannot substitute scope, policy, or evidence. |
| persisted scope -> restart reconstruction | Scope keys must preserve their original product identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-51 | Elevation | durable scope reconstruction | critical | mitigate | Persist exact target/product/pair-or-symbol/policy and reject mismatches. |
| T-03-52 | Replay | begin/complete transition | critical | mitigate | Revalidate one-time transition state in one immediate transaction. |
| T-03-53 | Elevation | recovery denial | critical | mitigate | Real-SQLite denials prove zero ticket/permit/lease/command/client-ID/submit effects. |
| T-03-SC | Tampering | package installs | low | accept | No package-manager operation is planned. |
</threat_model>

<verification>
Run product recovery-scope real-SQLite, kill-switch, uncertain-recovery, and approval-consumption tests. Confirm valid exact scopes alone reach READY and every forged/missing/cross/stale/restarted denial remains without outbound authority.
</verification>

<success_criteria>
The persisted SQLite recovery boundary enforces the exact Plan 03-12 product scope and evidence before READY. It remains Decimal-only, offline, paper-truth-independent, and incapable of allocating a new submission on recovery failure.
</success_criteria>

<output>
Create `.planning/phases/03-paper-product-core/03-14-SUMMARY.md` when done.
</output>
