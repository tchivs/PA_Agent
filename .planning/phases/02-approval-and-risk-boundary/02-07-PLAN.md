---
phase: 02-approval-and-risk-boundary
plan: 07
type: execute
wave: 6
depends_on: [02-08]
files_modified:
  - pa_agent/trading/ports/ledger.py
  - pa_agent/trading/application/approval.py
  - pa_agent/trading/application/submission.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/integration/execution/test_approval_consumption.py
autonomous: true
requirements: [SIM-03, SAFE-04]
must_haves:
  truths:
    - "一次成功批准只在一个 SQLite 事务中消费当前票据、绑定既有命令并写入 outbound_started。"
    - "双击、过期、绑定变化、事务失败或重启都不能从同一票据获得第二个 client order ID、出站授权或网关调用。"
  artifacts:
    - path: pa_agent/trading/ports/ledger.py
      provides: 原子消费当前票据并返回唯一 OutboundSubmission 的账本端口
    - path: pa_agent/trading/persistence/sqlite_ledger.py
      provides: 同一立即事务中的票据消费、命令绑定和 outbound_started 持久化
    - path: tests/integration/execution/test_approval_consumption.py
      provides: 真实 SQLite 并发、失败与重启的单次授权证明
  key_links:
    - from: pa_agent/trading/application/approval.py
      to: pa_agent/trading/ports/ledger.py
      via: consume_valid_ticket_and_begin_outbound 原子端口
      pattern: consume_valid_ticket_and_begin_outbound
    - from: pa_agent/trading/application/submission.py
      to: pa_agent/trading/ports/gateway.py
      via: 仅将账本返回的 OutboundSubmission 传给 submit_order
      pattern: OutboundSubmission|submit_order
---

<objective>
将已完成审阅和重新取证的有效票据原子地单次消费为 Phase 1 的唯一出站授权。

Purpose: 防止双击、竞态、过期或进程中断将操作员批准变成可重放的提交能力。
Output: 原子账本端口与 SQLite 实现、受限审批/提交交接，以及真实 SQLite 消费回归测试。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-VALIDATION.md
@.planning/phases/02-approval-and-risk-boundary/02-08-SUMMARY.md
@.planning/phases/01-execution-foundation/01-07-SUMMARY.md

<interfaces>
From Plan 02-08: `ProposalService` automatically creates exactly one current, digest-bound pending `phase2-v1` ticket only after candidate, complete fresh evidence, and accepted risk are persisted; `ApprovalService` cannot itself create admission or outbound authority.

From `pa_agent/trading/ports/ledger.py`: Phase 1 establishes ledger-owned client IDs, `SubmissionAdmission`, and `OutboundSubmission`; its separate public admission methods must not be chained after ticket consumption.

From `pa_agent/trading/application/submission.py`: the sole gateway call must receive only a ledger-produced `OutboundSubmission`; tickets, candidates, commands, GUI callbacks, alerts, and notifications remain invalid inputs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify atomic current-ticket consumption and outbound handoff</name>
  <files>tests/integration/execution/test_approval_consumption.py</files>
  <read_first>Read `pa_agent/trading/ports/ledger.py`, `pa_agent/trading/persistence/sqlite_ledger.py` methods `create_or_load_and_claim_submission` and `begin_outbound_submission`, `pa_agent/trading/application/approval.py`, `pa_agent/trading/application/submission.py`, Task 3 tests from 02-05, and `02-VALIDATION.md` rows for SIM-03 and SAFE-04.</read_first>
  <behavior>
    - Simultaneous approval/double-click, an injected failure inside the transaction, and process reopen yield at most one ticket consumption, one durable client order ID, and one outbound authorization.
    - The ledger transition checks pending status, fixed 60-second expiry, all `phase2-v1` bindings, current kill state, and conditional row count before returning the sole `OutboundSubmission`.
    - A submission coordinator accepts only the returned outbound object; failed, duplicate, expired, changed, or reopened approvals never reach the fake gateway.
  </behavior>
  <action>Write failing real-SQLite integration cases for D-11 concurrent approval, restart schedules, expiry/binding changes, and a controlled failure within the transaction. Use a gateway fake that records calls and rejects any value except a ledger-produced outbound object. Assert each case against ticket/event/claim row counts, the durable client order ID, outbound state, and gateway call count; keep all tests offline and do not use sleeps for concurrency coordination.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_approval_consumption.py</automated></verify>
  <acceptance_criteria>The tests fail before implementation and require one immediate transaction to decide ticket consumption, durable command identity, and outbound authority.</acceptance_criteria>
  <done>Wave 0 demonstrates that no stale, duplicate, failed, or reopened approval can produce a second authorization or a gateway call.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic ticket consumption and constrained submission handoff</name>
  <files>pa_agent/trading/ports/ledger.py, pa_agent/trading/application/approval.py, pa_agent/trading/application/submission.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_approval_consumption.py</files>
  <read_first>Read Task 1's failing tests, `pa_agent/trading/persistence/sqlite_ledger.py`, `pa_agent/trading/ports/ledger.py`, `pa_agent/trading/application/submission.py`, and `02-PATTERNS.md` sections “Approval, kill switch, and only outbound admission” and “SQLite migration, append-only audit, and atomic consumption”.</read_first>
  <behavior>
    - `consume_valid_ticket_and_begin_outbound(...)` returns one immutable `OutboundSubmission` only after all ticket conditions and ledger-owned command binding pass in one transaction.
    - `ApprovalService` delegates the final operation only after its fresh-evidence/risk precheck, and `SubmissionCoordinator` accepts no intermediate or advisory value.
  </behavior>
  <action>Extend the ledger with one atomic `consume_valid_ticket_and_begin_outbound(...)` operation. Within one existing `BEGIN IMMEDIATE`, verify D-10 hashes, the fixed 60-second TTL, `phase2-v1`, and current durable kill state; conditionally consume the pending ticket; create or bind the command through Phase 1 ledger-owned ID machinery; and set `outbound_started` before returning `OutboundSubmission`. Refactor `ApprovalService` to delegate only this final consume operation after its fresh-evidence/risk precheck. Refactor `SubmissionCoordinator` to accept only the ledger-produced approved outbound object and preserve ambiguity reconciliation without recreating admission. Do not perform a separate public admission then begin call after ticket consumption, and do not add a submit path to analysis, alerts, notifications, GUI, settings, or credential services.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_approval_consumption.py &amp;&amp; .venv/bin/ruff check pa_agent/trading/ports/ledger.py pa_agent/trading/application/approval.py pa_agent/trading/application/submission.py pa_agent/trading/persistence/sqlite_ledger.py tests/integration/execution/test_approval_consumption.py</automated></verify>
  <acceptance_criteria>The atomic-consumption tests pass: a successful approval returns exactly one ledger-generated client ID and immutable outbound authorization, while duplicate, expired, changed, failed, or reopened attempts produce no second authorization or gateway call.</acceptance_criteria>
  <done>Per D-11, exactly one transaction can consume a current `phase2-v1` ticket and create the Phase 1 outbound handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator approval -> ledger admission | An operator action must become exactly one durable authorization only after current verification. |
| ticket persistence -> outbound submission | Ticket status, hashes, expiry, and kill state must be checked in the same transaction that starts outbound authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Repudiation/Elevation | ticket consumption and `outbound_started` | high | mitigate | Conditional ticket consumption, hash/expiry/kill checks, command binding, and outbound start share one immediate SQLite transaction under D-11. |
| T-02-10 | Tampering | stale/replayed approval evidence | high | mitigate | Current ticket checks and fresh evidence/risk precheck reject stale or changed approval facts. |
| T-02-SC | Tampering | package installs | low | accept | No dependency installation is part of this plan. |
</threat_model>

<verification>
Run the consumption integration test after each task. Before the next wave, run `.venv/bin/pytest -q tests/unit/execution tests/integration/execution tests/property/execution`.
</verification>

<success_criteria>
An operator can obtain exactly one irrevocable outbound authorization only after reviewing complete current facts; every stale, changed, expired, duplicate, failed, or reopened ticket remains non-submit-capable.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-07-SUMMARY.md` when done.
</output>
