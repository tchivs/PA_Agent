---
phase: 01-execution-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pa_agent/trading/__init__.py
  - pa_agent/trading/domain/__init__.py
  - pa_agent/trading/domain/models.py
  - pa_agent/trading/domain/lifecycle.py
  - pa_agent/trading/domain/errors.py
  - tests/fixtures/execution_factories.py
  - tests/unit/execution/test_models.py
  - tests/property/execution/test_decimal_invariants.py
autonomous: true
requirements: [CORE-01, NFR-02]
must_haves:
  truths:
    - "Canonical execution values are immutable and use finite Decimal values for every monetary, quantity, price, fee, margin, leverage, and notional field."
    - "Spot, isolated margin, and USDT perpetual commands require distinct product contexts, and leverage is unavailable to spot commands."
    - "A lifecycle transition cannot infer a terminal remote result without normalized external evidence."
  artifacts:
    - path: pa_agent/trading/domain/models.py
      provides: Frozen canonical models, strict Decimal ingress, product contexts, and gateway evidence values.
    - path: pa_agent/trading/domain/lifecycle.py
      provides: Pure lifecycle transition guard and terminal-state classification.
    - path: tests/property/execution/test_decimal_invariants.py
      provides: Generated Decimal canonicalization and lifecycle invariants.
  key_links:
    - from: pa_agent/trading/domain/lifecycle.py
      to: pa_agent/trading/domain/models.py
      via: The transition guard accepts lifecycle event and state enums defined by the domain.
      pattern: "OrderState|LifecycleEvent"
---

<objective>
Establish the immutable Decimal domain and evidence-driven lifecycle rules that all later execution components obey, covering D-01 through D-04 and D-06.

Purpose: Make execution values and legal state movement independently testable before persistence, gateway contracts, or recovery behavior exist.
Output: Canonical trading domain, lifecycle guard, deterministic factories, and unit/property coverage.
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-execution-foundation/01-CONTEXT.md
@.planning/phases/01-execution-foundation/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define immutable Decimal execution values and product contexts</name>
  <files>pa_agent/trading/__init__.py, pa_agent/trading/domain/__init__.py, pa_agent/trading/domain/models.py, pa_agent/trading/domain/errors.py, tests/fixtures/execution_factories.py, tests/unit/execution/test_models.py, tests/property/execution/test_decimal_invariants.py</files>
  <behavior>
    - Test 1: Decimal ingress accepts Decimal and canonical text, and rejects binary floats, NaN, and infinities.
    - Test 2: frozen canonical commands, orders, fills, balances, positions, rules, capabilities, and evidence reject mutation and retain Decimal values.
    - Test 3: spot, isolated-margin, and USDT-perpetual contexts reject impossible field combinations; only the perpetual context carries product-gated leverage.
    - Test 4: generated finite Decimal values preserve their canonical text representation through domain serialization and parsing.
  </behavior>
  <action>Create the independent `pa_agent.trading.domain` package per D-01. Define public frozen dataclasses and enums for execution command, order request/order projection, fill, balance, position, instrument rules, product type, mode, gateway capabilities, account/quote/time/rule observations, and normalized reconciliation evidence per D-02 and D-04. Centralize strict Decimal parsing so public constructors accept only `Decimal` or text, reject floats and non-finite inputs, and preserve canonical string serialization per D-03. Model spot, isolated margin, and USDT perpetual as discriminated frozen contexts; omit generic leverage, borrowing behavior, adapter payloads, and execution behavior per D-04 and D-09. Add typed domain and reconciliation errors. Create deterministic factory helpers and write the unit/property tests before implementation without network, UI, LLM DTO, chart-bar, credential, or recommendation CSV input.</action>
  <verify><automated>python -m pytest tests/unit/execution/test_models.py tests/property/execution/test_decimal_invariants.py -q</automated></verify>
  <done>All canonical values are frozen and Decimal-safe, all three explicit product contexts exist, and property coverage proves exact canonical Decimal preservation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define legal evidence-driven lifecycle transitions</name>
  <files>pa_agent/trading/domain/lifecycle.py, tests/unit/execution/test_models.py, tests/property/execution/test_decimal_invariants.py</files>
  <behavior>
    - Test 1: legal evidence-driven lifecycle changes are accepted and illegal state/event combinations are rejected.
    - Test 2: local timeout, cancellation, missing stream observation, and malformed acknowledgement cannot establish a terminal order state.
    - Test 3: terminal projections require normalized definitive evidence.
  </behavior>
  <action>Implement one pure transition table and `assert_transition` per D-06. Represent `PROPOSED`, `SUBMITTING`, `SUBMISSION_UNKNOWN`, acknowledged/open/partial/cancel-requested states, and terminal states explicitly. Require normalized external evidence for terminal transitions; translate local interruption conditions only to unresolved/reconciliation-needed state. Keep the guard storage- and gateway-independent so the later ledger and recovery service invoke the same rule.</action>
  <verify><automated>python -m pytest tests/unit/execution/test_models.py tests/property/execution/test_decimal_invariants.py -q</automated></verify>
  <done>The domain cannot locally invent a rejected, cancelled, or filled remote outcome after an ambiguous condition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external/UI numeric input to domain | Untrusted text or binary values become canonical execution values. |
| future adapter evidence to lifecycle | External observations can advance durable order state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | Decimal ingress and product contexts | high | mitigate | Strict parser, frozen values, and discriminated contexts reject invalid precision and product combinations. |
| T-01-02 | Repudiation | lifecycle transition guard | high | mitigate | One pure evidence-driven transition function rejects locally inferred terminal outcomes. |
| T-01-SC | Tampering | package installation | low | accept | No package installation is planned. |
</threat_model>

<verification>Run the task command, then `python -m pytest tests/unit/execution/test_models.py tests/property/execution/test_decimal_invariants.py -q` and `ruff check pa_agent/trading/domain tests/unit/execution/test_models.py tests/property/execution/test_decimal_invariants.py` when Ruff is available.</verification>

<success_criteria>Phase 1 has an isolated, immutable, Decimal-safe domain with product-specific contexts and lifecycle rules that retain ambiguous work as unresolved.</success_criteria>

<output>Create `.planning/phases/01-execution-foundation/01-01-SUMMARY.md` when done.</output>
