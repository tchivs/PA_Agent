---
phase: 02-approval-and-risk-boundary
plan: 24
type: tdd
wave: 9
depends_on: [02-23]
files_modified:
  - pa_agent/trading/application/kill_switch.py
  - pa_agent/trading/ports/ledger.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/integration/execution/test_kill_switch.py
  - tests/property/execution/test_approval_kill_switch_machine.py
autonomous: true
gap_closure: true
requirements: [SAFE-02, SAFE-03, SIM-03]
must_haves:
  truths:
    - "无既有订单且无 active recovery scope 的锁存系统可在显式 begin 和 complete 操作后从 LATCHED 经 RECOVERING 回到 READY。"
    - "零 scope 恢复在每个持久化转换事务中仍验证处理完成的取消工作、无未决订单，并精确证明 active scope 集合为空；它不伪造 RecoveryAssessment 或 assessment ID。"
    - "存在一个或多个 scope 时，恢复仍要求每个 scope 的完整、规范、target/policy-bound、逐项新鲜的 accepted assessment ID；空 ID 集合不能绕过该路径。"
  artifacts:
    - path: pa_agent/trading/persistence/sqlite_ledger.py
      provides: 零 scope 与有 scope 区分的事务内恢复状态机和持久化证明事件
    - path: pa_agent/trading/application/kill_switch.py
      provides: 不伪造 assessment 的显式零 scope begin/complete 编排
    - path: tests/integration/execution/test_kill_switch.py
      provides: 真实 SQLite 零订单 LATCHED 到 RECOVERING 到 READY 回归及有 scope 负向保持测试
  key_links:
    - from: KillSwitchService.begin_recovery
      to: SQLiteExecutionLedger.begin_kill_switch_recovery
      via: 空 scope 传递空 ID 集合；有 scope 只传递服务记录的完整 assessment IDs
      pattern: list_kill_switch_recovery_scopes|begin_kill_switch_recovery|assessment_ids
---

<objective>
关闭 CR-02 的空 recovery scope 永久锁存：定义不伪造 assessment 的零 scope 显式恢复语义，同时保持有 scope 时完整新鲜证据与精确 ID 覆盖的 fail-closed 规则。

Purpose: 操作员在首笔订单前触发 kill switch 后，能够在持久化安全条件证明没有需清算或评估对象时完成可审计的两阶段恢复，而不是永久停留在 LATCHED。
Output: 区分空/非空 active scope 集合的 SQLite 状态机、持久化零 scope 安全证明事件、服务编排与真实 SQLite/state-machine 回归。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-REVIEW.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/02-approval-and-risk-boundary/02-19-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-21-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-22-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-23-SUMMARY.md
@pa_agent/trading/application/kill_switch.py
@pa_agent/trading/application/recovery_assessment.py
@pa_agent/trading/persistence/sqlite_ledger.py
@tests/integration/execution/test_kill_switch.py
@tests/property/execution/test_approval_kill_switch_machine.py

<interfaces>
`latch_kill_switch()` currently snapshots recovery scopes only from `order_commands`; with no order it leaves the active set empty. `KillSwitchService.begin_recovery()` already obtains IDs by iterating the ledger-loaded active scopes, so the normal zero-scope call naturally produces `()`. The current SQLite matcher rejects that tuple before checking whether its active scope set is empty.

Retain the existing RecoveryAssessmentService and RecoveryEvidence contract for every nonempty scope. The new semantics are exact: `assessment_ids == ()` is admissible only when the transaction reads an empty active scope set and `_recovery_facts_are_clear_in_transaction()` proves no queued work and no unresolved order. Begin and complete remain distinct actor-labelled transactions. Each successful zero-scope transition must append the existing durable kill-switch event with `recovery_assessment_ids_json` as the canonical empty array and a controlled zero-scope clearance reason; this is the persistent safety proof. An empty tuple with any active scope, duplicate IDs, forged IDs, stale evidence, rejected evidence, or mismatched scope evidence remains rejected.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify zero-scope recovery and nonempty-scope preservation regressions</name>
  <files>tests/integration/execution/test_kill_switch.py, tests/property/execution/test_approval_kill_switch_machine.py</files>
  <behavior>
    - A fresh real SQLite ledger with no order can latch, explicitly begin recovery with no assessment IDs, explicitly complete recovery with no assessment IDs, and observes LATCHED then RECOVERING then READY.
    - Both zero-scope transitions persist an audit event containing the canonical empty assessment-ID set and the controlled zero-scope clearance reason; no recovery assessment, ticket, command, claim, dispatch attempt, or gateway submission is created.
    - A nonempty active scope still rejects an empty ID tuple and reaches READY only through the existing complete fresh RecoveryAssessmentService evidence and exact-ID checks.
    - Pending cancellation work or unresolved lifecycle state prevents zero-scope recovery even if a caller passes an empty ID tuple.
  </behavior>
  <action>Write RED real-SQLite regressions for CR-02 in the existing kill-switch module. Start with no `order_commands`, latch through KillSwitchService, assert `list_kill_switch_recovery_scopes()` is empty, then call explicit begin and complete recovery in sequence using the empty tuple required by the API. Inspect the durable kill-switch event rows to prove each successful transition records canonical `[]`, the controlled zero-scope clearance reason, actor label, and state transition; assert recovery assessment count and all authorization row counts remain unchanged and the recording gateway submit count is zero. Add negative coverage that an active scope created through the normal approved lifecycle rejects the empty tuple and still requires current complete RecoveryAssessmentService evidence, including a stale/forged ID case. Add a state-machine action for latching before any order and require that only the explicit two-action zero-scope path can return it to READY. Per D-05, D-06, D-07, D-08, D-11, and D-12, do not synthesize an assessment, relax freshness, skip durable audit, or fold begin and complete into one action.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_kill_switch.py tests/property/execution/test_approval_kill_switch_machine.py</automated></verify>
  <done>RED regressions prove the no-order path can recover only through two explicit audited transitions with no fabricated assessment, while every nonempty-scope evidence gate remains required.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement audited zero-scope recovery without weakening scoped clearance</name>
  <files>pa_agent/trading/application/kill_switch.py, pa_agent/trading/ports/ledger.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_kill_switch.py, tests/property/execution/test_approval_kill_switch_machine.py</files>
  <behavior>
    - The SQLite recovery matcher loads active scopes first and accepts `()` only when that durable set is empty; it retains exact accepted, fresh, canonical assessment matching for every nonempty set.
    - Begin and complete independently check processed cancellation work and terminal lifecycle evidence, then persist an explicit zero-scope proof in the existing audit event before their state transition commits.
    - KillSwitchService does not create a RecoveryAssessment, invoke recovery evidence collection, or expose an alternate recovery authority when the ledger reports zero scopes.
  </behavior>
  <action>Implement the CR-02 state-machine branch at the SQLite boundary. Refactor `_recovery_assessments_match_current_scopes_in_transaction()` to query active scopes before imposing nonempty IDs: permit only the exact pair of an empty active scope set and `assessment_ids == ()`; for any nonempty active set retain the current unique, exact one-to-one ID coverage plus accepted status, scope/target/policy/evidence bindings, RecoveryEvidence canonical validation, and per-observation fixed freshness checks. Keep `_recovery_facts_are_clear_in_transaction()` mandatory in both begin and complete, so an empty ID list cannot bypass queued cancellation work or unresolved orders. Extend the existing transition event recording interface only as needed to persist a controlled zero-scope clearance reason together with canonical empty assessment IDs for both actor-labelled transitions; make this audit write occur in the same immediate transaction as its state update. In KillSwitchService, preserve the current loop over ledger-owned scopes and pass its naturally empty tuple through; do not invent a RecoveryAssessment, recovery assessment ID, raw evidence JSON, ticket, command, claim, permit, OutboundSubmission, or gateway action. Update the port documentation to state the exact empty-set contract. Make Task 1 pass and retain all scoped recovery regressions. This implements D-05 through D-08 and D-11/D-12, while preserving D-01 through D-04's no-advisory-submission rule and D-09/D-10 ticket behavior.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_kill_switch.py tests/property/execution/test_approval_kill_switch_machine.py &amp;&amp; .venv/bin/pytest -q tests/unit/execution tests/integration/execution tests/property/execution &amp;&amp; .venv/bin/ruff check pa_agent/trading/application/kill_switch.py pa_agent/trading/ports/ledger.py pa_agent/trading/persistence/sqlite_ledger.py tests/integration/execution/test_kill_switch.py tests/property/execution/test_approval_kill_switch_machine.py</automated></verify>
  <done>A no-order real SQLite latch can explicitly transition LATCHED to RECOVERING to READY with durable zero-scope proof events and no fabricated assessment or gateway work; any active scope still requires exact complete fresh assessment IDs and rejects the empty tuple.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | - | Operator recovery is deliberate, reconciled, and cannot create a submission bypass | 02-24 | COVERED | Provides the missing zero-scope recovery route while preserving the scoped evidence boundary.
REQ | SAFE-02 | Current product-aware evidence controls precede authorization | 02-24 | COVERED | Nonempty scopes retain complete target-bound canonical evidence and freshness; zero scope proves there is no object requiring that evidence.
REQ | SAFE-03 | Persistent latched kill switch requires deliberate recovery | 02-24 | COVERED | Two actor-labelled SQLite transitions, durable clearance proof, work/order checks, and restart-safe state enforce recovery.
REQ | SIM-03 | Recovery state and audit facts are durable and queryable | 02-24 | COVERED | Transition events retain canonical empty IDs and controlled zero-scope reason without minting authority.
REQ | CORE-03, SAFE-01, SAFE-04, SAFE-05 | Advisory conversion, Paper boundary, approval, credential isolation | Prior plans 02-01 to 02-23 | PRESERVED | Recovery remains incapable of ticket, command, permit, outbound, or gateway creation.
RESEARCH | - | SQLite immediate transactions, canonical values, full fresh evidence, explicit recovery, no package additions | 02-24 | COVERED | Uses existing transaction/event model and real offline SQLite fake-gateway tests.
CONTEXT | D-01-D-04 | Immutable advisory conversion and no advisory submission capability | Prior plans 02-01 to 02-23 | PRESERVED | The new branch creates no submission-capable object.
CONTEXT | D-05-D-08 | Complete fresh selected-target evidence and fixed policy controls | 02-24 | COVERED | Scoped paths remain exact; empty scope is accepted only after transaction-time proof that no scope exists.
CONTEXT | D-09-D-10 | Complete approval review and binding invalidation | Prior plans 02-08 to 02-23 | PRESERVED | No ticket or ticket-binding behavior changes.
CONTEXT | D-11-D-12 | Atomic single authorization and durable terminal events | 02-24 | COVERED | Begin and complete remain separate immediate durable transitions with explicit actor and event evidence.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KillSwitchService -> recovery transition | Caller intent cannot turn an empty ID list into recovery unless SQLite proves active scopes are empty and durable work is clear. |
| recovery assessment IDs -> SQLite state machine | IDs remain untrusted until exact current active scope coverage, canonical evidence, bindings, acceptance, and freshness are checked. |
| durable recovery state -> future authorization | Recovery transitions must not issue tickets, commands, claims, permits, outbound values, or gateway calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-56 | Denial of Service | zero-scope kill-switch recovery | high | mitigate | Permit empty IDs only for a transaction-loaded empty scope set after work/order clearance, with explicit two-step actor audit events. |
| T-02-57 | Elevation/Tampering | empty assessment-ID tuple | critical | mitigate | Reject the empty tuple whenever one or more active scopes exist; retain exact full-evidence ID validation for scoped recovery. |
| T-02-58 | Repudiation | zero-scope clearance | high | mitigate | Persist the canonical empty ID set, controlled reason, actor, timestamp, and transition in the same SQLite transaction. |
| T-02-59 | Elevation | recovery-to-submission boundary | critical | mitigate | Preserve recovery's separation from tickets, commands, claims, permits, outbound values, and gateway calls; regressions assert zero authority artifacts. |
| T-02-SC | Tampering | package installs | low | accept | No package installation is planned; existing Python, SQLite, Pytest, Hypothesis, and Ruff tooling is used. |
</threat_model>

<verification>
Run the real-SQLite and state-machine recovery suites after RED and GREEN work. Completion requires the no-order `LATCHED -> RECOVERING -> READY` proof, inspection of both durable zero-scope events, zero authorization/gateway side effects, the nonempty-scope empty-ID rejection, the full offline execution corpus, and scoped Ruff.
</verification>

<success_criteria>
CR-02 is closed only when a real SQLite ledger with no orders can perform two explicit actor-labelled recovery transitions from LATCHED through RECOVERING to READY, with each transition recording canonical empty assessment IDs and a controlled zero-scope proof after durable work/order clearance. The implementation must allocate no assessment or outbound authority in this case, and it must leave the existing nonempty-scope requirement for complete current accepted assessment IDs fully intact.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-24-SUMMARY.md` when done.
</output>
