---
phase: 02-approval-and-risk-boundary
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - pa_agent/trading/ports/ledger.py
  - pa_agent/trading/application/proposal.py
  - pa_agent/trading/persistence/migrations.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/integration/execution/test_intent_rejections.py
  - tests/integration/execution/test_approval_audit_ledger.py
autonomous: true
requirements: [SIM-03, SAFE-04]
must_haves:
  truths:
    - "每个提议、转换或风险拒绝、证据、评估和票据终止状态都会以受控、可查询的审计事实持久化。"
  artifacts:
    - path: pa_agent/trading/application/proposal.py
      provides: 将候选、受控拒绝、证据和风险评估持久化为审批前审计记录
    - path: pa_agent/trading/persistence/sqlite_ledger.py
      provides: 提议、受控拒绝、证据与评估的受控 SQLite 持久化
  key_links:
    - from: pa_agent/trading/domain/risk.py
      to: pa_agent/trading/domain/approval.py
      via: FeeEstimate 的目标、产品、标的、报价、费用率版本和 Decimal 金额哈希绑定
      pattern: FeeEstimate|fee_rate_version|quote_identifier
---

<objective>
交付先于票据生命周期的受控提议、拒绝、证据与风险审计存储。

Purpose: 使后续自动建票和单次消费只能建立在完整、可复核的持久化事实之上。
Output: 提议/风险审计迁移和端口、受控审计查询以及 SQLite 重开测试；票据生命周期由 02-08 负责，原子消费由 02-07 负责。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-VALIDATION.md
@.planning/phases/02-approval-and-risk-boundary/02-04-SUMMARY.md
@.planning/phases/01-execution-foundation/01-07-SUMMARY.md

<interfaces>
From Plan 02-04: candidates, policy, source snapshot, and a fresh evidence assessment are frozen/digest-bound canonical values; this plan records their controlled audit rows before issuing a ticket.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify durable proposal, rejection, evidence, and assessment audit behavior</name>
  <files>tests/integration/execution/test_intent_rejections.py, tests/integration/execution/test_approval_audit_ledger.py</files>
  <read_first>Read `pa_agent/trading/ports/ledger.py`, `pa_agent/trading/persistence/sqlite_ledger.py`, `pa_agent/trading/application/submission.py`, `tests/integration/execution/test_idempotency_recovery.py`, and `02-VALIDATION.md` rows for SIM-03 and SAFE-04.</read_first>
  <behavior>
    - Conversion rejection, evidence rejection, risk rejection, accepted candidate, complete evidence bundle, Decimal fee estimate, and risk assessment persist as independently queryable controlled audit facts.
    - Every persisted fact carries the applicable stable source ID, reason code or result, source/policy/evidence digest, observed/recorded time, and allowlisted summary; raw gateway exception data and secrets are absent.
    - Reopening the SQLite ledger preserves the audit chain and its foreign-key relationships before any ticket can be created.
  </behavior>
  <action>Write Wave 0 real-SQLite integration tests first for D-02 conversion rejections, D-07 evidence/risk rejections, accepted candidates, complete evidence, fee estimates, and SIM-03 audit records after reopen. Assert that each row retains stable source metadata, canonical source/policy/evidence digests, fixed-point Decimal fee facts where present, and only an allowlisted reason summary for a synthetic gateway failure. Keep every fake offline, record attempts, and assert this pre-ticket audit path neither creates an `OutboundSubmission` nor calls a gateway.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_intent_rejections.py tests/integration/execution/test_approval_audit_ledger.py</automated></verify>
  <acceptance_criteria>The tests fail before implementation and demonstrate durable, queryable, redacted conversion/evidence/risk audit facts after SQLite reopen with no ticket or outbound side effect.</acceptance_criteria>
  <done>Wave 0 captures the complete pre-ticket audit chain and proves it remains separate from command admission and gateway invocation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement proposal audit migration and ledger-port contract</name>
  <files>pa_agent/trading/ports/ledger.py, pa_agent/trading/application/proposal.py, pa_agent/trading/persistence/migrations.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_intent_rejections.py, tests/integration/execution/test_approval_audit_ledger.py</files>
  <read_first>Read Task 1's integration tests, `pa_agent/trading/persistence/migrations.py`, `pa_agent/trading/persistence/sqlite_ledger.py`, `pa_agent/trading/ports/ledger.py`, and `02-PATTERNS.md` sections “SQLite migration, append-only audit, and atomic consumption” and “Approval, kill switch, and only outbound admission”.</read_first>
  <behavior>
    - The proposal coordinator writes allowlisted candidate, conversion/evidence rejection, evidence, and risk-assessment summaries with source/policy/evidence digests before any ticket is issued.
    - The ledger port exposes only audit and ticket-lifecycle persistence contracts in this task; it does not expose a ticket-consumption or outbound-admission operation.
  </behavior>
  <action>Implement `ProposalService` as the sole ledger-owning coordinator around `IntentFactory`, `FreshEvidenceCollector`, and `RiskEngine`: persist D-02 conversion rejections and D-07 evidence/risk rejections with source metadata, stable reason code, and shared redaction/allowlist summaries; persist accepted candidates, complete evidence, fee estimate, and assessments using canonical JSON, SHA-256 digests, foreign keys, and query indexes. Add one ascending SQLite migration for proposals, controlled decision/audit events, evidence, assessments, and their indexes/foreign keys. Extend `ExecutionLedger` and `SQLiteExecutionLedger` only with focused pre-ticket audit repository operations; 02-08 adds ticket repository operations. Do not expose ticket consumption, command admission, `begin_outbound_submission`, an `OutboundSubmission`, or a gateway call in this task. Per D-02 and SIM-03, preserve reason codes and source/policy/evidence hashes while excluding raw gateway failures and secrets.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_intent_rejections.py tests/integration/execution/test_approval_audit_ledger.py &amp;&amp; .venv/bin/ruff check pa_agent/trading/ports/ledger.py pa_agent/trading/application/proposal.py pa_agent/trading/persistence/migrations.py pa_agent/trading/persistence/sqlite_ledger.py tests/integration/execution/test_intent_rejections.py tests/integration/execution/test_approval_audit_ledger.py</automated></verify>
  <acceptance_criteria>All proposal-audit tests pass after reopen; every pre-ticket outcome is durable and redacted, the schema is migration-safe, and the port has no ticket-consumption or outbound-authority method.</acceptance_criteria>
  <done>Proposal audit, controlled evidence/assessment storage, and the narrow persistence contract are durable before 02-08 implements ticket behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proposal/risk result -> approval ledger | Only controlled canonical summaries and hashes become durable audit records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Repudiation | proposal and risk audit | medium | mitigate | Append-only controlled source, policy, evidence, fee, and rejection facts survive SQLite reopen under D-02, D-07, and SIM-03. |
| T-02-08 | Information Disclosure | gateway failure audit | high | mitigate | Persist only allowlisted canonical summaries and digests, with synthetic-failure tests proving raw exception data and secrets do not cross the ledger boundary. |
| T-02-SC | Tampering | package installs | low | accept | No dependency installation is part of this plan. |
</threat_model>

<verification>
Run the focused audit integration tests during execution. Plan 02-08 adds ticket issuance and lifecycle; 02-07 performs the atomic-consumption regression before the final kill-switch plan.
</verification>

<success_criteria>
Before a ticket can be issued, its proposal, current evidence, and risk result are durable, reproducible, and redacted; 02-08 owns review facts and terminal ticket states.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-05-SUMMARY.md` when done.
</output>
