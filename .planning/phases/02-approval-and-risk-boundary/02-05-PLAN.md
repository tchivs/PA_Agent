---
phase: 02-approval-and-risk-boundary
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - pa_agent/trading/domain/approval.py
  - pa_agent/trading/domain/errors.py
  - pa_agent/trading/ports/ledger.py
  - pa_agent/trading/application/proposal.py
  - pa_agent/trading/application/approval.py
  - pa_agent/trading/persistence/migrations.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/unit/execution/test_approval_ticket.py
  - tests/integration/execution/test_intent_rejections.py
  - tests/integration/execution/test_approval_audit_ledger.py
autonomous: true
requirements: [SIM-03, SAFE-04]
must_haves:
  truths:
    - "操作员可读取一张包含完整执行摘要、来源、风险和数据年龄的单次、固定 60 秒 `phase2-v1` 审批票据。"
    - "票据的估算费用由目标、产品、标的、报价标识和费用率版本绑定的 Decimal 计算得出；费用证据缺失、过期或不匹配时不能建票或批准。"
    - "任意绑定输入或证据变化、过期或重复操作都会使旧票据不可消费。"
    - "每个提议、转换或风险拒绝、证据、评估和票据终止状态都会以受控、可查询的审计事实持久化。"
  artifacts:
    - path: pa_agent/trading/application/approval.py
      provides: 票据创建、拒绝、失效、刷新和单次批准协调
    - path: pa_agent/trading/application/proposal.py
      provides: 将候选、受控拒绝、证据和风险评估持久化为审批前审计记录
    - path: pa_agent/trading/persistence/sqlite_ledger.py
      provides: 提议/审计和票据生命周期的受控 SQLite 持久化
  key_links:
    - from: pa_agent/trading/domain/risk.py
      to: pa_agent/trading/domain/approval.py
      via: FeeEstimate 的目标、产品、标的、报价、费用率版本和 Decimal 金额哈希绑定
      pattern: FeeEstimate|fee_rate_version|quote_identifier
---

<objective>
交付可审阅、短时有效、不可变绑定的审批票据，以及先于批准的受控提议、拒绝、证据与风险审计存储。

Purpose: 使后续单次消费只能建立在完整的持久化审计事实和可验证票据生命周期之上。
Output: 提议/风险审计迁移和端口、完整票据/审阅摘要、审批协调器及失效测试；原子消费在 02-07 负责。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-VALIDATION.md
@.planning/phases/02-approval-and-risk-boundary/02-04-SUMMARY.md
@.planning/phases/01-execution-foundation/01-07-SUMMARY.md

<interfaces>
From Plan 02-04: candidates, policy, source snapshot, and a fresh evidence assessment are frozen/digest-bound canonical values; this plan records their controlled audit rows before issuing a ticket.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify approval-ticket content, invalidation, and audit lifecycle</name>
  <files>tests/unit/execution/test_approval_ticket.py, tests/integration/execution/test_intent_rejections.py, tests/integration/execution/test_approval_audit_ledger.py</files>
  <read_first>Read `pa_agent/trading/ports/ledger.py`, `pa_agent/trading/persistence/sqlite_ledger.py`, `pa_agent/trading/application/submission.py`, `tests/integration/execution/test_idempotency_recovery.py`, and `02-VALIDATION.md` rows for SIM-03 and SAFE-04.</read_first>
  <behavior>
    - Ticket review summaries include venue, environment, account, product, symbol, side, amount, expected price/slippage, a Decimal estimated fee with fee currency/rate version/quote identifier, leverage/borrow/position context, data age, analysis provenance, and complete risk result.
    - A missing, stale, or target/product/symbol/quote-mismatched fee rate creates no ticket and cannot be approved; mutating any fee input changes the evidence, assessment, and ticket bindings.
    - Tickets have the resolved fixed 60-second `phase2-v1` TTL; source, command field, target, policy, evidence, quote, or data-age changes require a new conversion/evidence/risk cycle.
    - Operator rejection, expiry, and binding invalidation are distinct durable terminal events with reason/time/snapshot facts; no terminal ticket creates an outbound authorization.
  </behavior>
  <action>Write Wave 0 unit tests for D-09 review completeness including the bound Decimal fee estimate, the resolved 60-second `phase2-v1` TTL, D-10 hash invalidation including fee-rate/quote mutations, and D-12 distinct operator-rejected/expired/invalidated events. Add tests that missing, stale, or target/product/symbol/quote-mismatched fee evidence prevents ticket creation and approval with no submit attempt. Write real SQLite integration tests for D-02 conversion rejections, D-07 evidence/risk rejections, and SIM-03 controlled proposal/assessment audit records after reopen. Assert a raw synthetic gateway failure is represented only by an allowlisted summary and reason code; test fakes record any gateway attempt and never use real network access.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_approval_ticket.py tests/integration/execution/test_intent_rejections.py tests/integration/execution/test_approval_audit_ledger.py</automated></verify>
  <acceptance_criteria>The tests fail before implementation and demonstrate auditable conversion/evidence outcomes, complete fee disclosure/binding, exact ticket expiry, and durable terminal events without a network dependency.</acceptance_criteria>
  <done>Wave 0 captures controlled pre-approval audit persistence, complete fee disclosure/binding, the fixed 60-second expiry, and all non-consumption invalidation paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement proposal audit migration and ledger-port contract</name>
  <files>pa_agent/trading/ports/ledger.py, pa_agent/trading/application/proposal.py, pa_agent/trading/persistence/migrations.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_intent_rejections.py, tests/integration/execution/test_approval_audit_ledger.py</files>
  <read_first>Read Task 1's integration tests, `pa_agent/trading/persistence/migrations.py`, `pa_agent/trading/persistence/sqlite_ledger.py`, `pa_agent/trading/ports/ledger.py`, and `02-PATTERNS.md` sections “SQLite migration, append-only audit, and atomic consumption” and “Approval, kill switch, and only outbound admission”.</read_first>
  <behavior>
    - The proposal coordinator writes allowlisted candidate, conversion/evidence rejection, evidence, and risk-assessment summaries with source/policy/evidence digests before any ticket is issued.
    - The ledger port exposes only audit and ticket-lifecycle persistence contracts in this task; it does not expose a ticket-consumption or outbound-admission operation.
  </behavior>
  <action>Implement `ProposalService` as the sole ledger-owning coordinator around `IntentFactory`, `FreshEvidenceCollector`, and `RiskEngine`: persist D-02 conversion rejections and D-07 evidence/risk rejections with source metadata, stable reason code, and shared redaction/allowlist summaries; persist accepted candidates, complete evidence, fee estimate, and assessments using canonical JSON, SHA-256 digests, foreign keys, and query indexes. Add one ascending SQLite migration for proposals, controlled decision/audit events, evidence, assessments, approval-ticket storage, terminal-ticket-event storage, and their indexes/foreign keys. Extend `ExecutionLedger` and `SQLiteExecutionLedger` with focused audit and ticket-lifecycle repository operations, but do not expose ticket consumption, command admission, `begin_outbound_submission`, or a gateway call in this task. Per D-02 and SIM-03, preserve reason codes and source/policy/evidence hashes while excluding raw gateway failures and secrets.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_intent_rejections.py tests/integration/execution/test_approval_audit_ledger.py &amp;&amp; .venv/bin/ruff check pa_agent/trading/ports/ledger.py pa_agent/trading/application/proposal.py pa_agent/trading/persistence/migrations.py pa_agent/trading/persistence/sqlite_ledger.py tests/integration/execution/test_intent_rejections.py tests/integration/execution/test_approval_audit_ledger.py</automated></verify>
  <acceptance_criteria>All proposal-audit tests pass after reopen; every pre-approval outcome is durable and redacted, the schema is migration-safe, and the port has no ticket-consumption or outbound-authority method yet.</acceptance_criteria>
  <done>Proposal audit, controlled evidence/assessment storage, and the narrow persistence contract are durable before ticket behavior is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement ticket domain and ApprovalService lifecycle</name>
  <files>pa_agent/trading/domain/approval.py, pa_agent/trading/domain/errors.py, pa_agent/trading/application/approval.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/unit/execution/test_approval_ticket.py</files>
  <read_first>Read Task 1's unit tests, Task 2's ledger contract, `pa_agent/trading/domain/risk.py`, `pa_agent/trading/application/evidence_collector.py`, `pa_agent/trading/application/risk_engine.py`, and `02-PATTERNS.md` section “Approval, kill switch, and only outbound admission”.</read_first>
  <behavior>
    - `ApprovalTicket` review summaries include the complete D-09 facts, with canonical source/policy/evidence/command hashes and target/product/symbol/quote/rate-version/Decimal fee-estimate binding.
    - The approval lifecycle creates, explicitly rejects, expires, or invalidates tickets with durable terminal events; fresh evidence and risk are recomputed before a later consumption boundary is eligible.
  </behavior>
  <action>Implement frozen `ApprovalTicket`, review summary, ticket status, and terminal-event values with canonical source/policy/evidence/command hashes plus target/product/symbol/quote/rate-version/Decimal fee-estimate binding, the fixed 60-second TTL, and `policy_version = "phase2-v1"`. Implement ticket creation, explicit operator rejection, expiration/invalidation recording, and approval precheck that refreshes evidence, re-runs risk, and rejects changed or unavailable fee evidence under D-06 and D-10. Persist lifecycle transitions through Task 2's repository contract. This task prepares only a current pending ticket for the next plan: it must not consume a ticket, create a command admission, begin outbound submission, or invoke a gateway.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_approval_ticket.py &amp;&amp; .venv/bin/ruff check pa_agent/trading/domain/approval.py pa_agent/trading/domain/errors.py pa_agent/trading/application/approval.py pa_agent/trading/persistence/sqlite_ledger.py tests/unit/execution/test_approval_ticket.py</automated></verify>
  <acceptance_criteria>All ticket tests pass; each terminal event is durable and distinct, every changed, stale, or fee-unverifiable ticket is non-current, and no ticket-lifecycle method creates outbound authority.</acceptance_criteria>
  <done>Per D-09, D-10, and D-12, tickets are reviewable, fee-verifiable, fixed-expiry, invalidatable, and durable before 02-07 adds atomic admission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proposal/risk result -> approval ledger | Only controlled canonical summaries and hashes become durable audit records. |
| operator action -> ticket lifecycle | A ticket cannot become current unless its complete review, fixed expiry, and fresh risk bindings are durable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | stale/replayed approval evidence | high | mitigate | Fresh evidence/risk re-evaluation, fixed TTL, immutable binding hashes, and D-10 invalidation tests block stale approvals. |
| T-02-11 | Repudiation | operator terminal actions | medium | mitigate | Distinct append-only terminal events preserve reason, timestamp, actor label, and binding snapshot under D-12. |
| T-02-13 | Tampering | fee estimate and review summary | high | mitigate | The target/product/symbol/quote/rate-version-bound Decimal fee estimate is hashed into evidence, assessment, and ticket; missing, stale, or mismatched rates reject before ticket issue or consumption. |
| T-02-SC | Tampering | package installs | low | accept | No dependency installation is part of this plan. |
</threat_model>

<verification>
Run the focused audit and ticket tests during execution. Plan 02-07 performs the atomic-consumption regression before the Wave 5 kill-switch plan.
</verification>

<success_criteria>
Before any approval can become an outbound authorization, its proposal, current evidence, risk result, complete review facts, fixed expiry, and terminal state are durable and reproducible; every stale, changed, fee-unverifiable, expired, or rejected ticket is non-submit-capable.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-05-SUMMARY.md` when done.
</output>
