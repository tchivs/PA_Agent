---
phase: 03-paper-product-core
plan: 13
type: tdd
wave: 2
depends_on: [03-03]
files_modified:
  - pa_agent/trading/domain/risk.py
  - pa_agent/trading/domain/approval.py
  - pa_agent/trading/application/approval.py
  - pa_agent/trading/persistence/migrations.py
  - pa_agent/trading/persistence/sqlite_ledger.py
  - tests/unit/execution/test_paper_product_policy_ticket.py
  - tests/integration/execution/test_paper_product_policy_ticket.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "Every new Paper Spot, isolated-margin, and USDT-perpetual ticket binds an immutable exact-target product policy ID, version, digest, and Decimal product limit before it can create a permit."
    - "Unsupported target/product/mode combinations fail before accepted assessment, pending ticket, permit, lease, or gateway call; legacy Phase 2 Paper Spot policy and ticket rows remain readable."
    - "SQLite ticket reconstruction validates the durable policy identity against the durable canonical product context rather than deriving a fixed Spot target or consulting mutable settings."
  artifacts:
    - path: pa_agent/trading/domain/risk.py
      provides: versioned immutable three-product Paper policy catalog and a legacy-compatible selector
    - path: pa_agent/trading/domain/approval.py
      provides: product-agnostic immutable ticket binding and authorization-equivalence validation
    - path: pa_agent/trading/persistence/sqlite_ledger.py
      provides: durable product-policy ticket persistence and reconstruction validation
    - path: tests/integration/execution/test_paper_product_policy_ticket.py
      provides: real-SQLite three-product ticket/permit and legacy-Spot compatibility regressions
  key_links:
    - from: selected immutable product policy
      to: candidate, assessment, ticket binding, and permit lease
      via: exact durable policy ID/version/digest and canonical product-context digest
    - from: durable ticket row
      to: policy selection and command reconstruction
      via: target/context-derived selector with legacy Phase 2 Spot decoding only
---

<objective>
Cut over the existing fixed-Spot risk and ticket boundary to an ordered, durable three-product Paper policy catalog before margin or perpetual admission/accounting can execute.

Purpose: Phase 2 intentionally fixed every ticket path to one Paper Spot policy. SIM-01 requires real target-bound Spot, isolated-margin, and USDT-perpetual policy contracts, while preserving historical Spot rows and the protected dispatch chain.
Output: Immutable three-product policy selection, product-aware ticket binding/issuance/reconstruction, migration, and real-SQLite authority-boundary coverage.
</objective>

<execution_context>
@/root/.omp/agent/gsd-core/workflows/execute-plan.md
@/root/.omp/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-paper-product-core/03-CONTEXT.md
@.planning/phases/03-paper-product-core/03-RESEARCH.md
@.planning/phases/03-paper-product-core/03-03-PLAN.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@pa_agent/trading/domain/risk.py
@pa_agent/trading/domain/approval.py
@pa_agent/trading/application/approval.py
@pa_agent/trading/persistence/migrations.py
@pa_agent/trading/persistence/sqlite_ledger.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify three-product policy selection and ticket authority cutover</name>
  <files>tests/unit/execution/test_paper_product_policy_ticket.py, tests/integration/execution/test_paper_product_policy_ticket.py</files>
  <behavior>
    - The immutable Paper policy catalog has exactly `paper-spot-primary` Spot, `paper-margin-isolated-primary` isolated-margin, and `paper-usdt-perpetual-primary` USDT-perpetual targets. Each policy has a distinct stable policy ID/version/digest and only its own Decimal fee, slippage, leverage, exposure, margin-health, protective-exit, or product limit fields.
    - The selector accepts only an exact Paper target/account/product combination and rejects unsupported target, mode, account, product, cross/portfolio margin, hedge, or unsafe perpetual context before an accepted assessment, pending ticket, permit, lease, command, or gateway call can occur.
    - Ticket creation, authorization-equivalence refresh, invalidation, and construction accept the selected immutable product policy rather than a fixed policy version. Every binding retains exact policy ID/version/digest and canonical product-context digest.
    - A real SQLite reopened ledger issues and consumes one valid ticket/permit for each of Spot, isolated margin, and perpetual through the existing chain. Pre-Phase-03 Phase 2 Spot policy/ticket rows decode, display, invalidate, and reconstruct under the legacy Spot policy; they neither acquire a new product context nor become margin/perpetual tickets.
  </behavior>
  <action>Write RED unit and real-SQLite integration tests first. Define fixtures for the three fixed Paper targets and prove their policy identities, immutable Decimal limits, and candidate/context binding differ. Exercise valid three-product ticket issuance and the protected permit/lease path with a gateway sentinel; exercise unsupported target or mode/context inputs before authority and assert zero accepted assessments, tickets, permits, leases, submissions, or Paper mutations. Cover legacy `phase2-v1` Spot ticket-row reads and current legacy selector calls, including reopen and binding invalidation. Do not manufacture authority with caller-built tickets, direct ledger rows, or a gateway command; use the ordinary persisted candidate and approval path.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_product_policy_ticket.py tests/integration/execution/test_paper_product_policy_ticket.py tests/integration/execution/test_approval_consumption.py</automated></verify>
  <done>Tests fail because the current policy and ticket code accept only the fixed Phase 2 Paper Spot policy and cannot reconstruct or issue three product-bound tickets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace fixed-Spot policy and ticket guards with exact product-policy binding</name>
  <files>pa_agent/trading/domain/risk.py, pa_agent/trading/domain/approval.py, pa_agent/trading/application/approval.py, tests/unit/execution/test_paper_product_policy_ticket.py</files>
  <behavior>
    - The policy selector is target-bound and backwards-compatible: existing Phase 2 Spot callers resolve the legacy Spot policy unchanged, while new selectors resolve only the immutable Phase 3 Spot, isolated-margin, or perpetual policy matching the canonical context.
    - `TicketBinding.from_persisted_facts`, authorization-equivalence checks, `ApprovalTicket.create`, and `ApprovalService.issue_ticket` validate an exact policy object and candidate context; they no longer rely on a Spot-only literal policy check.
    - Margin leverage/risk limits and perpetual leverage, isolated-one-way, maintenance, and protective-exit limits come from their selected immutable product policy; leverage is never a generic field.
  </behavior>
  <action>Replace the fixed-Spot guards in `domain/risk.py`, `domain/approval.py`, and `application/approval.py` with one immutable target/context-aware Paper policy catalog and selector. Preserve the existing `select_phase2_policy` public surface as a legacy Spot-only compatibility wrapper, but make all new candidate/ticket paths select and persist the exact policy ID/version/digest for the canonical product context. Require ticket construction, issuance, review refresh, and invalidation to compare policy identity/digest plus target/product context rather than a hard-coded version. Give isolated margin only its exact pair/risk limits and USDT perpetual only its isolated-one-way, bounded-leverage, maintenance, and protective-exit limits; reject unsupported target/mode/context before requesting ticket persistence. Preserve Decimal-only arithmetic, frozen values, and the sole permit-to-lease-to-coordinator submission route; do not add mutable policy settings, network/UI code, a generic leverage attribute, or a policy fallback to Spot.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_product_policy_ticket.py tests/unit/execution/test_models.py tests/unit/execution/test_risk_engine.py</automated></verify>
  <done>Policy and ticket APIs select exactly one immutable Paper product policy, preserve legacy Spot callers, and reject every incompatible product before ticket authority exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist and reconstruct product-policy-bound tickets through SQLite</name>
  <files>pa_agent/trading/persistence/migrations.py, pa_agent/trading/persistence/sqlite_ledger.py, tests/integration/execution/test_paper_product_policy_ticket.py</files>
  <behavior>
    - Forward migration adds durable policy ID/version/digest and canonical product-context binding wherever candidate, ticket binding, ticket event, permit, or leased command reconstruction needs them, without rewriting earlier migration history.
    - SQLite derives target/policy solely from persisted canonical candidate/ticket context, validates the selected immutable policy before ticket creation, permit consumption, and command reconstruction, and rejects tampered policy/context combinations before authority.
    - Pre-Phase-03 Spot rows reconstruct with the legacy decoder and historical policy values; non-Spot records never use legacy Spot derivation.
  </behavior>
  <action>Append an idempotent forward migration and update `sqlite_ledger.py` persistence/reconstruction paths for candidate, ticket, ticket event, permit, and leased command data. Persist exact selected policy ID/version/digest beside the canonical context/digest, validate them through the product-aware selector inside the same SQLite transactions, and reject mismatch, unknown policy, unsupported target/mode, or a legacy decoder requested for non-Spot data before any ticket/permit/lease side effect. Update persisted ticket reconstruction to build `TicketBinding` from durable canonical context and policy values only; do not derive `paper-spot-primary`, consult current settings, or accept caller replacement values. Retain the existing Phase 2 Spot decoder for historical rows and prove it cannot widen their scope. Leave migration bootstrap and the protected dispatch semantics unchanged.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/integration/execution/test_paper_product_policy_ticket.py tests/integration/execution/test_paper_product_migration.py tests/integration/execution/test_approval_consumption.py tests/integration/execution/test_uncertain_recovery.py</automated></verify>
  <done>Reopened SQLite rows reconstruct only their durable target-bound policy/context, valid three-product tickets can reach the existing permit path once, and unsupported or forged product-policy data creates no authority.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | — | Complete safe Paper Spot, margin, and perpetual lifecycle | 03-13 | COVERED | Three immutable product policies and ticket authority precede product flows.
REQ | SIM-01 | Configurable product limits and restart-safe Paper semantics | 03-13 | COVERED | Durable policy/context reconstruction survives reopen for all products.
RESEARCH | Fixed Spot policy gap | 03-13 | COVERED | Replaces fixed-Spot guards with target-bound policy selection and legacy decoding.
CONTEXT | D-06 | Isolated margin pair/risk limits | 03-13 | COVERED | Margin policy binds exact pair context and rejects cross/portfolio modes before authority.
CONTEXT | D-07 | Isolated one-way bounded perpetual leverage | 03-13 | COVERED | Perpetual policy binds limits and protective-exit requirements before authority.
CONTEXT | D-10/D-12 | Durable independent recovery and no resubmit | 03-13 | COVERED | Persisted ticket reconstruction is durable-only and preserves the one dispatch route.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| candidate/context -> policy selection | Untrusted or incompatible target/context must not select a broader product policy. |
| assessment -> ticket/permit | Ticket authority must bind the exact immutable policy and durable context. |
| SQLite row -> reconstruction | Only durable policy/context data may rebuild ticket or command review facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-44 | Elevation | product policy selector | critical | mitigate | Exact target/account/product/context selection and unsupported-input zero-authority tests. |
| T-03-45 | Tampering | ticket binding | critical | mitigate | Policy ID/version/digest plus context digest persist and validate on every durable path. |
| T-03-46 | Repudiation | legacy ticket reconstruction | high | mitigate | Explicit legacy Spot decoder preserves historical scope without widening it. |
| T-03-SC | Tampering | package installs | low | accept | No package-manager operation is planned. |
</threat_model>

<verification>
Run focused policy-ticket unit and real-SQLite tests plus the existing product migration, approval-consumption, and uncertain-recovery suites. Verify all three valid product targets can issue one protected ticket/permit, every unsupported target/mode/context rejection occurs before authority, and legacy Spot reads remain valid.
</verification>

<success_criteria>
No Phase 3 margin or perpetual flow starts until every fixed-Spot policy and ticket guard has a target-bound three-product replacement with durable reconstruction. Paper truth remains offline and Decimal-only, and the verified permit-to-lease-to-coordinator route remains the only submission invocation.
</success_criteria>

<output>
Create `.planning/phases/03-paper-product-core/03-13-SUMMARY.md` when done.
</output>
