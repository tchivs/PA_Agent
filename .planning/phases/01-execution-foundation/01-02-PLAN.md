---
phase: 01-execution-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - pa_agent/trading/ports/__init__.py
  - pa_agent/trading/ports/gateway.py
  - pa_agent/trading/ports/ledger.py
  - pa_agent/trading/ports/clock.py
  - tests/unit/execution/test_gateway_contract.py
autonomous: true
requirements: [CORE-02]
must_haves:
  truths:
    - "Future adapters receive and return only canonical trading values and typed failures."
    - "A future submission path must obtain one explicit admission claim before it may call the gateway."
    - "A repeated unresolved logical command receives its first durable identity and job as non-admissible rather than a second claim."
  artifacts:
    - path: pa_agent/trading/ports/gateway.py
      provides: Canonical-only TradingGateway abstract contract.
    - path: pa_agent/trading/ports/ledger.py
      provides: Atomic command creation and submission-admission claim protocol.
    - path: tests/unit/execution/test_gateway_contract.py
      provides: Introspection coverage for canonical port and claim-result contract.
  key_links:
    - from: pa_agent/trading/ports/ledger.py
      to: pa_agent/trading/ports/gateway.py
      via: A future coordinator may invoke submit_order only after an admissible durable claim result.
      pattern: "SubmissionAdmission|submit_order"
---

<objective>
Publish the canonical gateway, ledger, and clock contracts, including the durable submission-admission invariant required by D-05 and D-06.

Purpose: Make duplicate prevention an enforceable call boundary before SQLite implementation or a future submission coordinator exists.
Output: Canonical ports and contract tests; no gateway implementation or remote submission.
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-execution-foundation/01-CONTEXT.md
@.planning/phases/01-execution-foundation/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-execution-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish canonical gateway and clock contracts</name>
  <files>pa_agent/trading/ports/__init__.py, pa_agent/trading/ports/gateway.py, pa_agent/trading/ports/clock.py, tests/unit/execution/test_gateway_contract.py</files>
  <behavior>
    - Test 1: TradingGateway declares capability, server-time, quote, rule, account, submit, cancel, client-ID lookup, open-order, fill, and reconciliation operations.
    - Test 2: public gateway signatures use canonical domain requests/results and typed trading exceptions, with no Qt, LLM, chart, or venue-payload type in the contract.
  </behavior>
  <action>Define documented synchronous `TradingGateway` ABC methods per D-05 for canonical evidence, submit/cancel commands, and client-ID recovery. Return canonical values or typed domain failures only. Define an injectable UTC clock protocol. Do not instantiate a gateway or implement a transport, submission coordinator, UI worker, adapter, credential handling, or any exchange call in this plan.</action>
  <verify><automated>python -m pytest tests/unit/execution/test_gateway_contract.py -q</automated></verify>
  <done>The adapter boundary is complete, canonical-only, and independently testable without a concrete exchange.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define atomic submission-admission and recovery ledger contract</name>
  <files>pa_agent/trading/ports/ledger.py, tests/unit/execution/test_gateway_contract.py</files>
  <behavior>
    - Test 1: the ledger exposes one atomic create-or-load-and-claim operation returning command ID, original client ID, reconciliation job ID, and an explicit admissible/non-admissible decision.
    - Test 2: the non-admissible result is required to retain the first command/client/job identities and unresolved state; it has no second claim token.
    - Test 3: the contract specifies that a future coordinator may call TradingGateway.submit_order only with an admissible claim and must mark ambiguity/reconcile using the same identities.
  </behavior>
  <action>Define the repository protocol and immutable `SubmissionAdmission` result so one command-shaped atomic operation creates or loads by logical command key and grants exactly one opaque submission claim only for the first unresolved command. Its non-admissible result must return the existing command ID, first client order ID, existing reconciliation job ID, unresolved lifecycle state, and no usable claim; it must not allocate another ID or job. Document that a future coordinator must possess an admissible claim before calling `TradingGateway.submit_order`, and that recovery can only query evidence with the persisted identities. This is the durable admission contract for D-06; it deliberately does not implement remote submission in Phase 1.</action>
  <verify><automated>python -m pytest tests/unit/execution/test_gateway_contract.py -q</automated></verify>
  <done>Every future submit caller has one explicit, testable admission gate, and reusing an unresolved logical command is contractually non-admissible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| future coordinator to ledger | A local caller requests authority to submit a persisted command. |
| future adapter to gateway port | Venue payloads must be normalized before entering the trusted trading domain. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Elevation | gateway and admission ports | critical | mitigate | Only an admissible ledger claim authorizes a future gateway submit; canonical annotations prevent UI, LLM, chart, and raw transport objects crossing the boundary. |
| T-01-SC | Tampering | package installation | low | accept | No package installation is planned. |
</threat_model>

<verification>Run `python -m pytest tests/unit/execution/test_gateway_contract.py -q` and `ruff check pa_agent/trading/ports tests/unit/execution/test_gateway_contract.py` when Ruff is available.</verification>

<success_criteria>The port layer makes one durable submission claim a required precondition for any future gateway call while keeping all remote execution outside Phase 1.</success_criteria>

<output>Create `.planning/phases/01-execution-foundation/01-02-SUMMARY.md` when done.</output>
