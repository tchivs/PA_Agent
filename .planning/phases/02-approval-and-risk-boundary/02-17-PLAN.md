---
phase: 02-approval-and-risk-boundary
plan: 17
type: execute
wave: 3
depends_on: [02-16]
files_modified:
  - pa_agent/trading/ports/ledger.py
  - tests/unit/execution/test_gateway_contract.py
autonomous: true
gap_closure: true
requirements: [SAFE-04, SAFE-03, SIM-03]
must_haves:
  truths:
    - "网关派发契约将账本消费产生的待核验身份与经账本租约重建的 gateway-facing OutboundSubmission 明确分离。"
    - "本计划只发布类型和端口契约；没有宣称伪造输入已经被运行时阻断，也不添加网关调用行为。"
  artifacts:
    - path: pa_agent/trading/ports/ledger.py
      provides: 待核验派发证明和一次性账本租约的窄端口契约
    - path: tests/unit/execution/test_gateway_contract.py
      provides: 网关只接收账本租约重建值的结构契约
  key_links:
    - from: pa_agent/trading/application/submission.py
      to: pa_agent/trading/ports/ledger.py
      via: 下一计划实现的 lease operation 才会返回 gateway-facing OutboundSubmission
      pattern: OutboundDispatchPermit|lease_outbound_submission
---

<objective>
定义账本待核验派发证明与一次性租约的端口契约，为 02-18 的 SQLite 原子阻断实现准备接口。

Purpose: 让运行时实现可以把调用方提供的不可验证证明与仅由账本重建的网关值严格区分，而不在本计划制造半成品安全声明。
Output: 窄的类型/端口契约及其结构测试；不修改协调器、迁移或 SQLite 行为。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/02-approval-and-risk-boundary/02-REVIEW.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-07-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-16-SUMMARY.md

<interfaces>
`consume_valid_ticket_and_begin_outbound()` currently returns a public `OutboundSubmission`; 02-18 will instead mint a durable opaque dispatch permit and require `ExecutionLedger.lease_outbound_submission(permit)` to reconstruct the gateway-facing value. `TradingGateway.submit_order()` remains typed to `OutboundSubmission` and has no alternative command/ticket/admission input.

`02-PATTERNS.md` assigns `ports/ledger.py` to its exact protocol analog and `application/submission.py` to the gateway-only dependency pattern. Preserve those directions: the new permit contains identities/proof only, has no raw command or gateway reference, and this plan must not modify `SubmissionCoordinator`, SQLite, migrations, or a fake gateway. The later implementation must use the documented `BEGIN IMMEDIATE` and conditional-rowcount pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the two-stage outbound dispatch contract</name>
  <files>tests/unit/execution/test_gateway_contract.py</files>
  <behavior>
    - Ticket consumption returns an opaque dispatch permit containing durable identifiers and an opaque proof, not a caller-supplied gateway command.
    - The ledger protocol exposes one lease operation that takes that permit and returns the only gateway-facing OutboundSubmission.
    - TradingGateway retains exactly one submit input, OutboundSubmission, and has no raw command, ticket, claim, GUI, alert, or notification overload.
  </behavior>
  <action>Write RED structural contract tests for CR-01's required two-stage shape. Test names must state that this is contract preparation, not forgery blocking: inspect annotations and docstrings for `OutboundDispatchPermit`, ticket-consumption return type, and `lease_outbound_submission`. Preserve the existing gateway-only signature and ensure the permit carries no `ExecutionCommand`, ticket, gateway, UI, alert, notification, or credential field. Per D-04 and D-11, record that only the future ledger implementation may turn a consumed ticket into a gateway value; do not create a fake coordinator dispatch or any zero-call behavioral assertion in this plan.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_gateway_contract.py</automated></verify>
  <done>The test suite describes a narrow two-stage authorization contract without claiming persistent validation or forged-input rejection exists yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the opaque permit and ledger lease protocol</name>
  <files>pa_agent/trading/ports/ledger.py, tests/unit/execution/test_gateway_contract.py</files>
  <behavior>
    - The permit is an immutable identity/proof carrier with no command payload and the lease method is the sole protocol path that can return OutboundSubmission for dispatch.
  </behavior>
  <action>Implement the contract defined by Task 1 in `ports/ledger.py`: introduce a frozen `OutboundDispatchPermit` containing only durable command/client-order/reconciliation identities and opaque attempt proof, adjust the ticket-consumption return type, and add `lease_outbound_submission(permit) -> OutboundSubmission` with an explicit one-time durable-verification contract. Retain `OutboundSubmission` as the gateway-facing canonical value and do not rely on constructor privacy as a security boundary. Follow the `02-PATTERNS.md` exact port and gateway-only patterns: no raw command/ticket/admission substitute, no SQLite implementation, no migration, and no coordinator behavior in this plan. Per D-11, document that the next plan must mint and consume the proof through transaction-time rowcount checks.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_gateway_contract.py &amp;&amp; .venv/bin/ruff check pa_agent/trading/ports/ledger.py tests/unit/execution/test_gateway_contract.py</automated></verify>
  <done>The public contract cleanly separates unverified dispatch proof from a ledger-leased gateway submission value, ready for 02-18's implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-owned dispatch permit -> future ledger lease | The permit is untrusted until 02-18 verifies its persisted proof. |
| ledger lease result -> gateway | Only the lease result is eligible for the existing gateway-only submit signature. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-36 | Elevation/Tampering | outbound dispatch contract | critical | mitigate | This plan defines the separable permit/lease boundary; 02-18 supplies persistent verification, rowcount lease, and forged-input regressions. |
| T-02-SC | Tampering | package installs | low | accept | No package installation occurs. |
</threat_model>

<verification>
Run the contract test after each task. Do not treat this plan as closure for CR-01; only 02-18's real-SQLite and coordinator tests can verify the runtime boundary.
</verification>

<success_criteria>
The codebase has an executable two-stage outbound contract without a premature claim that arbitrary permits are already blocked from the gateway.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-17-SUMMARY.md` when done.
</output>
