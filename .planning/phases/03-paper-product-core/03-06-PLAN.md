---
phase: 03-paper-product-core
plan: 06
type: tdd
wave: 7
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-09, 03-11]
files_modified:
  - pa_agent/trading/gateways/paper/accounting_perpetual.py
  - pa_agent/trading/gateways/paper/gateway.py
  - tests/unit/execution/test_paper_perpetual.py
  - tests/integration/execution/test_paper_perpetual_liquidation.py
autonomous: true
requirements: [SIM-01]
must_haves:
  truths:
    - "USDT perpetual state is isolated, one-way, and symbol-scoped with bounded Decimal leverage, margin, mark PnL, funding, and a mandatory protective exit plan."
    - "Explicit observations update mark PnL and funding; maintenance-margin breach creates durable liquidation close evidence and fee, leaves no open unbounded position, and cannot silently produce negative balances."
    - "Unsupported hedge/cross/auto-add modes, missing/mismatched exit plan, leverage breach, or unsafe initial/maintenance state reject before simulated fill."
  artifacts:
    - path: pa_agent/trading/gateways/paper/accounting_perpetual.py
      provides: isolated one-way position, margin, PnL, funding, maintenance, and liquidation projector
    - path: tests/unit/execution/test_paper_perpetual.py
      provides: entry, reduce-only exit, mark/funding, and risk-gated rejection tests
    - path: tests/integration/execution/test_paper_perpetual_liquidation.py
      provides: durable liquidation/restart and no-negative/unbounded-state regression
  key_links:
    - from: `UsdtPerpetualOrderContext` and `ProtectiveExitPlan`
      to: PaperGateway perpetual projector
      via: policy-bound isolated-one-way symbol context
    - from: explicit market observation
      to: funding/mark/maintenance/liquidation evidence
      via: durable event sequence and versioned Decimal liquidation policy
---

<objective>
Complete PaperGateway product semantics with durable USDT perpetual isolated one-way positions, explicit mark/funding updates, and deterministic liquidation.

Purpose: D-07/D-08 require a product-specific risk/accounting lifecycle. A simple position balance or nonterminal maintenance warning is insufficient.
Output: Dedicated perpetual projector and deterministic liquidation evidence with focused offline lifecycle and reopen tests.
</objective>

<execution_context>
@/root/.omp/agent/gsd-core/workflows/execute-plan.md
@/root/.omp/agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-paper-product-core/03-CONTEXT.md
@.planning/phases/03-paper-product-core/03-RESEARCH.md
@.planning/phases/03-paper-product-core/03-03-PLAN.md
@.planning/phases/03-paper-product-core/03-05-PLAN.md
@.planning/phases/03-paper-product-core/03-09-PLAN.md
@pa_agent/trading/domain/models.py
@pa_agent/trading/domain/paper.py
@pa_agent/trading/gateways/paper/store.py
@pa_agent/trading/gateways/paper/gateway.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify isolated one-way perpetual valuation, exit gating, funding, and liquidation</name>
  <files>tests/unit/execution/test_paper_perpetual.py, tests/integration/execution/test_paper_perpetual_liquidation.py</files>
  <behavior>
    - A policy-bound isolated one-way entry creates/changes only one symbol position, locks exact initial Decimal margin, and rejects leverage outside the product policy or any hedge/cross/auto-add/missing-or-mismatched protective exit context before a fill.
    - New explicit observation versions update mark price, unrealized PnL, maintenance requirement, and versioned Decimal funding without local time. A reduce-only protective exit cannot increase exposure.
    - A maintenance breach deterministically appends liquidation/close evidence and a liquidating fill with immutable mark/fee/rule inputs, closes or zeroes the position, releases/resolves margin, and leaves no negative available USDT or unbounded exposure.
    - Repeated/stale/conflicting observation versions cannot apply funding twice, reopen a liquidated/terminal order, recreate a position, or change the liquidation fill/account truth after restart.
  </behavior>
  <action>Write RED tests with fixed Decimal entry/mark/funding/maintenance/liquidation policy values and explicit observation schedules. Cover long and short isolated one-way entries, policy leverage bounds, invalid/missing protective exit plans, reduce-only behavior, funding/mark PnL updates, and maintenance breaches. Assert the exact deterministic close price and liquidation fee defined by the versioned policy—not a floating approximation—and require durable liquidation event plus fill provenance. Use filesystem paper SQLite reopen to assert the final position is closed/zero, balances are nonnegative, and duplicate/reordered observations are no-ops/incidents. Per D-07, all valuation/funding comes from explicit observations; per D-08, liquidation is durable/reconcilable rather than a balance mutation; per D-11, prove no replay/regression.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_perpetual.py tests/integration/execution/test_paper_perpetual_liquidation.py</automated></verify>
  <done>The focused suite fails because PaperGateway has no perpetual accounting, protective-exit enforcement, funding, or durable liquidation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement isolated perpetual accounting and deterministic liquidation close events</name>
  <files>pa_agent/trading/gateways/paper/accounting_perpetual.py, pa_agent/trading/gateways/paper/gateway.py, tests/unit/execution/test_paper_perpetual.py, tests/integration/execution/test_paper_perpetual_liquidation.py</files>
  <behavior>
    - State is keyed by `(account_id, symbol)` and supports one signed net quantity only; opposite orders reduce/close before any reversal and reduce-only commands never increase absolute exposure.
    - Every entry, mark, funding, maintenance check, liquidation close, and fee is computed with frozen Decimal policy/observation inputs and committed in the paper store event transaction.
    - The gateway’s normal canonical evidence and query surface reports the independent paper truth, including a liquidation result, without producing a new lease, submission, or central-ledger write.
  </behavior>
  <action>Implement `accounting_perpetual.py` as a dedicated symbol-scoped projector. Enforce the Plan 03-03 `UsdtPerpetualOrderContext` and `ProtectiveExitPlan`: isolated margin, one-way mode, bounded Decimal leverage, exact symbol/side, and reduce-only exits. Define the discretionary funding and liquidation formulae as explicit fields of `PaperEconomicPolicy` with version strings, Decimal factors, deterministic mark close price, and liquidation fee; never source them from wall time or float. On each accepted observation, atomically apply matched fills, mark PnL, funding, and maintenance margin. If maintenance is breached, append liquidation event/evidence first, create a provenance-complete liquidation fill, close/zero the position, settle/release isolated margin, and reject any state that would leave negative available balance or nonzero unbounded exposure. Update `PaperGateway` to dispatch only the exact perpetual context to this projector after its existing leased submit validation. Do not add hedge/cross/portfolio/auto-add modes, a network adapter, a second gateway call path, or a central projection; retain all Spot/margin behavior unchanged. This implements D-07 and D-08 on the shared D-03/D-04/D-09/D-11 durable observation contract.</action>
  <verify><automated>.venv/bin/pytest -q -o addopts='' tests/unit/execution/test_paper_perpetual.py tests/integration/execution/test_paper_perpetual_liquidation.py tests/unit/execution/test_paper_margin.py</automated></verify>
  <done>Paper USDT perpetual entries, valuation, funding, exits, and liquidations are deterministic Decimal facts owned by the paper store. Unsafe modes/contexts fail before fill, and maintenance breaches produce auditable close/fill/fee evidence that survives restart without negative or unbounded state.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature or constraint | Plan | Status | Notes
GOAL | — | Complete safe Paper product lifecycle | 03-06 | COVERED | Perpetual lifecycle now has a bounded durable product projector.
REQ | SIM-01 | USDT perpetual semantics, leverage, deterministic fills/recovery | 03-06 | COVERED | Isolated one-way Decimal valuation and reopen liquidation test.
RESEARCH | Stage 4, Pitfall 5, A3 | Separate perpetual accounting; close/fill-based liquidation | 03-06 | COVERED | Versioned formula rules are test-locked and event-sourced.
CONTEXT | D-07 | Isolated one-way symbol position, margin/PnL/funding from observations | 03-06 | COVERED | All are symbol-scoped and observation-driven.
CONTEXT | D-08 | Deterministic durable liquidation/close events and fees | 03-06 | COVERED | Provenanced liquidation fill closes/zeros position with invariants.
CONTEXT | D-03-D-04 | Explicit observations and Decimal/versioned facts | 03-06 | COVERED | No wall-clock calculation; policy values persist per event/fill.
CONTEXT | D-09-D-11 | Ordered/deduplicated non-regressing projection | 03-06 | COVERED | Reuses event sequence/cursor across funding and liquidation.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| perpetual context -> projector | Only exact isolated-one-way/exit-plan facts can create or modify exposure. |
| market observation -> maintenance/liquidation | Versioned mark/funding facts may move PnL and trigger one deterministic close. |
| liquidation -> account/position snapshot | Close event/fill and position settlement must be atomic and audit-preserving. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-20 | Elevation | leverage/mode/exit context | critical | mitigate | Strict isolated-one-way/Decimal-bounded context and protective-exit validation before fill. |
| T-03-21 | Tampering | PnL/funding/liquidation math | critical | mitigate | Frozen Decimal inputs and versioned rules per observation/liquidation fill; replay tests prevent double funding. |
| T-03-22 | Integrity | liquidation state | critical | mitigate | Atomic event-before-close evidence/fill/snapshot transaction and nonnegative/zero-exposure invariants. |
| T-03-23 | Repudiation | forced close evidence | high | mitigate | Immutable provenance identifies observation, rules, mark price, fee, and paper sequence. |
| T-03-SC | Tampering | package installs | low | accept | No package operation. |
</threat_model>

<verification>
Run the perpetual unit/liquidation integration suites and margin regression. Verify liquidation is exact and durable, a reopened store cannot replay funding or recreate exposure, and every rejected context has zero fill/order-account mutation.
</verification>

<success_criteria>
The PaperGateway accurately models only its declared perpetual scope: isolated, one-way, symbol-bounded positions with Decimal policy calculations. Explicit observations produce valuations/funding; deterministic maintenance breaches create auditable liquidation fills and converge to safe closed account state.
</success_criteria>

<output>
Create `.planning/phases/03-paper-product-core/03-06-SUMMARY.md` when done.
</output>
