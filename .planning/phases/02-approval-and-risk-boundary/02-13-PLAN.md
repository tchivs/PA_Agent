---
phase: 02-approval-and-risk-boundary
plan: 13
type: execute
wave: 9
depends_on: [02-07, 02-08, 02-10, 02-11, 02-12]
files_modified:
  - pa_agent/trading/domain/errors.py
  - pa_agent/trading/domain/risk.py
  - pa_agent/trading/application/risk_engine.py
  - tests/unit/execution/test_risk_engine.py
  - tests/integration/execution/test_approval_ticket_issuance.py
  - tests/integration/execution/test_approval_consumption.py
  - tests/integration/execution/test_idempotency_recovery.py
autonomous: true
gap_closure: true
requirements: [SAFE-02]
must_haves:
  truths:
    - "所选 Paper Spot `phase2-v1` 策略以不可变 Decimal 限额和摘要约束价格偏离与买卖价差滑点；超限评估返回稳定原因码。"
    - "超限价格偏离或买卖价差滑点不能自动签发审批票据，也不能在重新取证的票据消费中获得命令、claim、OutboundSubmission 或网关提交。"
    - "完整执行测试语料库的构造器迁移历史断言承认有效迁移 4，而不降低迁移并发或原子性覆盖。"
  artifacts:
    - path: pa_agent/trading/domain/risk.py
      provides: 目标和产品绑定的 `maximum_price_deviation`、`maximum_bid_ask_slippage` Decimal 策略字段及 digest material
    - path: pa_agent/trading/application/risk_engine.py
      provides: 在生成可接受评估前执行的价格偏离和买卖价差滑点稳定拒绝
    - path: tests/integration/execution/test_approval_ticket_issuance.py
      provides: 真实 SQLite 下超限指标无法签发票据的回归
    - path: tests/integration/execution/test_approval_consumption.py
      provides: 真实 SQLite 下新鲜超限指标无法消费票据或形成出站授权的回归
    - path: tests/integration/execution/test_idempotency_recovery.py
      provides: 有效四版本迁移历史的并发构造器断言
  key_links:
    - from: pa_agent/trading/domain/risk.py
      to: pa_agent/trading/application/risk_engine.py
      via: 目标绑定的 Decimal 阈值、policy digest 和稳定 RiskRejectionReason
      pattern: maximum_price_deviation|maximum_bid_ask_slippage|PRICE_DEVIATION_LIMIT_EXCEEDED|BID_ASK_SLIPPAGE_LIMIT_EXCEEDED
    - from: pa_agent/trading/application/proposal.py
      to: pa_agent/trading/application/approval.py
      via: 被拒绝的 RiskAssessment 不会调用 create_pending_ticket
      pattern: assessment.accepted|create_pending_ticket
    - from: pa_agent/trading/application/approval.py
      to: pa_agent/trading/ports/ledger.py
      via: 新鲜重评估拒绝先于 consume_valid_ticket_and_begin_outbound
      pattern: risk_reassessment_rejected|consume_valid_ticket_and_begin_outbound
---

<objective>
闭合 SAFE-02 阻塞项：将价格偏离和买卖价差滑点从仅记录的度量提升为所选目标和产品绑定的 Decimal 风险限制，并在自动建票和票据消费的两道授权边界证明其失败关闭。

Purpose: 防止不利价格或过宽价差在已完成的审批链中绕过确定性风险控制，同时恢复完整离线执行测试语料库的迁移历史断言。
Output: 两个 digest-bound RiskPolicy 限额、两个稳定风险原因码、纯风险和真实 SQLite 回归，以及迁移 4 的断言修正。
</objective>

<scope_boundaries>
本计划只直接闭合 SAFE-02。CORE-03、SIM-03、SAFE-01、SAFE-03 与 SAFE-04 保持既有完成边界：候选仍来自冻结来源快照，审计仍由 SQLite 账本持久化，唯一 Paper Spot 目标和 durable kill switch 不变，票据仍以单次 `OutboundSubmission` 授权。SAFE-05 的 `SecretRedactor` 尚无生产日志、通知或审计消费者；依据 `02-VERIFICATION.md` 的人工架构/范围决定，它明确不属于本计划，且不得增加 redaction 接线、GUI、通知、分析路径或网关权限。

D-01、D-02、D-03 与 D-04 的候选转换、持久化拒绝、来源绑定和无提交自动建票边界保持不变。D-05、D-06、D-07 与 D-08 要求本计划在每次目标绑定的新鲜重评估中使用固定策略限额且失败关闭；D-09、D-10、D-11 与 D-12 的票据审阅、失效、单次消费和终止事件语义保持不变。
</scope_boundaries>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-RESEARCH.md
@.planning/phases/02-approval-and-risk-boundary/02-PATTERNS.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/02-approval-and-risk-boundary/02-VALIDATION.md
@.planning/phases/02-approval-and-risk-boundary/02-03-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-07-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-08-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-10-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-11-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-12-SUMMARY.md

<interfaces>
From `pa_agent/trading/domain/risk.py`: `RiskPolicy` is frozen, validates the sole `phase2-v1` Paper Spot policy, and hashes every fixed policy input through `_digest_material()`; `EvidenceBundle.quote` provides bid/ask Decimal evidence.

From `pa_agent/trading/application/risk_engine.py`: `RiskEngine.assess()` is pure and currently computes `price_deviation` and `slippage` metrics after other risk gates. It must remain free of gateway, ledger, proposal, approval, GUI, notification, and submission imports.

From `pa_agent/trading/application/proposal.py`: a rejected assessment is persisted but cannot reach `ApprovalService.create_pending_ticket()`.

From `pa_agent/trading/application/approval.py`: consumption refreshes evidence and re-runs `RiskEngine.assess()` before `consume_valid_ticket_and_begin_outbound()`; a rejected reassessment uses the established durable `risk_reassessment_rejected` terminal outcome.

From `tests/integration/execution/test_idempotency_recovery.py`: two constructor-race tests query exact `schema_migrations` version/count rows and must include the existing kill-switch migration 4.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify digest-bound price-deviation and bid-ask-slippage policy boundaries</name>
  <files>tests/unit/execution/test_risk_engine.py</files>
  <read_first>Read `pa_agent/trading/domain/risk.py`, `pa_agent/trading/domain/errors.py`, `pa_agent/trading/application/risk_engine.py`, `tests/unit/execution/test_risk_engine.py`, and `02-VERIFICATION.md` gap `SAFE-02`.</read_first>
  <behavior>
    - `select_phase2_policy(make_execution_target())` exposes exact Decimal `maximum_price_deviation=80` and `maximum_bid_ask_slippage=4`, and both values appear in immutable policy digest material. These values apply only to the selected `paper-spot-primary` Paper Spot `phase2-v1` policy per D-08.
    - A candidate price exactly 80 USDT from its side-specific expected quote price and a quote spread exactly 4 USDT remain acceptable when every other control passes; one valid price tick beyond either limit returns exactly one respective stable risk reason.
    - The rejected assessment retains `price_deviation` and `slippage` Decimal metrics so audit and ticket-review consumers can distinguish calculated values from policy acceptance; it performs no external interaction.
  </behavior>
  <action>Write failing unit tests before implementation. Use the current exact-Decimal Paper Spot candidate and evidence factories to verify the two fixed Decimal fields and their digest material. Parameterize independent threshold-boundary cases: a BUY candidate 80 USDT from the ask versus one 0.50-USDT price tick farther, and a bid/ask spread of 4 USDT versus one 0.50-USDT tick wider. Require `RiskRejectionReason.PRICE_DEVIATION_LIMIT_EXCEEDED` for the excessive expected-price delta and `RiskRejectionReason.BID_ASK_SLIPPAGE_LIMIT_EXCEEDED` for the excessive spread; avoid exceeding notional, precision, balance, exposure, or other existing limits in these cases. Per D-08, keep all inputs bound to the sole selected target and assert `RiskEngine` stays a pure test subject with no gateway, ledger, ticket, or submission collaborator.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_risk_engine.py</automated></verify>
  <acceptance_criteria>The new unit cases fail before the policy implementation, distinguish exact limit from one-tick excess for both metrics, require the two stable reason codes, and prove both values participate in policy digest material.</acceptance_criteria>
  <done>Executable pure-risk acceptance criteria define selected-target Decimal price-deviation and bid-ask-slippage behavior without widening product scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the two selected-target Decimal limits in the pure risk engine</name>
  <files>pa_agent/trading/domain/errors.py, pa_agent/trading/domain/risk.py, pa_agent/trading/application/risk_engine.py, tests/unit/execution/test_risk_engine.py</files>
  <read_first>Read Task 1's failing tests, `pa_agent/trading/domain/risk.py`, `pa_agent/trading/application/risk_engine.py`, `pa_agent/trading/domain/models.py` Decimal canonicalization helpers, and `02-10-SUMMARY.md` for the existing fixed-policy/digest pattern.</read_first>
  <behavior>
    - `RiskPolicy` normalizes and validates both new limit values as immutable finite Decimal fields, includes them in `_digest_material()`, and permits only `80` and `4` for the sole Paper Spot `phase2-v1` policy.
    - `RiskEngine.assess()` calculates price deviation against the existing side-specific expected quote and bid-ask slippage from the same fresh quote. An excess makes `accepted` false before a fee estimate or potential approval side effect can exist.
    - Existing target matching, precision, balance, exposure, rate, loss, drawdown, fee-evidence, kill-switch, ticket lifecycle, and sole outbound-submission boundaries retain their current semantics.
  </behavior>
  <action>Extend `RiskRejectionReason` with `PRICE_DEVIATION_LIMIT_EXCEEDED` and `BID_ASK_SLIPPAGE_LIMIT_EXCEEDED`. Extend frozen `RiskPolicy`, validation, `_digest_material()`, and `select_phase2_policy()` with `maximum_price_deviation=Decimal("80")` and `maximum_bid_ask_slippage=Decimal("4")`; reject substitutes so these cannot become global, caller-supplied, or unsupported-product limits. In `RiskEngine.assess()`, retain the current side-specific expected quote calculation and canonical Decimal metrics, compare absolute candidate-price deviation and absolute quote bid/ask spread against the new policy fields before acceptance, and append the corresponding stable reason whenever strictly above its limit. Preserve metrics on rejected assessments, preserve reason de-duplication/order, and leave the engine's pure dependency boundary intact. Do not modify ProposalService, ApprovalService, gateway authority, advisory packages, GUI, notifications, or redaction consumers.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_risk_engine.py &amp;&amp; .venv/bin/ruff check pa_agent/trading/domain/errors.py pa_agent/trading/domain/risk.py pa_agent/trading/application/risk_engine.py tests/unit/execution/test_risk_engine.py</automated></verify>
  <acceptance_criteria>All pure-risk tests pass: only the selected Paper Spot policy contains the two immutable Decimal limits, each over-limit condition returns its stable reason, and no threshold can be supplied outside the policy digest.</acceptance_criteria>
  <done>SAFE-02 has deterministic selected-target, product-bound policy enforcement for price deviation and bid-ask slippage in the pure risk boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove both authorization boundaries reject the metrics and repair migration-history expectations</name>
  <files>tests/integration/execution/test_approval_ticket_issuance.py, tests/integration/execution/test_approval_consumption.py, tests/integration/execution/test_idempotency_recovery.py</files>
  <read_first>Read Task 2's passing unit tests, `pa_agent/trading/application/proposal.py`, `pa_agent/trading/application/approval.py`, `tests/integration/execution/test_approval_ticket_issuance.py`, `tests/integration/execution/test_approval_consumption.py`, `tests/integration/execution/test_idempotency_recovery.py`, and `02-11-SUMMARY.md` for the established no-ticket/no-outbound SQLite assertions.</read_first>
  <behavior>
    - In real SQLite proposal workflows, an excessive expected-price delta and an excessive bid/ask spread each persist a rejected assessment with the matching stable risk reason, create no pending ticket, command, claim, or outbound record, and call no gateway submission method.
    - In real SQLite consumption workflows, a ticket issued from within-limit evidence is independently refreshed against an excessive expected-price delta or excessive bid/ask spread. Each reassessment retains no outbound value, terminates the ticket through the existing `risk_reassessment_rejected` lifecycle path, creates no command or claim, and calls no gateway submission method.
    - Both concurrent fresh- and reopened-constructor migration-history assertions expect one applied row for versions 1 through 4, preserving the existing foreign-key, WAL, synchronous, busy-timeout, and single-claim assertions.
  </behavior>
  <action>First add focused real-SQLite regressions using the normal ProposalService and ApprovalService paths, extending only their local offline quote scripting helpers. For automatic issuance, parameterize two cases: a candidate price one tick beyond 80 USDT from the current side-specific quote, and a quote spread one tick beyond 4 USDT while all other gates remain within limit. Assert the persisted `risk_rejected` audit reason is respectively `price_deviation_limit_exceeded` or `bid_ask_slippage_limit_exceeded`, no approval ticket exists, `order_commands` and `submission_claims` remain zero, and fake submit count is zero. For consumption, issue against a first within-limit quote then script a second fresh quote that independently breaches either price deviation or spread; assert `consume_ticket()` returns no outbound value, the ticket's final event/reason remains the established deterministic `binding_invalidated` / `risk_reassessment_rejected` contract, no command or claim exists, and no outbound gateway call occurs. Do not add alternate submit paths or call SubmissionCoordinator in rejection tests. Finally update only the two stale expected migration-history vectors in `test_idempotency_recovery.py` to include the existing `(4, 1)` migration row; do not alter migrations, constructor concurrency, or durability assertions.</action>
  <verify><automated>.venv/bin/pytest -q tests/unit/execution/test_risk_engine.py tests/integration/execution/test_approval_ticket_issuance.py tests/integration/execution/test_approval_consumption.py tests/integration/execution/test_idempotency_recovery.py &amp;&amp; .venv/bin/pytest -q tests/unit/execution tests/integration/execution tests/property/execution &amp;&amp; .venv/bin/ruff check pa_agent/trading/domain/errors.py pa_agent/trading/domain/risk.py pa_agent/trading/application/risk_engine.py tests/unit/execution/test_risk_engine.py tests/integration/execution/test_approval_ticket_issuance.py tests/integration/execution/test_approval_consumption.py tests/integration/execution/test_idempotency_recovery.py</automated></verify>
  <acceptance_criteria>Both metrics have independent real-SQLite rejection proof at issuance and consumption, every rejection produces no ticket or outbound authorization as applicable, and both migration-history assertions accept exactly one valid migration 4 row while the full offline execution corpus passes.</acceptance_criteria>
  <done>SAFE-02 is closed across pure policy, durable proposal issuance, and refreshed ticket consumption; the full execution corpus no longer fails solely because valid migration 4 was omitted from test expectations.</done>
</task>

</tasks>

<source_audit>
SOURCE    | ID       | Feature or constraint | Plan | Status | Notes
GOAL      | -        | Only fresh deterministic controls may authorize a typed command | 02-13 | COVERED | New selected-target policy gates run before ticket issue and consumption.
REQ       | SAFE-02  | Price deviation and slippage limits before submission | 02-13 | COVERED | Direct gap closure: pure policy plus two real-SQLite authorization boundaries.
REQ       | CORE-03  | Deterministic advisory conversion | 02-01, 02-09 | PRESERVED | No candidate conversion or advisory boundary change.
REQ       | SIM-03   | Durable execution audit | 02-05, 02-07, 02-08, 02-11 | PRESERVED | Existing rejected-assessment and terminal-event persistence is exercised.
REQ       | SAFE-01  | Explicit Paper target selection | 02-03 | PRESERVED | New limits exist only on the existing Paper Spot policy.
REQ       | SAFE-03  | Latched kill switch | 02-06, 02-12 | PRESERVED | No latch or recovery semantics change.
REQ       | SAFE-04  | Single-use, expiring operator approval | 02-07, 02-08 | PRESERVED | Existing ticket and atomic outbound boundaries are exercised, not redesigned.
REQ       | SAFE-05  | Credential isolation and output redaction | 02-02 | DEFERRED BY HUMAN SCOPE DECISION | `02-VERIFICATION.md` identifies absent production consumer wiring as a human architecture decision; no implementation is planned here.
RESEARCH  | -        | No new packages; immutable Decimal, pure risk engine, SQLite ledger, offline fakes | 02-13 | COVERED | No dependency install; extends established values and test layers only.
CONTEXT   | D-01-D-04 | Frozen conversion and review-only automatic ticket issue | 02-13 | PRESERVED | Rejection tests retain existing no-advisory-submit boundary.
CONTEXT   | D-05-D-08 | Full fresh evidence and target/product policy binding | 02-13 | COVERED | Fresh quote drives both limits; fixed policy/digest has no caller override.
CONTEXT   | D-09-D-12 | Review, invalidation, atomic consumption, durable terminal events | 02-13 | PRESERVED | Existing consumption invalidation and no-outbound behavior are asserted.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fresh quote evidence -> pure risk assessment | A stale or adverse quote must not be treated as acceptable merely because the two metrics are displayed. |
| accepted assessment -> pending ticket | A metric-over-limit assessment must be blocked before review authorization is persisted. |
| refreshed assessment -> outbound authority | A ticket based on earlier quote facts must not survive a new adverse price-deviation or spread observation. |
| SQLite migration history -> constructor safety tests | Test expectations must describe every valid migration without weakening concurrency/durability assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-25 | Tampering/Elevation | `RiskPolicy` and `RiskEngine.assess` | high | mitigate | Task 2 binds fixed 80/4 Decimal limits to the sole selected Paper Spot policy digest and rejects over-limit values with two stable reason codes under D-08. |
| T-02-26 | Elevation | proposal-to-ticket issuance | high | mitigate | Task 3 uses real SQLite to prove each rejected metric persists no ticket, command, claim, outbound authorization, or gateway submission under D-04. |
| T-02-27 | Tampering/Elevation | approval consumption refresh | high | mitigate | Task 3 scripts independent fresh quote evidence and proves D-05/D-06 reassessment terminates the pending ticket before the atomic outbound boundary. |
| T-02-28 | Repudiation | rejection audit and ticket terminal event | medium | mitigate | Stable pure reasons remain in rejected assessment audit rows; existing `risk_reassessment_rejected` terminal events remain durable and deterministic. |
| T-02-29 | Tampering | migration-history test expectation | medium | mitigate | Correct only the two exact expected vectors to include migration 4, retaining all bootstrap and single-claim assertions. |
| T-02-SC | Tampering | package installs | low | accept | No package-manager operation is planned; project Python, SQLite, Pytest, and existing offline fakes are used. |
</threat_model>

<verification>
Run the focused unit and three integration files after their tasks, then run `.venv/bin/pytest -q tests/unit/execution tests/integration/execution tests/property/execution` and the declared Ruff command. The full suite is the completion gate because `02-VERIFICATION.md` reported stale migration-history failures there.
</verification>

<success_criteria>
The only selectable Phase 2 policy digest contains immutable Decimal limits for 80 USDT price deviation and 4 USDT bid-ask slippage. Over-limit values receive deterministic stable risk reasons and cannot issue or consume authorization in real SQLite workflows; no rejection path makes an outbound gateway call. Both migration-history constructor tests include the valid migration 4 row, and the offline execution corpus is green.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-13-SUMMARY.md` when done.
</output>
