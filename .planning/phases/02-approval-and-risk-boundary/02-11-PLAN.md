---
phase: 02-approval-and-risk-boundary
plan: 11
type: execute
wave: 7
depends_on: [02-08, 02-10]
files_modified:
  - tests/integration/execution/test_approval_ticket_issuance.py
  - tests/integration/execution/test_approval_consumption.py
autonomous: true
gap_closure: true
requirements: [SIM-03, SAFE-02, SAFE-04]
must_haves:
  truths:
    - "超过总敞口上限的 ProposalService 风险结果不会创建待审批票据。"
    - "已签发票据在消费前重新取证发现超敞口时被持久化失效，且不会产生命令、claim、OutboundSubmission 或网关调用。"
  artifacts:
    - path: tests/integration/execution/test_approval_ticket_issuance.py
      provides: 过度敞口不能自动建票的真实 SQLite 回归
    - path: tests/integration/execution/test_approval_consumption.py
      provides: 新鲜超敞口不能消费既有票据的真实 SQLite 回归
  key_links:
    - from: pa_agent/trading/application/proposal.py
      to: pa_agent/trading/application/approval.py
      via: rejected RiskAssessment 阻止 create_pending_ticket
      pattern: EXPOSURE_LIMIT_EXCEEDED|create_pending_ticket
    - from: pa_agent/trading/application/approval.py
      to: pa_agent/trading/ports/ledger.py
      via: refresh then reassess rejects before consume_valid_ticket_and_begin_outbound
      pattern: risk_reassessment_rejected|consume_valid_ticket_and_begin_outbound
  prohibitions:
    - "测试不得增加候选、票据、GUI、通知或分析到 gateway 的替代提交路径。"
    - "超敞口重评估不得消费票据或调用 SubmissionCoordinator。"
---

<objective>
证明新的总敞口风险拒绝在自动建票和既有票据消费两个授权边界都能阻断出站工作。

Purpose: 将纯风险策略连接到可观察的审计、票据和唯一出站授权结果。
Output: 两条真实 SQLite 工作流回归，分别覆盖建票拒绝和消费前重新取证拒绝。
</objective>

<execution_context>
@/root/.config/opencode/gsd-core/workflows/execute-plan.md
@/root/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-approval-and-risk-boundary/02-CONTEXT.md
@.planning/phases/02-approval-and-risk-boundary/02-VERIFICATION.md
@.planning/phases/02-approval-and-risk-boundary/02-07-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-08-SUMMARY.md
@.planning/phases/02-approval-and-risk-boundary/02-10-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove over-limit proposal assessment cannot issue a ticket</name>
  <files>tests/integration/execution/test_approval_ticket_issuance.py</files>
  <read_first>Read `pa_agent/trading/application/proposal.py`, `pa_agent/trading/application/approval.py`, `pa_agent/trading/application/risk_engine.py`, `tests/integration/execution/test_approval_ticket_issuance.py`, `tests/fixtures/fake_exchange.py`, and `02-VERIFICATION.md` gap 2.</read_first>
  <behavior>
    - The scripted fresh evidence contains a selected-target BTCUSDT position whose existing gross exposure plus the candidate notional exceeds the fixed policy cap.
    - ProposalService persists a rejected assessment with `EXPOSURE_LIMIT_EXCEEDED`, creates no approval ticket, and creates no command, claim, outbound authorization, or gateway submission.
  </behavior>
  <action>Extend the local `_gateway()` fixture only as needed to inject canonical `Position` values into its account observation. Add a real-SQLite integration test that invokes the normal ProposalService conversion, fresh-evidence collection, risk assessment, and automatic issuance chain with projected exposure above 1000 USDT. Assert `assessment.accepted is False`, the stable exposure reason is present, `ledger.list_approval_tickets()` is empty, `order_commands` and `submission_claims` remain zero, and the scripted gateway records zero submit calls. Preserve the normal selected Paper Spot target and no-network fake behavior.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_approval_ticket_issuance.py</automated></verify>
  <acceptance_criteria>The new integration case fails before the policy implementation and proves an over-limit risk assessment has no automatic ticket or outbound persistence side effect.</acceptance_criteria>
  <done>The proposal-to-ticket boundary demonstrably rejects excess exposure before any approval record or command authority exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove refreshed over-limit exposure cannot consume a ticket</name>
  <files>tests/integration/execution/test_approval_consumption.py</files>
  <read_first>Read `pa_agent/trading/application/approval.py`, `pa_agent/trading/application/submission.py`, `pa_agent/trading/persistence/sqlite_ledger.py` method `consume_valid_ticket_and_begin_outbound`, `tests/integration/execution/test_approval_consumption.py`, and `02-07-SUMMARY.md`.</read_first>
  <behavior>
    - A ticket issued from within-limit facts is retried against a second complete fresh evidence bundle whose selected-target position makes projected exposure exceed the cap.
    - Consumption returns no outbound value, persists the binding-invalidated terminal event with the risk-reassessment reason, retains zero commands and claims, and invokes no gateway submit method.
  </behavior>
  <action>Make `_EvidenceAndSubmissionGateway` supply a scripted account-observation sequence so issuance consumes an in-limit first response and `ApprovalService.consume_ticket()` receives a fresh over-limit second response. Add the real-SQLite regression with the normal consume path, assert `RiskEngine` evaluates the second bundle as excess exposure, `consume_ticket()` returns `None`, the ticket terminal event is `binding_invalidated` with `risk_reassessment_rejected`, and `order_commands`, `submission_claims`, and recorded outbound submissions remain zero. Do not call `SubmissionCoordinator` in this rejection case; retain the existing test that proves it is the sole actual gateway submit caller for an accepted outbound value.</action>
  <verify><automated>.venv/bin/pytest -q tests/integration/execution/test_approval_ticket_issuance.py tests/integration/execution/test_approval_consumption.py</automated></verify>
  <acceptance_criteria>The consumer regression requires a newly fetched over-limit account state to terminate the pending ticket before any durable command or outbound authorization is created.</acceptance_criteria>
  <done>Both ticket issuance and ticket consumption have end-to-end SQLite proof that excess exposure cannot issue or consume authorization.</done>
</task>

</tasks>

## Artifacts this phase produces

- An over-limit-exposure no-ticket regression in `tests/integration/execution/test_approval_ticket_issuance.py`.
- A refreshed-over-limit no-consumption regression in `tests/integration/execution/test_approval_consumption.py`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted risk result -> pending ticket | A rejected total-exposure assessment cannot become reviewable authorization. |
| refreshed evidence -> ticket consumption | A stale ticket cannot bypass newly observed account exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-20 | Elevation | proposal-to-ticket wiring | high | mitigate | Real SQLite test verifies exposure rejection stops ticket creation, command persistence, and claims. |
| T-02-21 | Tampering/Elevation | approval refresh and consumption | high | mitigate | Fresh position evidence triggers risk reassessment, durable ticket invalidation, and no outbound handoff under D-05/D-10. |
| T-02-SC | Tampering | package installs | low | accept | No package-manager operation is planned. |
</threat_model>

<verification>
Run `.venv/bin/pytest -q tests/integration/execution/test_approval_ticket_issuance.py tests/integration/execution/test_approval_consumption.py` and `.venv/bin/ruff check` on those two test files.
</verification>

<success_criteria>
An over-limit target-bound position cannot create a ticket, and a newly over-limit position invalidates an existing ticket before it can produce a command, claim, outbound authorization, or gateway submission.
</success_criteria>

<output>
Create `.planning/phases/02-approval-and-risk-boundary/02-11-SUMMARY.md` when done.
</output>
